# Backend SIGECOP - copia a .env y ajusta valores

NODE_ENV=development
PORT=4000

# Base de datos (usa "db" como host dentro de docker-compose; "localhost" si corres en bare-metal)
DB_HOST=localhost
DB_PORT=5432
DB_USER=sigecop
DB_PASSWORD=sigecop_dev_2026
DB_NAME=sigecop_db

# Autenticación
JWT_SECRET=cambiar_este_secreto_en_produccion
JWT_EXPIRES_IN=8h

# CORS
CORS_ORIGIN=http://localhost:5173
