// alta-v2: la metadata académica (badge HU + sprint) se eliminó junto con el "modo
// proyecto". Componente NEUTRALIZADO: no renderiza. Ya no lo usa HeaderVista; se conserva
// (con su firma) por si alguna página lo importa, para no romper el build.
export default function BadgeSprint() {
  return null;
}
