# Plan de pruebas NEGATIVAS (validaciones y gates) — SIGECOP · 24-jun-2026

> **Para ejecución por el equipo humano.** Todo lo que el sistema debe **RECHAZAR o BLOQUEAR** (🔴). Campos y reglas de `CATALOGO_CAMPOS_SISTEMA.md`.

> **[BUG CONOCIDO #N]** en la columna *Resultado esperado* = gate que HOY falla (ver `REPORTE_PRUEBAS_EXHAUSTIVAS_24jun.md`). El comportamiento que sigue al marcador es lo que el sistema hace HOY (incorrecto): **no es error del tester**, es un bug ya documentado. Sin el marcador, el caso debe pasar (el sistema sí bloquea).

## Cuentas demo (contraseña común `Sigecop2026!`)

| Cuenta | Rol | Papel en el flujo |
|---|---|---|
| `residente@sigecop.test` | residente | Da de alta, abre/firma bitácora, **autoriza/rechaza** estimación, finiquito |
| `contratista@sigecop.test` | contratista (**superintendente**) | Integra/presenta/reingresa estimación, registra avance |
| `supervision@sigecop.test` | supervisión | Observa, firma, **turna** la estimación |
| `dependencia@sigecop.test` | dependencia | Crea/autoriza convenios, padrón, roster, finiquito |
| `finanzas@sigecop.test` | finanzas | Tránsito a pago + **registra el pago** (único) |

**Contratos demo:** `SOP-2026-001..010` (alta completa + bitácora; `SOP-2026-001` con convenio v1/v2) y `PRUEBA-HU-01..24` (uno por HU en su estado). Login: `#login-usuario`, `#login-password`, botón «Iniciar sesión». Al entrar a una pantalla de contrato sale el modal `modal-elegir-contrato` → elige el contrato (se hereda).

## Cómo usar este plan

- Columnas **Resultado obtenido** y **Estatus** van vacías (`▢`): las llena el tester al ejecutar. En **Estatus** anota ✅ (pasó) / ❌ (falló) / ⚠️ (bloqueado).
- Cada caso trae la cuenta, la pantalla (cómo llegar), los datos exactos (campo→valor con su testid) y el resultado esperado observable.


---

## Validaciones de campos (auth/registro/login/empresas + alta wizard)

Casos NEGATIVOS (🔴 el sistema debe rechazar/bloquear). Login y registro: pantalla pública en `/` (login con `#login-usuario` / `#login-password` / botón "Iniciar sesión"; registro vía enlace "Regístrate" → `link-registro`). Empresas: como `dependencia@sigecop.test` en `/admin/empresas`. Alta wizard: como `residente@sigecop.test` en `/contratos/alta` (ruta libre, no pide modal de contrato). Pass común de todas las cuentas demo: `Sigecop2026!`.

| ID | Caso de prueba | Cuenta (rol) | Pantalla / cómo llegar | Datos de entrada (campo → valor) | Resultado esperado | Resultado obtenido | Estatus |
|---|---|---|---|---|---|---|---|
| NEG-CAMPO-01 | Login con ambos campos vacíos | (sin sesión) | `/` vista login (pantalla inicial) | `#login-usuario → (vacío)`; `#login-password → (vacío)` → botón "Iniciar sesión" | No autentica. Back exige ambos: 400 "email y password son requeridos" (auth.controller.js:27-29); en UI no se concede sesión | ▢ | ▢ |
| NEG-CAMPO-02 | Login con correo correcto y password en blanco | (sin sesión) | `/` vista login | `#login-usuario → residente@sigecop.test`; `#login-password → (vacío)` → "Iniciar sesión" | Rechazo: 400 "email y password son requeridos" (auth.controller.js:27); no entra | ▢ | ▢ |
| NEG-CAMPO-03 | Login con correo capitalizado distinto (case-sensitive backend) | (sin sesión) | `/` vista login (probar vía POST directo o sin pre-normalizar) | `#login-usuario → Residente@Sigecop.test`; `#login-password → Sigecop2026!` | Debería entrar (register guarda en minúsculas → login normaliza igual). [BUG CONOCIDO #16: el backend NO normaliza; `WHERE email=$1` crudo devuelve 401 "Credenciales inválidas"; en la UI solo "funciona" porque SeleccionRol.jsx:40 baja a minúsculas antes de enviar] | ▢ | ▢ |
| NEG-CAMPO-04 | Registro: nombre de una sola palabra (no cumple ≥2) | (sin sesión) | `/` → "Regístrate" (`link-registro`) | `reg-nombres → Juan`; `reg-apellidos → (vacío)`; `reg-email → juan.solo@test.com`; `reg-rol → residente`; `reg-password → Sigecop2026!`; `reg-password2 → Sigecop2026!` → enviar | Bloqueo: "Captura tu nombre y apellido(s)…" en `registro-error` (front SeleccionRol.jsx:150-154; back 400 art. 123 RLOPSRM, auth.controller.js:91-93) | ▢ | ▢ |
| NEG-CAMPO-05 | Registro: contraseña menor a 8 caracteres | (sin sesión) | `/` → "Regístrate" (`link-registro`) | `reg-nombres → Ana`; `reg-apellidos → López`; `reg-email → ana.lopez@test.com`; `reg-rol → residente`; `reg-password → Sige1`; `reg-password2 → Sige1` → enviar | Bloqueo: "La contraseña debe tener al menos 8 caracteres." en `registro-error` (front SeleccionRol.jsx:155-158; back 400 auth.controller.js:94-96) | ▢ | ▢ |
| NEG-CAMPO-06 | Registro: las contraseñas no coinciden | (sin sesión) | `/` → "Regístrate" (`link-registro`) | `reg-nombres → Ana`; `reg-apellidos → López`; `reg-email → ana.lopez2@test.com`; `reg-rol → residente`; `reg-password → Sigecop2026!`; `reg-password2 → Sigecop2027!` → enviar | Bloqueo: "Las contraseñas no coinciden." en `registro-error` (front SeleccionRol.jsx:159-162; el back ni recibe la confirmación) | ▢ | ▢ |
| NEG-CAMPO-07 | Registro: contratista sin elegir empresa (obligatoria) | (sin sesión) | `/` → "Regístrate" (`link-registro`) | `reg-nombres → Carlos`; `reg-apellidos → Ruiz`; `reg-email → carlos.ruiz@test.com`; `reg-rol → contratista`; `reg-empresa-select → (sin selección)`; `reg-password → Sigecop2026!`; `reg-password2 → Sigecop2026!` → enviar | Bloqueo: "Elige tu empresa: es obligatoria para contratista y supervisión." (front SeleccionRol.jsx:171-174; back 400 auth.controller.js:114-117) | ▢ | ▢ |
| NEG-CAMPO-08 | Registro: correo en blanco / solo espacios (POST directo, evade type=email) | (sin sesión) | `/` → "Regístrate" (o POST `/api/auth/register`) | `reg-nombres → Juan`; `reg-apellidos → Pérez`; `reg-email → "   "` (3 espacios); `reg-rol → residente`; `reg-password → Sigecop2026!`; `reg-password2 → Sigecop2026!` → enviar | Debería rechazar 400 (correo obligatorio, no vacío tras trim). [BUG CONOCIDO #14: hoy NO bloquea — crea cuenta con email='' (201); un 2º registro con espacios choca el UNIQUE y da 409 "Ese correo ya está registrado" engañoso] | ▢ | ▢ |
| NEG-CAMPO-09 | Registro: correo sin formato (sin @ ni dominio), POST directo | (sin sesión) | `/` → "Regístrate" (o POST `/api/auth/register`) | `reg-nombres → Juan`; `reg-apellidos → Pérez`; `reg-email → esto-no-es-email`; `reg-rol → residente`; `reg-password → Sigecop2026!`; `reg-password2 → Sigecop2026!` → enviar | Debería rechazar 400 por formato inválido (backend = fuente de verdad). [BUG CONOCIDO #25: hoy NO valida formato — 201, cuenta creada con email='esto-no-es-email'; el `type=email` del front no protege el endpoint ante POST directo] | ▢ | ▢ |
| NEG-CAMPO-10 | Registro: correo ya existente (duplicado) | (sin sesión) | `/` → "Regístrate" (`link-registro`) | `reg-nombres → Otro`; `reg-apellidos → Usuario`; `reg-email → residente@sigecop.test`; `reg-rol → residente`; `reg-password → Sigecop2026!`; `reg-password2 → Sigecop2026!` → enviar | Bloqueo: 409 "Ese correo ya está registrado" (UNIQUE email, auth.controller.js:131-133) | ▢ | ▢ |
| NEG-CAMPO-11 | Registro: empresa nueva que ya existe en el catálogo (aviso anti-duplicado) | (sin sesión) | `/` → "Regístrate" (`link-registro`), rol contratista | `reg-rol → contratista`; `reg-empresa-select → __nueva__`; `reg-empresa-nueva → Constructora del Bajío S.A. de C.V.` (match fuerte: acentos/sufijos) | Aviso visible `reg-empresa-existente`: "✓ Ya existe «Constructora del Bajío, S.A. de C.V.»: mejor selecciónala" (empresa.js:28-53; SeleccionRol.jsx:295-302). NOTA: es AVISO, no bloqueo duro; el back deduplica con `resolverOCrearEmpresa` | ▢ | ▢ |
| NEG-CAMPO-12 | Padrón: fusionar empresa duplicada que tiene contratos | dependencia@sigecop.test | `/admin/empresas` → pestaña `tab-porvalidar` → `fusionar-<id>` de una empresa referenciada por un contrato (p. ej. "Constructora del Bajío") | `fusionar-<id_source>` → canonica_id = `<id_canónica>` | Debería fusionar reapuntando personas Y contratos y eliminar la duplicada. [BUG CONOCIDO #3: hoy devuelve HTTP 500 "Error interno" opaco — la FK `contratos_contratista_empresa_id_fkey` (NO ACTION) no se reapunta; transacción hace ROLLBACK, la empresa sigue existiendo. Las empresas SIN contratos sí fusionan] | ▢ | ▢ |
| NEG-CAMPO-13 | Alta P2: catálogo con clave de concepto duplicada | residente@sigecop.test | `/contratos/alta` → Paso 2 "Catálogo de conceptos" | `concepto-clave-0 → C-01`; `concepto-concepto-0 → Excavación`; `concepto-unidad-0 → m³`; `concepto-cantidad-0 → 100`; `concepto-pu-0 → 50`; `concepto-clave-1 → C-01` (repetida); `concepto-cantidad-1 → 10`; `concepto-pu-1 → 5` → intentar avanzar | Bloqueo: clave debe ser ÚNICA entre conceptos (front validarPaso(1) jsx:1540-1543; back 400 "clave repetida" UNIQUE(contrato_id,clave), art. 45 fr. IX RLOPSRM) | ▢ | ▢ |
| NEG-CAMPO-14 | Alta P3: programa que no cuadra al 100% por concepto | residente@sigecop.test | `/contratos/alta` → Paso 3 "Programa de obra" (con catálogo de 1 concepto cant=100) | `select-ciclo → mensual`; `celda-0-0 → 40`; `celda-0-1 → 30` (Σ=70 ≠ 100 contratado) → intentar avanzar | Bloqueo por descuadre: Σ por concepto = contratado (|restante|≤0.0005), si no rechaza (front jsx:1570-1578; back PROGRAMA_DESCUADRE 400, art. 45-A-X RLOPSRM + 52 LOPSRM). Cuadre exacto al centavo, sin parciales | ▢ | ▢ |
| NEG-CAMPO-15 | Alta P5: garantía con monto mayor al monto del contrato | residente@sigecop.test | `/contratos/alta` → Paso 5 "Garantías" (monto contrato derivado p. ej. $5,000.00) | `garantia-tipo-0 → Cumplimiento`; `garantia-afianzadora-0 → Afianzadora X`; `garantia-poliza-0 → P-001`; `garantia-monto-0 → 9999999`; `garantia-vigencia-0 → 2027-12-31` → intentar avanzar/guardar | Bloqueo: monto póliza ≤ monto del contrato, si no rechaza (front validarPaso(4) jsx:1627; back 400 "gm ≤ monto contrato", controller:181). Aviso adicional `modal-exceso-garantia` si excede el % esperado | ▢ | ▢ |
| NEG-CAMPO-16 | Alta P5: garantía con vigencia ya vencida | residente@sigecop.test | `/contratos/alta` → Paso 5 "Garantías" | `garantia-tipo-0 → Cumplimiento`; `garantia-afianzadora-0 → Afianzadora X`; `garantia-poliza-0 → P-002`; `garantia-monto-0 → 500`; `garantia-vigencia-0 → 2020-01-01` (pasada) → intentar avanzar | Bloqueo: vigencia ≥ hoy (front validarPaso(4) jsx:1622; back 400 "ya está vencida", art. 48 LOPSRM, controller:188-193) | ▢ | ▢ |
| NEG-CAMPO-17 | Alta P5: garantía con tipo arbitrario fuera del catálogo legal (POST directo) | residente@sigecop.test | Paso 5 (la UI solo ofrece Cumplimiento/Anticipo/Vicios ocultos/Otra) o POST `/api/garantias/contrato/:id` | `tipo → CualquierCosaInventada`; `afianzadora → X`; `monto → 50000`; `vigencia → 2027-12-31` | Debería limitar al catálogo de ley (anticipo art.48-I / cumplimiento art.48-II / vicios_ocultos art.66). [BUG CONOCIDO #17: hoy NO hay whitelist — 201, se guarda como tipo 'cualquiercosainventada'; solo se exige no-vacío. Afecta tanto HU-02 como el alta congelada] | ▢ | ▢ |
| NEG-CAMPO-18 | Alta P6: plan de amortización cuya suma ≠ monto del anticipo | residente@sigecop.test | `/contratos/alta` → Paso 6 "Plan de amortización" (visible con %anticipo>0; montoAnticipo p. ej. $1,500.00) | `anticipo-input → 30` (paso previo); `plan-monto-0 → 500`; `plan-monto-1 → 500` (Σ=$1,000 ≠ $1,500) → intentar avanzar/guardar | Bloqueo: Σ plan = montoAnticipo EXACTO al centavo, si no rechaza (front validarPaso(5) jsx:1662; back 400 Σ=anticipo, controller:275, art. 138 párr. 3 RLOPSRM) | ▢ | ▢ |
| NEG-CAMPO-19 | Alta P5: anticipo >30% sin PDF de autorización | residente@sigecop.test | `/contratos/alta` → Paso 5 "Garantías" | `anticipo-input → 50`; `anticipo-pdf-input → (sin archivo)` → intentar avanzar/guardar | Bloqueo del avance Y del guardado: PDF de autorización OBLIGATORIO si %anticipo>30% (front validarPaso(4) jsx:1602; art. 50 fr. IV LOPSRM) | ▢ | ▢ |
| NEG-CAMPO-20 | Alta P1: fecha de inicio fuera de rango (día imposible) | residente@sigecop.test | `/contratos/alta` → Paso 1 "Datos generales" (o POST `/api/contratos`) | `dg-folio → OBRA-NEG-26`; `dg-objeto → Prueba`; `dg-dependencia → (válida)`; `dg-plazo → 90`; `dg-fecha → 2026-02-30` (también 2026-13-45 / 2026-00-00) | Debería rechazar 400 "fecha inválida". [BUG CONOCIDO #26: hoy devuelve HTTP 500 "Error interno" — Postgres lanza SQLSTATE 22008 y el catch solo mapea 22007/22P02 a 400 (controller:512); 'not-a-date' sí da 400 correcto] | ▢ | ▢ |
| NEG-CAMPO-21 | Alta P1: folio de contrato ya existente | residente@sigecop.test | `/contratos/alta` → Paso 1 "Datos generales" | `dg-folio → SOP-2026-001` (ya existe); `dg-objeto → Prueba`; `dg-dependencia → (válida)`; `dg-plazo → 90`; `dg-fecha → 2026-07-01` → intentar guardar | Bloqueo: 409 "El folio ya existe" (UNIQUE folio, err 23505 controller:500); el front lo mapea al campo `dg-folio` y regresa al tab 0 (jsx:1844) | ▢ | ▢ |
| NEG-CAMPO-22 | Alta P1: plazo en días no entero / cero | residente@sigecop.test | `/contratos/alta` → Paso 1 "Datos generales" | `dg-folio → OBRA-NEG-27`; `dg-objeto → Prueba`; `dg-dependencia → (válida)`; `dg-plazo → 0` (o 12.5); `dg-fecha → 2026-07-01` → intentar avanzar | Bloqueo: plazo entero >0 (front validarPaso(0) jsx:1526; back 400 "plazoDias debe ser un entero mayor a 0", controller:60-63) | ▢ | ▢ |

---

## Gates legales (art. 54, 118, 64, 122, 50, 24, 102)

Contexto: pruebas NEGATIVAS donde el sistema DEBE bloquear/avisar. Se entra por `#login-usuario` / `#login-password` / botón «Iniciar sesión». Al abrir una pantalla de contrato sale `modal-elegir-contrato` → clic en `modal-contrato-<id>` (se hereda `banner-contrato-activo`). Contratos: SOP-2026-001..010 y PRUEBA-HU-01..24; contratos CERRADOS de demo = PRUEBA-HU-17 y SOP-2026-010. Pass común: `Sigecop2026!`.

| ID | Caso de prueba | Cuenta (rol) | Pantalla / cómo llegar | Datos de entrada (campo → valor) | Resultado esperado | Resultado obtenido | Estatus |
|---|---|---|---|---|---|---|---|
| NEG-LEY-01 | Registrar avance que EXCEDE la cantidad contratada del concepto (art. 118) | contratista@sigecop.test (contratista/superintendente) | Avance · /seguimiento/trabajos-terminados → modal-contrato → SOP-2026-001; ciclo Avance · HU-06 | cap-concepto → un concepto del catálogo (p.ej. CONC-01); cap-periodo → periodo en curso; cap-cantidad → 999999 (mayor que lo contratado); cap-foto-evidencia → JPG válido | Borde rojo en cap-cantidad (validación en vivo excede118) y botón Registrar deshabilitado; si se fuerza por API, 409 «Excede lo contratado (art. 118)» (art. 118 RLOPSRM). Σ ejecutado vigente + nueva ≤ contratado | ▢ | ▢ |
| NEG-LEY-02 | Registrar avance en periodo FUTURO (aún no inicia) | contratista@sigecop.test (contratista) | Avance · /seguimiento/trabajos-terminados → SOP-2026-001 | cap-concepto → CONC-01; cap-periodo → último periodo (inicio > hoy); cap-cantidad → 5; cap-foto-evidencia → PNG | 409 «aún no inicia; no se puede reportar avance de trabajo no ejecutado» (FIX 22-jun, validar periodo actual). No se crea avance | ▢ | ▢ |
| NEG-LEY-03 | Integrar estimación de un periodo NO vencido (art. 54) | contratista@sigecop.test (contratista) | Estimación · /estimaciones/integracion (HU-12) → PRUEBA-HU-12 | periodo-inicio → 2026-06-01; periodo-fin → 2099-12-31 (periodo aún no cierra) | 409 «periodoNoCerrado»: el periodo debe estar vencido (periodo_fin < CURRENT_DATE) antes de integrar (art. 54 LOPSRM). No integra | ▢ | ▢ |
| NEG-LEY-04 | Integrar estimación con periodo mayor a 1 mes / fin anterior al inicio (art. 54) | contratista@sigecop.test (contratista) | Estimación · /estimaciones/integracion (HU-12) → PRUEBA-HU-12 | periodo-inicio → 2026-03-01; periodo-fin → 2026-05-15 (>1 mes) | 400: periodo_fin ≤ inicio+1 mes (art. 54). Variante fin<inicio (periodo-fin → 2026-02-28) → 400. No integra | ▢ | ▢ |
| NEG-LEY-05 | Badge de plazo de revisión > 20 días desde presentación (art. 54) — visual | residente@sigecop.test (residente) | Presentación/Revisión de estimación (HU-15) → contrato con estimación 'enviada' hace >15 días (SOP-2026-001) | (sin captura) observar semaforo-plazo-{id} / sello-presentacion-{id} | Semáforo de 15 días de revisión derivado de enviada_en marca VENCIDO/rojo cuando se rebasa el plazo (art. 54 LOPSRM). Es aviso visual, NO bloquea | ▢ | ▢ |
| NEG-LEY-06 | Emitir nota de bitácora SIN apertura firmada por TODOS (art. 122/123) | residente@sigecop.test (residente) | Bitácora · /bitacora/notas (HU-09) → PRUEBA-HU-08 (apertura sin firmar completa) | select-tipo → res_autoriza; input-contenido → "Nota de prueba" | Botón btn-emitir deshabilitado (candado UI gate-emision); por API 409 «No se pueden emitir notas hasta que la apertura esté firmada por TODOS (faltan N firma(s))» (art. 122/123 RLOPSRM). No emite | ▢ | ▢ |
| NEG-LEY-07 | Registrar avance en contrato SIN bitácora abierta (art. 122) | contratista@sigecop.test (contratista) | Avance · /seguimiento/trabajos-terminados → contrato recién dado de alta SIN apertura (PRUEBA-HU-01) | cap-concepto → CONC-01; cap-periodo → en curso; cap-cantidad → 3; cap-foto-evidencia → JPG | 409 «requiere bitácora abierta» (art. 122 RLOPSRM, trabajos.controller.js:256-259). El POST exige bitácora abierta. No registra | ▢ | ▢ |
| NEG-LEY-08 | Registrar convenio modificatorio SIN bitácora abierta (art. 122) | dependencia@sigecop.test (dependencia) | Convenios · /contratos/modificatorios (HU-03) → contrato SIN apertura (PRUEBA-HU-01) | cm-tipo → plazo; cm-plazo-nuevo → 120; cm-motivo → "Ampliación"; cm-oficio → OF-001 | 409: exige bitácora abierta antes de registrar (P23, art. 122 RLOPSRM, convenios.controller.js:170). No registra el convenio | ▢ | ▢ |
| NEG-LEY-09 | Anticipo > 30% SIN PDF de autorización (art. 50 fr. IV) | residente@sigecop.test (residente) | Alta · /contratos/alta → Paso 5 Garantías (tab 4) | anticipo-input → 31; (NO subir anticipo-pdf-input) | Bloquea avance Y guardado: indicador anticipo-pdf-requerido / validarPaso(4); por API 409 inmutable si falta el PDF de autorización del anticipo (art. 50 fr. IV LOPSRM). No avanza el wizard | ▢ | ▢ |
| NEG-LEY-10 | Generar instrucción de pago SIN suficiencia presupuestal / excede techo (art. 24) | contratista@sigecop.test (contratista) | Tránsito a pago · /pagos/transito (HU-20) Paso 3 → estimación autorizada cuyo neto excede el techo disponible | select-estimacion → estimación 'autorizada'; btn-generar-instruccion | 409: neto > disponible bajo lock FOR UPDATE del techo (art. 24 LOPSRM); bloqueos UI «sin techo / excede techo» deshabilitan btn-generar-instruccion. No genera instrucción | ▢ | ▢ |
| NEG-LEY-11 | Convenio con variación de monto/plazo > 25% (art. 102) — AVISO, no bloquea | dependencia@sigecop.test (dependencia) | Convenios · /contratos/modificatorios (HU-03) → SOP-2026-001 (con bitácora) | cm-tipo → plazo; cm-plazo-nuevo → valor que sube el plazo +30%; cm-motivo → "Atraso justificado"; cm-oficio → OF-2026-30 | Aviso aviso-sfp «revisión SFP art. 102 RLOPSRM» (informativo). El registro SÍ procede (art. 102 NO bloquea, es aviso). No es rechazo | ▢ | ▢ |
| NEG-LEY-12 | Autorizar convenio > 25% SIN oficio PDF cargado (art. 102) | dependencia@sigecop.test (dependencia) | Convenios · /contratos/modificatorios (HU-03) → SOP-2026-001, sobre convenio 'registrado' con variación >25% sin oficio adjunto | conv-autorizar-{id} (sin haber subido conv-oficio-subir-{id}) | 409: guardrail art. 102 exige el oficio de aprobación YA cargado para autorizar variación >25% (convenios.controller.js:405-409). No autoriza | ▢ | ▢ |
| NEG-LEY-13 | Emitir NOTA en contrato CERRADO (art. 64) — gate correcto | residente@sigecop.test (residente) | Bitácora · /bitacora/notas (HU-09) → PRUEBA-HU-17 (estado='cerrado') | select-tipo → res_suspension; input-contenido → "Acto posterior al cierre" | 409 «El contrato ya está cerrado (finiquito elaborado); no se emiten notas… (art. 64 LOPSRM)». Bloqueado correctamente | ▢ | ▢ |
| NEG-LEY-14 | EDITAR garantía en contrato CERRADO (art. 64) | dependencia@sigecop.test (dependencia) | Fianzas · /contratos/fianzas (HU-02) → SOP-2026-010 (estado='cerrado'); modal póliza editar | mp-afianzadora → "Afianzadora Nueva"; mp-monto-edit → 12345; mp-vigencia → 2027-12-31; guardar (PUT /garantias/:id) | DEBERÍA: 409 solo-lectura, no se edita la garantía de un contrato finiquitado (art. 64 LOPSRM). [BUG CONOCIDO #5: hoy NO bloquea — editarGarantia devuelve 200 y persiste el cambio] | ▢ | ▢ |
| NEG-LEY-15 | SUBIR PDF de póliza en contrato CERRADO (art. 64) | dependencia@sigecop.test (dependencia) | Fianzas · /contratos/fianzas (HU-02) → SOP-2026-010 (cerrado); botón subir PDF de la póliza | mp-pdf-poliza → poliza_test.pdf (PDF válido) (POST /garantias/:id/pdf) | DEBERÍA: 409 solo-lectura, no se sube el PDF de póliza tras el finiquito (art. 64 LOPSRM). [BUG CONOCIDO #5: hoy NO bloquea — subirPdfGarantia devuelve 201 y carga el PDF] | ▢ | ▢ |
| NEG-LEY-16 | VINCULAR (responder) nota en contrato CERRADO (art. 64) | residente@sigecop.test (residente) | Bitácora · /bitacora/notas (HU-09) → PRUEBA-HU-17 (cerrado); btn-responder-{numero} sobre una nota existente | select-tipo → res_suspension; input-contenido → "Respuesta post-cierre" (POST /bitacora/notas/:id/vincular) | DEBERÍA: 409 art. 64 (el cierre es solo-lectura; vincular inserta nota NUEVA igual que emitir). [BUG CONOCIDO #9: hoy NO bloquea — vincularNota devuelve 201 y crea nota nueva en el contrato finiquitado] | ▢ | ▢ |
| NEG-LEY-17 | ANULAR la nota de FINIQUITO en contrato CERRADO (art. 64 + inmutabilidad) | residente@sigecop.test (residente, emisor) | Bitácora · /bitacora/notas (HU-09) → PRUEBA-HU-17 (cerrado); btn-anular-{numero} sobre la nota tipo 'finiquito' | textarea correccion → "dice/debe decir cerrado"; btn-confirmar-anular-{numero} (POST /bitacora/notas/:id/anular) | DEBERÍA: 409 — el contrato está cerrado (art. 64 LOPSRM) y la nota de finiquito es el acta de cierre, tan inmutable como la apertura. [BUG CONOCIDO #10: hoy NO bloquea — anularNota devuelve 201 y anula el acta de finiquito] | ▢ | ▢ |
| NEG-LEY-18 | FIRMAR nota en contrato CERRADO (art. 64) | contratista@sigecop.test (contratista) | Bitácora · /bitacora/notas (HU-09) → PRUEBA-HU-17 (cerrado); btn-firmar-nota-{numero} sobre una nota 'emitida' | (acción de firma; POST /bitacora/notas/:id/firmar) | DEBERÍA: 409 art. 64 (no se firman/aceptan notas en un contrato finiquitado). [BUG CONOCIDO #19: hoy NO bloquea — firmarNota permite firmar la nota en el contrato cerrado] | ▢ | ▢ |
| NEG-LEY-19 | PAGAR una estimación 'autorizada' de un contrato CERRADO (art. 64) | finanzas@sigecop.test (finanzas) | Pago · /pagos/registro (HU-21) → PRUEBA-HU-17 (cerrado, con est #2 'autorizada' no pagada) | pago-estimacion → est #2 (autorizada); pago-referencia-spei → 1234567890; pago-cfdi → CFDI-001; pago-fecha-factura → 2026-06-20; registrar (POST /pagos) | DEBERÍA: 409 — el contrato cerrado es solo-lectura, el saldo se liquida ÚNICAMENTE por el finiquito; pagar la est por separado es doble liquidación (art. 64 LOPSRM). [BUG CONOCIDO #2: hoy NO bloquea — registrarPago devuelve 201, paga $208,500 ya contemplados en el saldo del finiquito → doble liquidación] | ▢ | ▢ |
| NEG-LEY-20 | Convenio: REDUCIR la cantidad de un concepto original (art. 118 / congelado) | dependencia@sigecop.test (dependencia) | Convenios · /contratos/modificatorios (HU-03) → SOP-2026-001 (con bitácora); editor de catálogo | cm-tipo → programa; cm-concepto-cantidad-N → valor MENOR que lo ya estimado en un concepto original | 400/409: el concepto original está CONGELADO (cantidad/PU no cambian, 409) y no se reduce por debajo de lo ya estimado en estimaciones no rechazadas (400, art. 118 trata excedentes no reducciones). No registra | ▢ | ▢ |

---

## Gates de rol (matriz de permisos) + deep-link

Cuenta a usar la indicada por caso (pass común `Sigecop2026!`). Login: `#login-usuario`, `#login-password`, botón «Iniciar sesión». Tras login, navega a la pantalla indicada (si pide contrato, elige `OBRA-2026-PRUEBA-FINAL` en `modal-elegir-contrato` → `modal-contrato-<id>`). El gate vive en dos capas: la guarda de ruta (`App.jsx:62`, `nivelDe(hu,rol)===null` → `<Navigate to="/" replace />`) y `<SoloRol>` (App.jsx:71). Estos casos verifican que el rol equivocado **NO** pueda ejecutar la acción. Base: `frontend/src/data/permisos.js` (E=edita, C=consulta, null=sin acceso).

| ID | Caso de prueba | Cuenta (rol) | Pantalla / cómo llegar | Datos de entrada (campo → valor) | Resultado esperado | Resultado obtenido | Estatus |
|---|---|---|---|---|---|---|---|
| NEG-ROL-01 | Deep-link a Alta (HU-01) con rol sin acceso (finanzas=null) | finanzas@sigecop.test | Barra de URL → `http://localhost:5173/contratos/alta` | (sin captura; navegación directa por URL) | HU-01 finanzas=null (permisos.js:12). La guarda redirige a Inicio `/` (sin error, `<Navigate to="/">`). NO aparece el wizard de alta | ▢ | ▢ |
| NEG-ROL-02 | Finanzas intenta dar de alta contrato desde el modal | finanzas@sigecop.test | Sidebar → entrar a una pantalla de contrato → `modal-elegir-contrato` | (observar botones del modal) | El modal NO ofrece `modal-contrato-crear` ni `modal-contrato-crear-footer` (solo roles HU-01 nivel 'E' = residente). El sidebar tampoco muestra «Alta de contratos» para finanzas | ▢ | ▢ |
| NEG-ROL-03 | Contratista intenta autorizar/rechazar estimación (es acto de HU-15) | contratista@sigecop.test | URL → `http://localhost:5173/estimaciones/revision` | (navegación directa) | HU-15 contratista=null (permisos.js:29). Redirige a Inicio `/`. NO se ve el panel de resolución (`btn-autorizar`/`btn-rechazar` ausentes) | ▢ | ▢ |
| NEG-ROL-04 | Supervisión NO autoriza directo: solo observa/turna | supervision@sigecop.test | Sidebar → «Ciclo de estimación» → pestaña `pestana-revision` (`/estimaciones/revision`), contrato `OBRA-2026-PRUEBA-FINAL` | (en la estimación: intentar resolver) | HU-15 supervision='E' pero su acto es **turnar** a residencia (`btn-turnar`/observación), NO autorizar. La autorización/rechazo (`btn-autorizar`/`btn-rechazar`) es exclusiva de residente; supervisión no la ve | ▢ | ▢ |
| NEG-ROL-05 | Contratista intenta crear convenio modificatorio (HU-03 es de dependencia) | contratista@sigecop.test | URL → `http://localhost:5173/contratos/modificatorios` | (navegación directa) | HU-03 contratista='C' (consulta, permisos.js:14). Entra en SOLO-LECTURA: el formulario de captura del convenio está deshabilitado; el botón de registrar convenio no aparece/está disabled (crear es nivel 'E' = solo dependencia/residente) | ▢ | ▢ |
| NEG-ROL-06 | Supervisión intenta crear convenio (también es solo 'C') | supervision@sigecop.test | URL → `http://localhost:5173/contratos/modificatorios`, contrato `SOP-2026-001` (tiene convenio v1/v2) | (intentar registrar convenio) | HU-03 supervision='C' (permisos.js:14). Solo-lectura: puede consultar el convenio v1/v2 pero NO registrar uno nuevo (crear=dependencia/residente). Backend POST /api/convenios/contrato/:id → 403 si forzara la llamada | ▢ | ▢ |
| NEG-ROL-07 | Finanzas NO integra estimación (HU-12 finanzas=null) | finanzas@sigecop.test | URL → `http://localhost:5173/estimaciones/integracion` | (navegación directa) | HU-12 finanzas=null (permisos.js:23). Redirige a Inicio `/`. No aparece el wizard de integración (`wpaso-periodo`…) | ▢ | ▢ |
| NEG-ROL-08 | Finanzas NO presenta estimación (HU-13 finanzas=null) | finanzas@sigecop.test | URL → `http://localhost:5173/estimaciones/envio` | (navegación directa) | HU-13 finanzas=null (permisos.js:27). Redirige a Inicio `/`. El botón `btn-presentar-${e.id}` no es alcanzable | ▢ | ▢ |
| NEG-ROL-09 | Contratista intenta aprobar solicitudes de registro (admin de dependencia) | contratista@sigecop.test | URL → `http://localhost:5173/usuarios/solicitudes` | (navegación directa) | `<SoloRol roles={['dependencia']}>` (App.jsx:139). Redirige a Inicio `/`. NO se ve la bandeja (`btn-aprobar`/`btn-rechazar` ausentes) | ▢ | ▢ |
| NEG-ROL-10 | Residente intenta entrar al Padrón de empresas (solo dependencia) | residente@sigecop.test | URL → `http://localhost:5173/admin/empresas` | (navegación directa) | `<SoloRol roles={['dependencia']}>` (App.jsx:132). Redirige a Inicio `/`. NO se ven `tab-padron`/`validar-<id>`/`fusionar-<id>` | ▢ | ▢ |
| NEG-ROL-11 | Contratista intenta entrar a Roster / sustitución (solo dependencia/residente) | contratista@sigecop.test | URL → `http://localhost:5173/contratos/roster` | (navegación directa) | `<SoloRol roles={['dependencia','residente']}>` (App.jsx:142). Redirige a Inicio `/`. Sustitución de personas no accesible | ▢ | ▢ |
| NEG-ROL-12 | Finanzas intenta firmar/aperturar bitácora (HU-08 finanzas=null) | finanzas@sigecop.test | URL → `http://localhost:5173/bitacora/ambiente` | (navegación directa) | HU-08 finanzas=null; `<SoloRol roles={['residente','contratista','supervision']}>` (App.jsx:112). Redirige a Inicio `/`. No accede al wizard de apertura | ▢ | ▢ |
| NEG-ROL-13 | Finanzas firma una apertura de bitácora sin ser firmante (deep-link a Por firmar) | finanzas@sigecop.test | URL → `http://localhost:5173/bitacora/por-firmar` | (intentar `btn-firmar` en fila `fila-por-firmar[data-folio="OBRA-2026-PRUEBA-FINAL"]`) | `<SoloRol roles={['residente','contratista','supervision']}>` (App.jsx:85). Redirige a Inicio `/`; finanzas no está en la terna de firmantes. Si forzara POST /bitacora/.../firmar → 403 (no es firmante) | ▢ | ▢ |
| NEG-ROL-14 | Contratista intenta dar tránsito a pago / registrar pago (HU-21 contratista=null) | contratista@sigecop.test | URL → `http://localhost:5173/pagos/registro` | (navegación directa) | HU-21 contratista=null (permisos.js:35). Redirige a Inicio `/`. El registro de pago es exclusivo de finanzas; `btn-registrar-pago` no alcanzable | ▢ | ▢ |
| NEG-ROL-15 | Dependencia intenta registrar avance físico (HU-06 dependencia=null) | dependencia@sigecop.test | URL → `http://localhost:5173/seguimiento/trabajos-terminados` | (navegación directa) | HU-06 dependencia=null (permisos.js:17); `<SoloRol roles={['contratista','residente','supervision']}>` (avance, App.jsx:127). Redirige a Inicio `/`. `btn-registrar-avance` no aparece | ▢ | ▢ |
| NEG-ROL-16 | Supervisión intenta finiquitar/cerrar el contrato (HU-24, solo dependencia/residente) | supervision@sigecop.test | URL → `http://localhost:5173/contratos/finiquito` | (navegación directa) | `<SoloRol roles={['dependencia','residente']}>` (App.jsx:105). Redirige a Inicio `/`. Supervisión no cierra contratos. **[BUG CONOCIDO #7: el backend NO acota finiquito por empresa — una dependencia de OTRA empresa SÍ puede leer/cerrar el finiquito por id directo (GET/POST /api/finiquito/contrato/:id, fail-open); el front bloquea por rol pero el endpoint no por empresa]** | ▢ | ▢ |
| NEG-ROL-17 | Dependencia (ya autenticada) cambia el rol de un usuario YA ACTIVO vía aprobar | dependencia@sigecop.test | Bandeja `Solicitudes de registro` (`/usuarios/solicitudes`); o PATCH directo /api/usuarios/1/aprobar | `select-rol → finanzas` sobre `residente@sigecop.test` (id=1, estado='activo') | La UI solo lista pendientes, así que no debería poder cambiar a un activo; el backend debería exigir estado='pendiente' (404/409). **[BUG CONOCIDO #13: hoy NO valida estado — PATCH /api/usuarios/1/aprobar {rol:'finanzas'} sobre un usuario activo devuelve 200 y le cambia el rol; falta `AND estado='pendiente'`. No alcanzable desde la UI, solo por PATCH manual]** | ▢ | ▢ |
| NEG-ROL-18 | Finanzas intenta ver el Tablero de estimaciones (HU-17 finanzas=null) | finanzas@sigecop.test | URL → `http://localhost:5173/estimaciones/tablero` | (navegación directa) | HU-17 finanzas=null (permisos.js:31). Redirige a Inicio `/`. Finanzas no ve Tablero (ni en sidebar) | ▢ | ▢ |
| NEG-ROL-19 | Dependencia intenta entrar a la Curva de avance (HU-05 dependencia='C' pero el grupo Avance está gated) | dependencia@sigecop.test | URL → `http://localhost:5173/seguimiento/curva-avance` | (navegación directa) | El grupo Avance está gated a `[contratista,residente,supervision]` (App.jsx:127); HU-05 NO se promueve a dependencia. Redirige a Inicio `/`. La curva no es accesible para dependencia | ▢ | ▢ |

---

## Gates de estado + Inmutabilidad

Casos NEGATIVOS (el sistema debe RECHAZAR o BLOQUEAR). Login en `#login-usuario` / `#login-password` (pass común `Sigecop2026!`). Al entrar a una pantalla de contrato sale `modal-elegir-contrato` → elige `modal-contrato-<id>` (se hereda en `banner-contrato-activo`). Donde un gate hoy falta, el "Resultado esperado" indica el comportamiento correcto y luego `[BUG CONOCIDO #N]` para que el tester no lo confunda con error propio.

| ID | Caso de prueba | Cuenta (rol) | Pantalla / cómo llegar | Datos de entrada (campo → valor) | Resultado esperado | Resultado obtenido | Estatus |
|---|---|---|---|---|---|---|---|
| NEG-ESTADO-01 | Presentar una estimación que aún está 'integrada' sin haberla integrado primero — no aplica: probar presentar una ya 'enviada' (re-presentar) | contratista@sigecop.test (contratista/superintendente) | Presentación de la estimación · `/estimaciones/envio` → modal elige PRUEBA-HU-13 (id 6937); pestaña Presentar | acción `btn-presentar-{id}` sobre una estimación ya en estado 'enviada' | El botón `btn-presentar-{id}` solo aparece si estado='integrada'; sobre una 'enviada'/'autorizada'/'pagada' NO se ofrece, y el backend rechaza re-presentar con 409 (estimaciones-ciclo.controller.js: solo desde 'integrada', art. 54 LOPSRM) | ▢ | ▢ |
| NEG-ESTADO-02 | Autorizar una estimación SIN que supervisión la haya turnado a residencia | residente@sigecop.test (residente) | Recepción/revisión · `/estimaciones/revision` → modal elige PRUEBA-HU-15 (id 6939); `select-estimacion` una estimación 'enviada' no turnada | `btn-autorizar` (residencia) | Botón `btn-autorizar` deshabilitado (`puedeResolver` exige turnada); por API 409 'turnado previo obligatorio' (residencia exige EXISTS turnado_a='residencia', art. 54 LOPSRM). El flujo es secuencial supervisión→turna→residencia | ▢ | ▢ |
| NEG-ESTADO-03 | Rechazar como residencia una estimación aún NO turnada | residente@sigecop.test (residente) | `/estimaciones/revision` → PRUEBA-HU-15 (6939); `select-estimacion` 'enviada' no turnada | `motivo-rechazo` → "Generadores incompletos"; `btn-rechazar` | `btn-rechazar` deshabilitado sin turnado; backend 409 (residencia exige turnado previo). Solo supervisión puede rechazar directo sin turnar (`motivo-rechazo-supervision`) | ▢ | ▢ |
| NEG-ESTADO-04 | Turnar a residencia con observaciones SIN marcar 'sin observaciones' ni capturar ninguna | supervision@sigecop.test (supervisión) | `/estimaciones/revision` → PRUEBA-HU-15 (6939); estimación 'enviada' no turnada, cero observaciones | `chk-sin-observaciones` → desmarcado; `btn-turnar` | `btn-turnar` deshabilitado (`puedeTurnar = totalObs>0 OR sinObservaciones`); backend 409 'turnar requiere n>0 o sin_observaciones=true' (estimaciones-ciclo.controller.js:459-462) | ▢ | ▢ |
| NEG-ESTADO-05 | Pagar una estimación que NO está 'autorizada' (estado 'enviada' o 'integrada') | finanzas@sigecop.test (finanzas) | Registro de pago · `/pagos/registro` → modal elige PRUEBA-HU-13 (6937); `pago-estimacion` | `pago-estimacion` → estimación en estado 'enviada'/'integrada' | El selector `pago-estimacion` solo lista estado='autorizada' (PAGABLES); una no-autorizada no aparece. Por API 409 'SOLO autorizada' (pagos.controller.js:76, art. 54 LOPSRM) | ▢ | ▢ |
| NEG-ESTADO-06 | Pagar dos veces la MISMA estimación 'autorizada' (doble pago) | finanzas@sigecop.test (finanzas) | `/pagos/registro` → contrato con estimación 'autorizada' ya pagada; intentar registrar pago de nuevo | `pago-estimacion` → estimación ya 'pagada'; `pago-referencia` → 1234567; `pago-cfdi` → UUID; `pago-fecha` / `pago-fecha-factura` → hoy | Estimación 'pagada' no aparece en `pago-estimacion`; por API 409 'NO pagada' / no-doble-pago (UNIQUE + FOR UPDATE, pagos.controller.js:70,78,128) | ▢ | ▢ |
| NEG-ESTADO-07 | Pagar una estimación 'autorizada' de un contrato YA CERRADO (finiquitado) | finanzas@sigecop.test (finanzas) | `/pagos/registro` → modal elige PRUEBA-HU-17 (id 6941, estado='cerrado'); `pago-estimacion` la est #2 'autorizada' | `pago-estimacion` → est #2 (autorizada, no pagada); `pago-referencia` → 1234567890; `pago-cfdi` → UUID; `pago-fecha`/`pago-fecha-factura` → hoy | El contrato cerrado es solo-lectura (art. 64 LOPSRM): el saldo se liquida SOLO por el finiquito; pagar la est #2 por separado debe rechazarse 409. **[BUG CONOCIDO #2 y #12: hoy NO bloquea — el form lista la estimación y el backend acepta el pago (201), causando doble liquidación del mismo saldo]** | ▢ | ▢ |
| NEG-ESTADO-08 | Generar instrucción de pago (tránsito) sobre un contrato cerrado | contratista@sigecop.test (contratista) | Tránsito a pago · `/pagos/transito` → modal elige PRUEBA-HU-17 (6941, cerrado); `select-estimacion` est autorizada; paso 3 | `select-estimacion` → est #2 autorizada; `btn-generar-instruccion` | 409 'El contrato ya está cerrado… no se generan nuevas instrucciones de pago (art. 64 LOPSRM)' (instruccion-pago.controller.js:286). Este gate SÍ existe (contraste con el pago directo de NEG-ESTADO-07) | ▢ | ▢ |
| NEG-ESTADO-09 | Finiquitar un contrato SIN bitácora aperturada | dependencia@sigecop.test (dependencia) | Finiquito · `/contratos/finiquito` → modal elige un contrato PRUEBA-HU sin bitácora abierta | `btn-abrir-cierre` | Botón `btn-abrir-cierre` deshabilitado (`disabled={!data.tiene_bitacora}`, banner `finiquito-sin-bitacora`); por API 409 'Abre la bitácora… antes' (finiquito.controller.js:120-121) | ▢ | ▢ |
| NEG-ESTADO-10 | Finiquitar dos veces el mismo contrato (ya cerrado) | dependencia@sigecop.test (dependencia) | `/contratos/finiquito` → modal elige PRUEBA-HU-17 (6941, ya cerrado); `btn-abrir-cierre` → `btn-confirmar-cierre` | `finiquito-ajustes` → 0.00; `btn-confirmar-cierre` | 409 'El contrato ya está cerrado' / 'Este contrato ya tiene finiquito' (1 por contrato, finiquito.controller.js:116-118, art. 172 RLOPSRM). El formulario de cierre no debe mostrarse en un contrato cerrado | ▢ | ▢ |
| NEG-ESTADO-11 | Reingresar una estimación que NO está 'rechazada' (p. ej. 'autorizada' o 'integrada') | contratista@sigecop.test (contratista) | Reingreso · `/estimaciones/reingreso` → modal elige un contrato con estimación 'autorizada'; `select-estimacion` | `select-estimacion` → estimación 'autorizada' | El selector `select-estimacion` solo lista 'rechazada' sin reingreso previo; por API 409 'solo estado rechazada' (estimaciones-ciclo.controller.js:671-685) | ▢ | ▢ |
| NEG-ESTADO-12 | Reingresar dos veces la MISMA estimación rechazada | contratista@sigecop.test (contratista) | `/estimaciones/reingreso` → modal elige PRUEBA-HU-16 (id 6940); `select-estimacion` la rechazada que ya tiene reingreso | `select-estimacion` → est rechazada con reemplaza_a existente; `textarea-nota` → "atendido"; `chk-confirmado` → marcado; `btn-reingresar` | La rechazada que ya tiene reingreso NO aparece en `select-estimacion`; por API 409 (unicidad 1 rechazada→1 reingreso, UNIQUE reemplaza_a, estimaciones-ciclo.controller.js) | ▢ | ▢ |
| NEG-INMUT-13 | Editar/borrar un avance ya registrado de forma directa (debe ser append-only: anula + nueva) | contratista@sigecop.test (contratista) | Trabajos terminados · `/seguimiento/trabajos-terminados` → modal elige SOP-2026-001; sobre una fila de avance existente | intento de editar/eliminar la fila original (PATCH/DELETE /api/trabajos/:id) | No existe edición/borrado directo: PATCH/DELETE eliminados. Corregir = `POST /api/trabajos/:id/corregir` que ANULA la original (estado vigente→anulada, trigger `sigecop_avance_inmutable`) y crea una vigente vinculada (reemplaza_a) + nota 'dice/debe decir' (art. 123 fr. VI/VII RLOPSRM) | ▢ | ▢ |
| NEG-INMUT-14 | Registrar avance en un contrato cerrado (finiquitado) | contratista@sigecop.test (contratista) | `/seguimiento/trabajos-terminados` → modal elige PRUEBA-HU-17 (6941, cerrado) | `cap-concepto` → un concepto; `cap-periodo` → periodo vencido; `cap-cantidad` → 5; `cap-foto-evidencia` → JPG | 409 'El contrato ya está cerrado…' (art. 64 LOPSRM, trabajos.controller.js:249-253). El avance es append-only y un contrato cerrado es solo-lectura | ▢ | ▢ |
| NEG-INMUT-15 | Abrir una SEGUNDA bitácora del mismo contrato | residente@sigecop.test (residente) | Apertura de bitácora · `/bitacora/apertura` → modal elige SOP-2026-001 (que ya tiene bitácora abierta) | `input-fecha-apertura` → fecha inicio; campos `md-*` completos; `btn-aperturar` | 409 (código 23505) 'Ya existe una bitácora para este contrato' (UNIQUE por contrato, bitacora.controller.js). La apertura es única e inmutable (art. 122-123 RLOPSRM) | ▢ | ▢ |
| NEG-INMUT-16 | Emitir manualmente la nota tipo 'apertura' (folio #1) | residente@sigecop.test (residente) | Emisión de notas · `/bitacora/notas` → modal elige SOP-2026-001; `select-tipo` | `select-tipo` → 'apertura'; `input-contenido` → "texto"; `btn-emitir` | 'apertura' no aparece en `select-tipo` (catálogo filtra clave!=='apertura'); por API 400 'no se emite a mano' (bitacora.controller.js:548). La nota #1 la genera la apertura, no el usuario | ▢ | ▢ |
| NEG-INMUT-17 | Anular la nota de APERTURA (folio #1) | residente@sigecop.test (residente) | `/bitacora/notas` → modal elige SOP-2026-001; sobre la nota #1 'apertura' | `btn-anular-1` (no debe existir) / POST /api/bitacora/notas/:id/anular sobre la apertura | 403 'La nota de apertura (folio #1) no puede anularse' (bitacora.controller.js:774). La apertura es inmutable (art. 123 fr. VI RLOPSRM) | ▢ | ▢ |
| NEG-INMUT-18 | Anular como emisor una nota YA FIRMADA/aceptada por la contraparte | residente@sigecop.test (emisor) + contratista@sigecop.test (firma) | `/bitacora/notas` → modal elige PRUEBA-HU-09; emitir nota como residente → contratista la firma (`btn-firmar-nota-{n}`) → residente intenta `btn-anular-{n}` | `btn-confirmar-anular-{n}`; correccion → "dice/debe decir…" | 409: una nota aceptada por la contraparte (firma en `bitacora_nota_firmas`) NO puede anularse, inclusive por el emisor original (art. 123 fr. VI RLOPSRM; la anulación fr. VII es solo ANTES de la firma). **[BUG CONOCIDO #8: hoy SÍ pasa (201) — anularNota no consulta `bitacora_nota_firmas`, solo respuestas vinculadas]** | ▢ | ▢ |
| NEG-INMUT-19 | Anular la nota de FINIQUITO (acta de cierre) en un contrato cerrado | residente@sigecop.test (emisor del finiquito) | `/bitacora/notas` → modal elige PRUEBA-HU-17 (6941, cerrado); sobre la nota tipo 'finiquito' | POST /api/bitacora/notas/:id/anular ; correccion → "…" | 409 doble: (a) contrato cerrado solo-lectura (art. 64 LOPSRM) y (b) la nota de finiquito es el acta de cierre, tan inmutable como la apertura. **[BUG CONOCIDO #10: hoy SÍ pasa (201) — anularNota no llama gateCierre ni protege el tipo 'finiquito']** | ▢ | ▢ |
| NEG-INMUT-20 | Crear una nota NUEVA por la ruta 'vincular' en un contrato cerrado | residente@sigecop.test (residente) | `/bitacora/notas` → modal elige PRUEBA-HU-17 (6941, cerrado); `btn-responder-{n}` sobre una nota existente | `select-tipo` → 'res_suspension'; `input-contenido` → "nota nueva"; envía respuesta (vincular) | 409 'contrato cerrado… no se vinculan notas' (art. 64 LOPSRM): vincular inserta una nota nueva igual que emitir, debe bloquearse. **[BUG CONOCIDO #9: hoy SÍ pasa (201) — vincularNota no invoca gateCierre, evade el gate que sí aplica emitirNota]** | ▢ | ▢ |
| NEG-INMUT-21 | Firmar/aceptar una nota en un contrato cerrado | contratista@sigecop.test (contraparte) | `/bitacora/notas` → modal elige PRUEBA-HU-17 (6941, cerrado); `btn-firmar-nota-{n}` sobre una nota 'emitida' | `btn-firmar-nota-{n}` | 409 coherente con el gate de cierre (art. 64 LOPSRM): aceptar es una mutación append (`bitacora_nota_firmas`) que el resto del dominio bloquea en contrato cerrado. **[BUG CONOCIDO #19: hoy SÍ pasa (201) — firmarNota no invoca gateCierre, inconsistente con emitir/minuta/visita que sí dan 409]** | ▢ | ▢ |
| NEG-INMUT-22 | Emitir manualmente una nota tipo 'cierre' a mano | residente@sigecop.test (residente) | `/bitacora/notas` → modal elige SOP-2026-002; `select-tipo` | `select-tipo` → 'cierre'; `input-contenido` → "texto"; `btn-emitir` | El tipo 'cierre' (asiento del sistema) no debe poder emitirse a mano, igual que 'apertura' está protegida. **[BUG CONOCIDO #21: hoy NO está protegido — la apertura sí, pero el 'cierre' se puede emitir manualmente]** | ▢ | ▢ |
| NEG-INMUT-23 | Editar la garantía de un contrato CERRADO (debe ser solo-lectura tras finiquito) | dependencia@sigecop.test (dependencia) | Registro de fianzas · `RegistroFianzas` (HU-02) → modal elige SOP-2026-010 (cerrado); editar una póliza existente | `mp-afianzadora` → "Otra Afianzadora"; `mp-monto` → 12345; guardar (PUT /api/garantias/:id) | 409 'el contrato ya está cerrado… no se edita la garantía' (art. 64 LOPSRM): crear/endosar ya devuelven 409, editar y subir-PDF también deberían. **[BUG CONOCIDO #5: hoy editarGarantia (PUT) y subirPdfGarantia NO están gateados → 200/201 en contrato cerrado]** | ▢ | ▢ |
| NEG-INMUT-24 | Modificar la carátula/montos de una estimación YA integrada (inmutable) | contratista@sigecop.test (contratista) | Integración · `/estimaciones/integracion` → modal elige un contrato con estimación ya 'integrada' | intento de re-integrar/alterar generadores o deductivas de una estimación existente | La estimación integrada es append-only (trigger `sigecop_estimacion_inmutable`): no se editan carátula/generadores/notas; corregir tras rechazo = reingreso (HU-16) con fila nueva. El wizard no expone edición de una integrada | ▢ | ▢ |
| NEG-INMUT-25 | Registrar un convenio modificatorio sobre un contrato cerrado | dependencia@sigecop.test (dependencia) | Convenios · `/contratos/modificatorios` → modal elige PRUEBA-HU-17 (6941, cerrado) | `cm-tipo` → 'plazo'; `cm-plazo-nuevo` → 200; `cm-motivo` → "dictamen"; `cm-oficio` → "OF-001"; `btn-registrar-convenio` | 409 'El contrato ya está cerrado… no se registran convenios' (art. 64 LOPSRM, convenios.controller.js:166). Este gate SÍ existe (contraste con bitácora/garantías/pago donde falta) | ▢ | ▢ |

---

## Sesión única · Casos de borde · Resumen de gates que FALTAN (bugs)

Todos los casos son **🔴 negativos**: el sistema debe RECHAZAR, BLOQUEAR o AVISAR. Login: `#login-usuario` / `#login-password` / botón «Iniciar sesión» (pass común `Sigecop2026!`). Al entrar a una pantalla de contrato sale `modal-elegir-contrato` → elige con `modal-contrato-<id>` (se hereda en `banner-contrato-activo`). Donde un gate legal HOY no bloquea, el «Resultado esperado» lo indica con **[BUG CONOCIDO #N]** para que NO lo confundas con error de tu prueba. La mini-tabla final lista los gates ausentes.

| ID | Caso de prueba | Cuenta (rol) | Pantalla / cómo llegar | Datos de entrada (campo → valor) | Resultado esperado | Resultado obtenido | Estatus |
|---|---|---|---|---|---|---|---|
| NEG-BORDE-01 | Sesión única: el segundo login de la MISMA cuenta invalida el token del primero | `contratista@sigecop.test` (contratista) en 2 pestañas/navegadores | Pestaña A: login y entra a cualquier pantalla (sidebar → «Bitácora»). Pestaña B: login con la MISMA cuenta. Vuelve a A y dispara un request (recargar lista / cambiar contrato) | A: `#login-usuario` → `contratista@sigecop.test`; `#login-password` → `Sigecop2026!`. B: idéntico (segundo login) | El segundo login emite JWT con `tv` (token_version) incrementado; el token de la pestaña A queda inválido → al siguiente request A recibe **401** y es expulsada a login (last-login-wins, auth.controller.js incrementa token_version) | ▢ | ▢ |
| NEG-BORDE-02 | Sesión única: token de sesión vieja rechazado tras re-login (verificación cruzada de rol) | `dependencia@sigecop.test` (dependencia) | Login en pestaña A → abre «Solicitudes de registro» (`/usuarios/solicitudes`). Re-login en pestaña B con la misma cuenta. En A pulsa Aprobar/recargar | A primer login; B segundo login (mismo email) | El request de A devuelve **401** (token_version desfasado), no 200; la acción NO se ejecuta con el token viejo | ▢ | ▢ |
| NEG-BORDE-03 | Anticipo EXACTAMENTE 30% NO exige PDF, pero 31% SÍ lo exige y bloquea el guardado | `residente@sigecop.test` (residente) | Alta de contrato (`/contratos/alta`, ruta libre, sin modal) → Paso 5 Garantías | `anticipo-input` → `31` (sin subir PDF) | Aparece `anticipo-pdf-uploader` con `anticipo-pdf-requerido`: «sin este PDF no se puede avanzar ni guardar (anticipo > 30%, art. 50 fr. IV LOPSRM)»; el wizard queda bloqueado hasta `anticipo-pdf-input` → `anticipo-pdf-pendiente-file` → `anticipo-pdf-ok`. Con `anticipo-input` → `30` NO se exige PDF (umbral es `> 30`) | ▢ | ▢ |
| NEG-BORDE-04 | Convenio de plazo +25% EXACTO no levanta aviso SFP; +26% sí lo levanta (art. 102) | `dependencia@sigecop.test` (dependencia) | «Convenios» (`/contratos/modificatorios`) sobre `SOP-2026-002` (plazo vigente 90) → tipo Plazo | `cm-tipo` → `Plazo`; `cm-plazo-nuevo` → `113` (90→113 = +25.5%); `cm-motivo` → `Ampliación por causa justificada (art. 99 RLOPSRM)`; `cm-oficio` → `OF-SFP-001` | A >25% el convenio se registra **con AVISO** `aviso-sfp` / `badge-sfp-<id>` (NO bloquea el registro); al **Autorizar** sin oficio adjunto → **409** «…art. 102… carga el oficio…». A exactamente +25% (`cm-plazo-nuevo`=`112.5` no aplica en días; usa 90→112 ≈ +24.4% para quedar bajo umbral) NO aparece `badge-sfp` | ▢ | ▢ |
| NEG-BORDE-05 | Autorizar convenio >25% SIN oficio adjunto se bloquea (art. 102) | `dependencia@sigecop.test` (dependencia) | «Convenios» → fila del convenio recién registrado con +33% (plazo `120`) | `conv-autorizar-<id>` (clic) sin haber subido oficio | **409** «…art. 102… carga el oficio…»; tras `conv-oficio-subir-<id>` (PDF) y reintentar → `conv-badge-autorizado-…` (Autorizado) | ▢ | ▢ |
| NEG-BORDE-06 | Oficio de autorización del convenio en formato NO-PDF se rechaza | `dependencia@sigecop.test` (dependencia) | «Convenios» → `conv-oficio-subir-<id>` del convenio >25% | `conv-oficio-subir-<id>` → archivo `.jpg` | Toast «El oficio debe ser un PDF»; el oficio no se carga; un 2º oficio sobre el mismo convenio → **409** (inmutable, append-only) | ▢ | ▢ |
| NEG-BORDE-07 | Autorizar convenio sin ser dependencia | `residente@sigecop.test` (residente) | «Convenios» (`/contratos/modificatorios`) → fila `fila-convenio-<id>` | `conv-autorizar-<id>` (clic como residente) | **403** (el acto de autorización es exclusivo de la dependencia, art. 59 p3 LOPSRM) | ▢ | ▢ |
| NEG-BORDE-08 | Re-autorizar un convenio ya autorizado (acto único) | `dependencia@sigecop.test` (dependencia) | «Convenios» → fila de un convenio con `conv-badge-autorizado-…` | `conv-autorizar-<id>` (segundo clic) | **409** (acto único e irrepetible, art. 59 p3); el badge sigue «Autorizado» | ▢ | ▢ |
| NEG-BORDE-09 | B4 ampliación: el P.U. del concepto adicional NO puede diferir del original (art. 59) | `dependencia@sigecop.test` (dependencia) | «Convenios» → `cm-tipo` → `Monto` → `editor-programa-convenio` → panel «Ampliar» de C-03 | `cm-ampliar-extra` → `50` (cantidad adicional); P.U. = read-only con candado 🔒 (`2500.00` heredado, no se teclea); `cm-ampliar-periodo` → Periodo 3 | El P.U. del adicional HEREDA el del original (`2500.00`) bloqueado en UI; el backend valida que coincida con el original y **rechaza (400)** cualquier P.U. distinto enviado por API (art. 59 LOPSRM, gate ya aplicado, convenios.controller.js) | ▢ | ▢ |
| NEG-BORDE-10 | Convenio de Monto con programa descuadrado no se guarda | `dependencia@sigecop.test` (dependencia) | «Convenios» → `cm-tipo` → `Monto` → `editor-programa-convenio` | `cm-celda-0-1` → `900` (deja `cm-restante-0` ≠ 0) | Banner `cm-programa-descuadre` (rojo); `btn-registrar-convenio` bloqueado hasta que cada `cm-restante-<i>` = 0 (regla 100%, art. 45-A-X + 52 LOPSRM) | ▢ | ▢ |
| NEG-BORDE-11 | Convenio de Monto: reducir un concepto por debajo de lo ya estimado se bloquea | `dependencia@sigecop.test` (dependencia) | «Convenios» → `cm-tipo` → `Monto` → `editor-programa-convenio` sobre `SOP-2026-001` (tiene avance/estimación) | `cm-concepto-cantidad-0` → `100` (menor a lo ya estimado en estimaciones no rechazadas) | **400** (no se reduce por debajo de lo ya estimado; el original está congelado, criterio de diseño + art. 118 RLOPSRM) | ▢ | ▢ |
| NEG-BORDE-12 | Foto de avance: quitarla en la UI bloquea el botón Registrar | `contratista@sigecop.test` (contratista) | «Avance y seguimiento» → `pestana-trabajos` (`/seguimiento/trabajos-terminados`) sobre `SOP-2026-002` | `cap-concepto` → C-01; `cap-periodo` → Periodo 1; `cap-cantidad` → `1000`; foto de evidencia → (sin adjuntar) | `btn-registrar-avance` deshabilitado (la regla A1 `puedeGuardar = … && !!fotoEvidencia` exige al menos una foto). **[BUG CONOCIDO #22]**: el backend NO valida la foto — un POST directo a `/api/trabajos` sin imagen devuelve 201 y crea el avance sin evidencia; la barrera vive solo en el front | ▢ | ▢ |
| NEG-BORDE-13 | Avance que excede lo contratado se rechaza (art. 118) | `contratista@sigecop.test` (contratista) | `/seguimiento/trabajos-terminados` sobre `SOP-2026-002` | `cap-concepto` → C-01 (contratado 1000); `cap-periodo` → Periodo 1; `cap-cantidad` → `1500` | **409** + banner `aviso-exceso` «…excede lo contratado (art. 118 RLOPSRM)»; el avance NO se registra | ▢ | ▢ |
| NEG-BORDE-14 | Avance back-dateado tras convenio recalcula la etapa histórica «congelada» | `residente@sigecop.test` (residente) | `SOP-2026-001` (convenio v1/v2, fecha convenio 2026-06-24) → registrar avance en periodo pre-convenio (P2, fin 2026-05-31) y abrir `pestana-curva` (`/seguimiento/curva-avance`) | Avance: `cap-concepto` → C-01; `cap-periodo` → Periodo 2; `cap-cantidad` → `1600` (capturado hoy, fecha del periodo cae antes del convenio) | La etapa rotulada `Histórico · congelado` debería mantener su % ejecutado fijo. **[BUG CONOCIDO #28]**: hoy el % ejecutado de la etapa v1 CAMBIA (p.ej. 46.9%→53.1%) porque la partición es por `concepto_avance.fecha`, no por momento de captura; solo el denominador queda congelado, el numerador se mueve | ▢ | ▢ |
| NEG-BORDE-15 | Asentar el mismo (concepto, periodo) de atraso dos veces es idempotente | `residente@sigecop.test` (residente) | `pestana-alertas` (`/seguimiento/alertas`) sobre un contrato con atraso → `btn-asentar-<contrato_concepto_id>` | Pulsar `btn-asentar-<id>` dos veces sobre la misma fila `fila-atraso-<id>` | Primer clic → nota de atraso (art. 53) `aviso-ok`; segundo → **409** idempotente (`atraso_asentado`) `aviso-error`; doble clic rápido → botón disabled «Asentando…» | ▢ | ▢ |
| NEG-BORDE-16 | Pólizas: 2ª garantía del mismo tipo se rechaza (una por tipo, art. 48) | `dependencia@sigecop.test` (dependencia) | «Fianzas / garantías» (`/contratos/fianzas`) sobre `SOP-2026-003` → `btn-agregar-poliza` → `modal-agregar-poliza` | `mp-tipo` → `Cumplimiento` (ya existe una); `mp-afianzadora` → `X`; `mp-monto` → `50000`; `mp-vencimiento` → `2027-12-31`; `mp-confirmar` | **409** UNIQUE(contrato_id,tipo) «ya tiene garantía de ese tipo» (art. 48 LOPSRM). **[BUG CONOCIDO #4]**: por la vía EDITAR (PUT) cambiando la capitalización («Anticipo» vs «anticipo») el UNIQUE NO muerde y quedan 2 fianzas del mismo tipo efectivo | ▢ | ▢ |
| NEG-BORDE-17 | Endoso con monto mayor al contrato se rechaza (art. 48) | `dependencia@sigecop.test` (dependencia) | «Fianzas / garantías» → `btn-endoso-<g.id>` → `modal-endoso` | `endoso-motivo` → `Ampliación de monto`; `endoso-monto` → `9999999`; `endoso-vigencia` → `2028-06-01`; `endoso-confirmar` | **400** (monto excede el contrato, art. 48 LOPSRM); con `endoso-vigencia` → `2020-01-01` también **400** (vigencia vencida) | ▢ | ▢ |
| NEG-BUG-01 | Contrato CERRADO: registrar pago de estimación autorizada debe rechazarse | `finanzas@sigecop.test` (finanzas) | «Pago y tránsito» → `pestana-registro` (`/pagos/registro`) → elige `PRUEBA-HU-17` (id 6941, cerrado) → `pago-estimacion` | `pago-estimacion` → estimación #2 «autorizada» no pagada; referencia SPEI numérica; CFDI; fecha factura no futura; «Registrar pago» | DEBERÍA dar **409** (contrato cerrado = solo-lectura; el saldo se liquida solo por finiquito, art. 64 LOPSRM). **[BUG CONOCIDO #2: hoy devuelve 201 y registra el pago → doble liquidación del mismo saldo]**. El selector tampoco excluye el contrato cerrado **[BUG CONOCIDO #12]** | ▢ | ▢ |
| NEG-BUG-02 | Contrato CERRADO: EDITAR garantía y SUBIR PDF de póliza deben rechazarse | `dependencia@sigecop.test` (dependencia) | «Fianzas / garantías» sobre un contrato cerrado (`PRUEBA-HU-17` / `SOP-2026-010`) → editar una póliza existente y subir PDF | PUT sobre la garantía: `endoso/edición` afianzadora/monto/vigencia; luego subir PDF de póliza | DEBERÍA dar **409** en TODO el módulo (art. 64 LOPSRM): crear póliza y endoso ya devuelven 409 correctamente. **[BUG CONOCIDO #5: hoy EDITAR garantía devuelve 200 y SUBIR PDF de póliza devuelve 201 sobre un contrato cerrado]** | ▢ | ▢ |
| NEG-BUG-03 | Contrato CERRADO: vincular/responder nota nueva debe rechazarse | `residente@sigecop.test` (residente) | «Bitácora» → `pestana-consulta` (`/bitacora/consulta`) sobre `PRUEBA-HU-17` (cerrado) → responder una nota | Vincular nota nueva (`tipo` res_*, contenido) a una nota del contrato cerrado | DEBERÍA dar **409** (contrato cerrado no admite actos nuevos, art. 64 LOPSRM); emitir nota normal sí da 409. **[BUG CONOCIDO #9: hoy «vincular» crea la nota nueva con 201, evadiendo el gate que sí aplica a emitir]** | ▢ | ▢ |
| NEG-BUG-04 | Contrato CERRADO: anular la nota de FINIQUITO debe rechazarse | `residente@sigecop.test` (residente) | «Bitácora» → consulta de notas sobre `PRUEBA-HU-17` (cerrado) → anular la nota tipo `finiquito` (`form-anular-<n>` + `btn-confirmar-anular-<n>`) | Anular la nota de finiquito (el asiento de cierre) | DEBERÍA dar **409** (a) contrato cerrado solo-lectura art. 64 y (b) el acta de finiquito es inmutable como la apertura. **[BUG CONOCIDO #10: hoy devuelve 201, anula la nota de finiquito y crea correctiva — el asiento legal de cierre queda alterado]** | ▢ | ▢ |
| NEG-BUG-05 | Anular una nota ya FIRMADA por la contraparte debe rechazarse | `residente@sigecop.test` (residente, emisor) | «Bitácora» → emite una nota; con `contratista@` fírmala (`btn-firmar-nota-<n>`); vuelve como residente y anúlala (`btn-confirmar-anular-<n>`) sobre `PRUEBA-HU-09` | Anular una nota que la contraparte ya firmó/aceptó | DEBERÍA dar **409** (art. 123 fr. VI RLOPSRM: prohibido modificar notas ya firmadas, inclusive para el emisor original; la anulación fr. VII es solo PRE-firma). **[BUG CONOCIDO #8: hoy devuelve 201 y deja la nota firmada en estado anulada]** | ▢ | ▢ |
| NEG-BUG-06 | Contrato CERRADO: firmar/aceptar una nota debe rechazarse | `contratista@sigecop.test` (contratista, contraparte) | «Bitácora» → consulta de notas sobre `PRUEBA-HU-17` (cerrado) → `btn-firmar-nota-<n>` de una nota «emitida» | Firmar una nota de un contrato cerrado | DEBERÍA dar **409** coherente con emitir/minuta/visita que sí bloquean por art. 64. **[BUG CONOCIDO #19: hoy `firmarNota` devuelve 201 y registra la firma «superintendente» sin gate de cierre]** | ▢ | ▢ |
| NEG-BUG-07 | Fusionar empresa duplicada que tiene contratos debe completarse (no error opaco) | `dependencia@sigecop.test` (dependencia) | «Padrón de empresas» (`/admin/empresas`) → pestaña `tab-porvalidar` → `fusionar-<id>` de un duplicado referenciado por un contrato | Fusionar empresa duplicada (source con contratos) en la canónica (`canonica_id`) | DEBERÍA fundir reapuntando personas Y la referencia del contrato y eliminar la duplicada (dedup pedido por el profe). **[BUG CONOCIDO #3: hoy devuelve HTTP 500 «Error interno» (FK contratos→empresa no reapuntada); la fusión falla siempre que el duplicado tenga contratos]** | ▢ | ▢ |
| NEG-BUG-08 | Reportes Excel #2/#3: la fila TOTALES no debe sumar estimaciones RECHAZADAS | `residente@sigecop.test` (residente) | «Reportes» (`/reportes`) sobre `PRUEBA-HU-16` (id 6940: EST#1 rechazada 208,500 + EST#2 reingreso 208,500) → exportar reporte #2 y #3 (Excel) | Exportar reporte #2 «Avance financiero» y #3 «Listado de estimaciones» y leer la fila TOTALES Neto | TOTAL Neto DEBERÍA excluir la rechazada = **208,500** (igual que el Expediente). **[BUG CONOCIDO #6: hoy TOTALES suma todo = 417,000, contradiciendo la propia métrica «Estimado» del mismo archivo y el Expediente]** | ▢ | ▢ |
| NEG-BUG-09 | Finiquito por id directo debe acotar por empresa de la dependencia | `dep2@sigecop.test` (dependencia «Norte», otra empresa) | URL directa `GET /api/finiquito/contrato/<id>` (o deep-link) a un contrato de la empresa «Dependencia Demo» | Acceder al finiquito de un contrato cuya dependencia es de OTRA empresa | DEBERÍA dar **403** (aislamiento por empresa, como el Portafolio). **[BUG CONOCIDO #7: hoy devuelve 200 (lectura) e incluso permite CERRAR el contrato ajeno con 201 — `getContrato` del finiquito no trae `dependencia_empresa_id` → fail-open por id]** | ▢ | ▢ |
| NEG-BUG-10 | Aprobar usuario: no debe poder cambiar el rol de una cuenta YA ACTIVA | `dependencia@sigecop.test` (dependencia) | API directa `PATCH /api/usuarios/1/aprobar` (no alcanzable desde la UI, que solo lista pendientes) | Body `{"rol":"finanzas"}` sobre `residente@` (id 1, estado activo) | DEBERÍA dar **409/400** (aprobar solo opera sobre estado `pendiente`). **[BUG CONOCIDO #13: hoy devuelve 200 y cambia el rol de una cuenta activa — falta el guard `estado='pendiente'`]** | ▢ | ▢ |
| NEG-BUG-11 | Registro: correo vacío / solo espacios debe rechazarse | (público, sin sesión) | Pantalla de acceso → «Regístrate» (`link-registro`) → `form-registro`; o API `POST /auth/register` | `reg-nombres` → `Juan`; `reg-apellidos` → `Pérez`; `reg-email` → `   ` (solo espacios); `reg-password` → `Sigecop2026!`; `reg-rol` → residente | DEBERÍA dar **400** (correo obligatorio, no vacío tras trim). **[BUG CONOCIDO #14: por API el correo «   » colapsa a «» y se crea la cuenta (201); el siguiente registro con espacios choca con el UNIQUE → 409 engañoso]** (el front sí valida con «Completa todos los campos.») | ▢ | ▢ |
| NEG-BUG-12 | Registro: correo sin formato válido (sin @) debe rechazarse | (público, sin sesión) | API `POST /auth/register` (el front usa `type=email`, un POST directo lo evade) | `email` → `esto-no-es-email`; resto válido | DEBERÍA dar **400** por formato inválido. **[BUG CONOCIDO #25: hoy devuelve 201 y crea la cuenta — el backend no valida formato de correo]** | ▢ | ▢ |
| NEG-BUG-13 | Alta: fecha de inicio fuera de rango debe dar 400, no 500 | `residente@sigecop.test` (residente) | API `POST /api/contratos` (el date-picker de la UI no produce estas fechas) | `fechaInicio` → `2026-02-30` (o `2026-13-45`, `2026-00-00`); resto válido | DEBERÍA dar **400** «fecha inválida». **[BUG CONOCIDO #26: hoy devuelve 500 «Error interno» — el catch mapea 22007/22P02 pero no 22008 (fecha fuera de rango)]** | ▢ | ▢ |
| NEG-BUG-14 | Atraso/badge no debe contar avances ANULADOS | `residente@sigecop.test` (residente) | `pestana-alertas` (`/seguimiento/alertas`) sobre un contrato donde un avance se corrigió a la baja (queda 1 anulado + 1 vigente) | Comparar `ejecutado_acumulado` del panel de atraso vs la curva (HU-05) / trabajos (HU-06) para el mismo concepto | El déficit DEBERÍA contar solo avances `vigente` (como curva y trabajos). **[BUG CONOCIDO #11: hoy `alertas` suma sin filtro estado='vigente' → cuenta el anulado; puede inflar el déficit o, peor, ocultar un atraso real haciéndolo negativo y el concepto desaparece del panel y del badge]** | ▢ | ▢ |

### Gates conocidos que FALTAN (referencia para el tester)

| # | Acto | Qué DEBERÍA bloquear (ley) | Qué hace HOY |
|---|---|---|---|
| #2 | Registrar pago en contrato cerrado | 409: contrato cerrado solo-lectura, saldo solo por finiquito (art. 64 LOPSRM) | 201: paga la estimación → doble liquidación del mismo saldo |
| #5 | Editar garantía / subir PDF de póliza en contrato cerrado | 409 en todo el módulo de garantías (art. 64 LOPSRM) | EDITAR → 200, SUBIR PDF → 201 (crear/endoso sí dan 409) |
| #9 | Vincular/responder nota en contrato cerrado | 409: no se admiten actos nuevos (art. 64 LOPSRM) | 201: crea la nota nueva, evadiendo el gate de emitir |
| #10 | Anular la nota de finiquito (contrato cerrado) | 409: cierre solo-lectura + acta de finiquito inmutable (art. 64; análogo a apertura) | 201: anula el asiento de cierre y crea correctiva |
| #19 | Firmar/aceptar nota en contrato cerrado | 409: solo-lectura art. 64 (como emitir/minuta/visita) | 201: registra la firma sin gate de cierre |
| #22 | Registrar avance sin foto de evidencia | Rechazar/marcar incompleto server-side (regla A1 del equipo) | 201 por API: la obligatoriedad de foto vive solo en el front |
