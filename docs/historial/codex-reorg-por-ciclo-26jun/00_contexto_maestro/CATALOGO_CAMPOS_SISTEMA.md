# Catálogo de campos del sistema — SIGECOP

> **Generado 24-jun-2026** (workflow `catalogo-campos-sigecop`, 7 grupos en paralelo, read-only).
>
> **Propósito:** referencia reutilizable de TODOS los campos/validaciones por pantalla. En la próxima sesión de pruebas: VERIFICAR si cambió algo y agregar lo nuevo, sin re-analizar desde cero. Roles: residente / contratista(superintendente) / supervision / dependencia / finanzas (nivel E=edita, C=consulta; ver `frontend/src/data/permisos.js`).


**Cobertura:** 40 pantallas · 247 campos catalogados.

## Índice

- **Auth / registro / empresas** — Login (Iniciar sesión), Crear cuenta (FormRegistro embebido en SeleccionRol), Solicitud de acceso (SolicitudRegistro, página standalone), Solicitudes de registro (SolicitudesRegistro — bandeja de aprobación), Padrón de empresas (EmpresasPadron — administración)
- **Alta / fianzas / roster (HU-01, HU-02, sustitución roster)** — AltaContrato — Paso 1: Datos generales (wizard tab 0), AltaContrato — Paso 2: Catálogo de conceptos (tab 1), AltaContrato — Paso 3: Programa de obra / matriz concepto×periodo (tab 2), AltaContrato — Paso 4: Datos jurídicos (tab 3), AltaContrato — Paso 5: Garantías, penalizaciones y amortización (tab 4), AltaContrato — Paso 6: Plan de amortización del anticipo (tab 5, oculto sin anticipo), AltaContrato — Paso 7: PDF firmado + Registrados (tab 6 y tab 7), RegistroFianzas (gestión post-alta de pólizas), RegistroFianzas — Modal Endoso (ajuste de garantía), RosterContrato — Sustitución de personas del roster
- **Bitácora** — Apertura formal de la bitácora (AperturaBitacora.jsx), Emisión y respuesta de notas tipificadas con firma (EmisionNotas.jsx), Consulta y búsqueda de notas de bitácora (ConsultaNotas.jsx), Minutas y agenda de visitas (MinutasVisitas.jsx), Por firmar — aperturas de bitácora (PorFirmar.jsx)
- **Avance / curva / atrasos** — TrabajosTerminados.jsx (Registro de trabajos terminados / avance ejecutado), CurvaAvance.jsx (Programa y curva de avance / curva S), AlertasAtraso.jsx (Atraso por concepto), AmbienteAvance.jsx (Ambiente de avance físico y seguimiento — cascarón de navegación)
- **Estimación (ciclo)** — Integración del periodo (wizard de 5 pasos: Periodo / Generadores / Carátula / Soportes y notas / Integrar) + historial, Presentación de la estimación (lista de estimaciones con acción Presentar), Recepción, revisión técnica y autorización/rechazo (tabs por sección + observaciones + panel de resolución), Reingreso de estimación tras rechazo (observaciones de la rechazada + crear nueva versión), Historial de estimaciones del contrato (filtros + tabla + drawer de expediente), Tablero de estimaciones (KPIs agregados + contadores por estado + grid de tarjetas + mis pendientes)
- **Pago / convenios** — RegistroPago (registro de pago efectuado), TransitoPago (tránsito a pago: suficiencia + soportes + instrucción) — wizard 4 pasos, ConveniosModificatorios (trámite de convenios modificatorios), AmbientePago (ambiente de pago / ciclo de cobro — cascarón de navegación), AmbienteConvenio (ambiente de convenio modificatorio — cascarón de navegación, ciclo episódico)
- **Expediente / reportes / finiquito / portafolio** — ConsultaExpediente (Expediente consolidado), ExportacionReportes (7 reportes del contrato), Finiquito y cierre del contrato, Portafolio ejecutivo (semáforos multi-contrato), AmbienteExpediente (cascarón de cierre documental)


---

## Auth / registro / empresas

### Login (Iniciar sesión)  ·  HU-00 (transversal, no en matriz de permisos.js)
- **Ruta:** `/ (cuando no hay rol/token → WithLayout/SoloRol renderiza <SeleccionRol /> vista 'login'). Pública.`
- **Roles que editan:** Todos (público sin sesión). No aplica useVistaHU; HU-00 transversal (permisos.js:37).
- **Notas:** Server-side: emite JWT {id,rol,nombre,empresa_id,tv} con expiresIn 8h (auth.controller.js:62-68). token_version invalida tokens anteriores (sesión única). Errores claros: 401 credenciales, 403 estado pendiente/rechazado. El front guarda token+rol en SesionContext/localStorage (SesionContext.jsx:34-38). Enlace 'Regístrate' (data-testid=link-registro) → vista 'registro' (FormRegistro embebido).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Correo (#login-usuario, label 'Correo') | email (input type=email, autoComplete=username, autoFocus) | sí | FRONT: requerido implícito; se normaliza a minúsculas+trim antes de enviar (SeleccionRol.jsx:40). Sin testid (solo id #login-usuario). BACK: requerido — 400 'email y password son requeridos' si falta (auth.controller.js:27-29); búsqueda exacta por email (auth.controller.js:33). Sin formato/regex de email server-side. |
| Contraseña (#login-password, label 'Contraseña') | password (input type=password, autoComplete=current-password) | sí | FRONT: requerido implícito (SeleccionRol.jsx:71-81). BACK: requerido 400 (auth.controller.js:27); bcrypt.compare contra password_hash → 401 'Credenciales inválidas' si no coincide o usuario inexistente (auth.controller.js:42-45,38-40). Gating de estado: estado!='activo' → 403 'pendiente'/'rechazada' (auth.controller.js:50-53). Sesión única last-login-wins: incrementa token_version y firma tv en JWT (auth.controller.js:55-68; schema.sql:356-358). |

### Crear cuenta (FormRegistro embebido en SeleccionRol)  ·  HU-00 / auto-registro (no en matriz)
- **Ruta:** `/ vista 'registro' (SeleccionRol.jsx, botón link-registro). Pública. POST /auth/register.`
- **Roles que editan:** Todos (público sin sesión). Sin useVistaHU.
- **Notas:** Append-only: crea usuario en estado 'pendiente' con rol NULL (auth.controller.js:120-121); NO devuelve token (no entra hasta aprobación). Server-side único: normalización email, hash, dedup empresa, gate empresa obligatoria. Al éxito vuelve a login con mensaje de cuenta pendiente (SeleccionRol.jsx:181-185). Errores en banner data-testid=registro-error. NOTA: hay DOS submits redundantes (esta vista embebida + SolicitudRegistro standalone) con campos espejo pero distintos testid/ids.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Nombre(s) (#reg-nombres, data-testid=reg-nombres, label 'Nombre(s)') | text | sí | FRONT: !nombres.trim() → 'Captura tu(s) nombre(s).' (SeleccionRol.jsx:137-140). Se concatena con apellidos a campo `nombre` (col fuente única) y se valida esNombreCompleto (≥2 palabras /\p{L}{2,}/gu) → 'Captura tu nombre y apellido(s)…' (SeleccionRol.jsx:104,150-154). BACK: esNombreCompleto sobre `nombre` concatenado → 400 'Captura tu nombre y apellido(s)… (art. 123 RLOPSRM)' (auth.controller.js:19,91-93). Fundamento: nombre completo aparece en la bitácora, art. 123 fr. III RLOPSRM (criterio del equipo el umbral ≥2). |
| Apellido(s) (#reg-apellidos, data-testid=reg-apellidos, label 'Apellido(s)') | text | sí | FRONT: !apellidos.trim() → 'Captura tu(s) apellido(s).' (SeleccionRol.jsx:141-143). Junto con nombre cumple la regla ≥2 palabras (art. 123 RLOPSRM). BACK: no recibe apellidos por separado — valida el `nombre` ya concatenado (auth.controller.js:91). |
| Correo (#reg-email, data-testid=reg-email, label 'Correo') | email (autoComplete=username) | sí | FRONT: !email.trim() → 'Completa todos los campos.' (SeleccionRol.jsx:145-148); se envía minúsculas+trim (SeleccionRol.jsx:179). BACK: requerido 400 (auth.controller.js:86); normaliza emailNorm minúsculas+trim (auth.controller.js:100); UNIQUE email → 23505 → 409 'Ese correo ya está registrado' (auth.controller.js:131-133). |
| Rol que solicita (#reg-rol, data-testid=reg-rol, label 'Rol que solicita') | select (opciones ROLES de permisos.js: residente, contratista, supervision, dependencia, finanzas) | sí | FRONT: default ROLES[0]='residente' (siempre tiene valor); copy 'Informativo: la dependencia confirma el rol definitivo al aprobar' (SeleccionRol.jsx:261-263). BACK: solo informativo → se guarda en rol_solicitado si está en ROLES_VALIDOS, si no NULL (auth.controller.js:99); la columna `rol` efectiva queda NULL hasta aprobación (auth.controller.js:120-121). Condiciona la obligatoriedad de empresa. |
| Empresa (#reg-empresa-select, data-testid=reg-empresa-select, label 'Empresa *' o '(opcional)') | select (catálogo público listarEmpresas) + opción '__nueva__' Registrar nueva | condicional | FRONT: obligatoria si empresaObligatoriaPara(rol)=contratista\|supervision → 'Elige tu empresa: es obligatoria para contratista y supervisión.' (empresa.js:12-13; SeleccionRol.jsx:171-174); label muestra '*' vs '(opcional)' según rol. BACK (defensa en profundidad): ROLES_EMPRESA_OBLIGATORIA=['contratista','supervision'] sin empresaId → 400 'La empresa es obligatoria para contratista y supervisión.' (auth.controller.js:114-117). Catálogo público GET /auth/empresas (empresas.controller.js:103-111, sin token). |
| Nombre de la nueva empresa (#reg-empresa-nueva, data-testid=reg-empresa-nueva) — solo si selecciona '➕ Registrar nueva' | text (condicional, oculto salvo empresaSel='__nueva__') | condicional | FRONT: si rama nueva y rol exige empresa, empresaFinal debe no estar vacío (mismo check de obligatoriedad). Aviso anti-duplicado (data-testid=reg-empresa-existente) si empresaExistentePorNombre (match FUERTE: acentos+puntuación+sufijos razón social) coincide con catálogo → '✓ Ya existe «…»: mejor selecciónala' (empresa.js:28-53; SeleccionRol.jsx:295-302). BACK: resolverOCrearEmpresa deduplica nivel1 (normalizado lower/btrim/regexp) + nivel2 (forma fuerte); si no existe crea estado 'por_validar' tipo 'contratista' (empresas.controller.js:58-99). Índice único uq_empresas_nombre_norm respalda (schema.sql:1721-1722). |
| Contraseña (#reg-password, data-testid=reg-password, label 'Contraseña (mín. 8 caracteres)') | password (autoComplete=new-password) | sí | FRONT: !password → 'Completa todos los campos.'; password.length<8 → 'La contraseña debe tener al menos 8 caracteres.' (SeleccionRol.jsx:155-158). BACK: requerido 400 (auth.controller.js:86); length<8 → 400 'La contraseña debe tener al menos 8 caracteres' (auth.controller.js:94-96); se hashea bcrypt cost 10 (auth.controller.js:102). |
| Confirmar contraseña (#reg-password2, data-testid=reg-password2, label 'Confirmar contraseña') | password (autoComplete=new-password) | sí | FRONT: password!==password2 → 'Las contraseñas no coinciden.' (SeleccionRol.jsx:159-162). BACK: no se envía (solo confirmación de UI). |

### Solicitud de acceso (SolicitudRegistro, página standalone)  ·  HU-00 / auto-registro (no en matriz)
- **Ruta:** `/solicitud-acceso (PÚBLICA, sin SoloRol/WithLayout — App.jsx:138). POST /auth/register.`
- **Roles que editan:** Todos (público sin sesión). Sin useVistaHU.
- **Notas:** Misma lógica/endpoint que FormRegistro pero página standalone con header propio (no Layout). Al éxito muestra pantalla 'Solicitud enviada' (data-testid=solicitud-exito) en vez de redirigir; errores en data-testid=solicitud-error; form data-testid=form-solicitud, submit data-testid=sol-submit. Append-only: usuario 'pendiente', rol NULL, sin token. DISCREPANCIA con FormRegistro: orden (empresa primero) y email no se baja a minúsculas en cliente (lo arregla el backend).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Empresa (#sol-empresa-select, data-testid=sol-empresa-select, label 'Empresa *' o '(opcional para tu rol)') — sección '1 · Empresa' | select (catálogo listarEmpresas) + opción '__nueva__' | condicional | FRONT: obligatoria contratista\|supervision → 'Elige tu empresa: es obligatoria para contratista y supervisión.' (SolicitudRegistro.jsx:77-80; empresa.js:13). Modelo 'empresa primero' (FIX 22-jun): la persona se registra DENTRO de la empresa (SolicitudRegistro.jsx:140-144). BACK: igual que Crear cuenta — 400 server-side si rol exige y falta (auth.controller.js:114-117). |
| Nombre de la nueva empresa (#sol-empresa-nueva, data-testid=sol-empresa-nueva) — solo si '➕ Registrar nueva' | text (condicional) | condicional | FRONT: aviso duplicado data-testid=sol-empresa-existente '✓ Ya existe «…»' vía empresaExistentePorNombre (match fuerte) (SolicitudRegistro.jsx:159-164). BACK: resolverOCrearEmpresa dedup + alta 'por_validar' (empresas.controller.js:58-99). |
| Nombre(s) (#sol-nombres, data-testid=sol-nombres, label 'Nombre(s) *') — sección '2 · Tus datos' | text | sí | FRONT: !nombres.trim() → 'Captura tu(s) nombre(s).' (SolicitudRegistro.jsx:44-46); concatena y valida esNombreCompleto ≥2 palabras → 'Captura tu nombre y apellido(s)…' (SolicitudRegistro.jsx:14,57-60). BACK: esNombreCompleto → 400 (art. 123 RLOPSRM) (auth.controller.js:91-93). |
| Apellido(s) (#sol-apellidos, data-testid=sol-apellidos, label 'Apellido(s) *') | text | sí | FRONT: !apellidos.trim() → 'Captura tu(s) apellido(s).' (SolicitudRegistro.jsx:48-50). Concatenado al `nombre` para la regla ≥2 palabras (art. 123 RLOPSRM). |
| Correo electrónico (#sol-email, data-testid=sol-email, label 'Correo electrónico *') | email (autoComplete=username) | sí | FRONT: !email.trim() → 'Completa nombre, correo y contraseña.' (SolicitudRegistro.jsx:52-54). OJO: aquí se envía email.trim() SIN .toLowerCase() (SolicitudRegistro.jsx:84), a diferencia de FormRegistro que sí baja a minúsculas. BACK: igual normaliza emailNorm minúsculas+trim al guardar (auth.controller.js:100); UNIQUE → 409 (auth.controller.js:131-133). |
| Rol que solicita (#sol-rol, data-testid=sol-rol, label 'Rol que solicita *') | select (ROLES; default 'residente') | sí | FRONT: default 'residente' (siempre tiene valor); 'Informativo: la dependencia confirma el rol definitivo al aprobar' (SolicitudRegistro.jsx:193). BACK: solo rol_solicitado; rol efectivo NULL hasta aprobar (auth.controller.js:99,120). Condiciona obligatoriedad de empresa. |
| Contraseña (#sol-password, data-testid=sol-password, label 'Contraseña (mín. 8 caracteres) *') | password (autoComplete=new-password) | sí | FRONT: !password → 'Completa nombre, correo y contraseña.'; length<8 → 'La contraseña debe tener al menos 8 caracteres.' (SolicitudRegistro.jsx:52,62-64). BACK: requerido + length<8 → 400 (auth.controller.js:86,94-96); bcrypt cost 10. |
| Confirmar contraseña (#sol-password2, data-testid=sol-password2, label 'Confirmar contraseña *') | password (autoComplete=new-password) | sí | FRONT: password!==password2 → 'Las contraseñas no coinciden.' (SolicitudRegistro.jsx:66-68). BACK: no se envía. |

### Solicitudes de registro (SolicitudesRegistro — bandeja de aprobación)  ·  Administración de usuarios (fuera del catálogo de HU; gate por rol vía SoloRol)
- **Ruta:** `/usuarios/solicitudes (SoloRol roles=['dependencia'] — App.jsx:139). GET /usuarios?estado=pendiente; PATCH /usuarios/:id/aprobar\|rechazar.`
- **Roles que editan:** dependencia (E, único; SoloRol bloquea deep-link de otros roles). No usa useVistaHU; gate por SoloRol + requireRole('dependencia') en routes.
- **Notas:** Solo-lectura para no-dependencia: si rol!='dependencia' muestra banner 'exclusiva del rol Dependencia' (SolicitudesRegistro.jsx:92-96), aunque SoloRol ya bloquea el acceso. Sin token (modo demo) → data-testid=solicitudes-sin-sesion (SolicitudesRegistro.jsx:98-102). Estados de tabla: solicitudes-error / solicitudes-vacio / solicitudes-panel; filas data-testid=fila-solicitud con data-email. Columnas read-only: Nombre, Correo, Rol solicitado (ROLES.find por rol_solicitado). Server-side: aprobado_por sale del JWT (no del body); rol nunca se hereda del solicitado (decisión de la dependencia).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Rol a asignar por fila (data-testid=select-rol, columna 'Rol a asignar') | select (ROLES) — uno por solicitud pendiente (rolElegido[u.id]) | sí | FRONT: default '' '— Elige rol —'; NO se hereda el rol_solicitado (SolicitudesRegistro.jsx:27-28,46); aprobar sin elegir → toast 'Elige el rol a otorgar antes de aprobar.' (SolicitudesRegistro.jsx:45-46). BACK: aprobarUsuario exige rol en ROLES_VALIDOS → 400 'Debes indicar el rol a otorgar (rol válido requerido)' (usuarios.controller.js:72-74). |
| Botón Aprobar (data-testid=btn-aprobar) — acción de fila | button (acción, no campo de captura) | no | FRONT: deshabilitado mientras procesando===u.id; quita la fila al éxito (SolicitudesRegistro.jsx:44-57). BACK: PATCH /usuarios/:id/aprobar fija rol efectivo, estado='activo', aprobado_por=req.user.id (del JWT, no del body), aprobado_en=NOW() (usuarios.controller.js:78-84); id inválido → 404; sin filas → 404. requireRole('dependencia') (usuarios.routes.js:22). |
| Botón Rechazar (data-testid=btn-rechazar) — acción de fila | button (acción) | no | FRONT: deshabilitado en proceso; quita fila al éxito (SolicitudesRegistro.jsx:59-70). BACK: PATCH /usuarios/:id/rechazar → estado='rechazado' (usuarios.controller.js:104-106); 404 si no existe. Sin confirmación de UI. |

### Padrón de empresas (EmpresasPadron — administración)  ·  Administración del padrón (fuera del catálogo HU; art. 43 RLOPSRM / art. 74 Bis LOPSRM)
- **Ruta:** `/admin/empresas (SoloRol roles=['dependencia'] — App.jsx:132). GET /empresas/padron\|por-validar\|dependencias\|:id/personas; POST /empresas/:id/validar\|fusionar.`
- **Roles que editan:** dependencia (E, único; SoloRol + requireRole('dependencia') en empresas.routes.js:12). Sin useVistaHU.
- **Notas:** Pantalla 100% administrativa (sin formularios de captura de texto; solo selects/tabs y acciones validar/fusionar). Gating duro: SoloRol dependencia (front) + requireRole('dependencia') en TODOS los endpoints (empresas.routes.js:11-19). Sin sesión → data-testid=empresas-aviso + bloque 'Inicia sesión como Dependencia'. Modelo: empresa propuesta en el registro nace 'por_validar' (autoservicio) y la dependencia VALIDA o FUSIONA (art. 43 RLOPSRM / art. 74 Bis LOPSRM). Dedup en 2 niveles (normalizado débil con índice único + forma fuerte best-effort, posible carrera). Fusión transaccional reapunta cuentas; NO toca contratos (la empresa del contrato es columna propia).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Pestañas tab-padron / tab-porvalidar / tab-dependencias (Tabs) | button (navegación de tab; no captura) | no | FRONT: tab-porvalidar muestra contador (n) de pendientes (EmpresasPadron.jsx:17). cambiarTab resetea fila expandida (EmpresasPadron.jsx:68). Sin sesión → aviso 'Inicia sesión como Dependencia' (EmpresasPadron.jsx:122). |
| Fila empresa del padrón (data-testid=empresa-<id>) — expandible a cuentas | derivado/read-only (Razón social, Tipo, Personas, Contratos, Estado) | no | BACK derivado server-side: listarPadron cuenta personas (usuarios.empresa_id) y contratos por empresa del CONTRATO (contratista_empresa_id/supervision_empresa_id, COALESCE a derivación por usuario), filtra tipo IN ('contratista','supervision'), ordena por_validar primero (empresas.controller.js:120-137). Al expandir carga GET /empresas/:id/personas (solo lectura, empresas.controller.js:208-218). |
| Botón 'Por validar · validar' / 'Validar' (data-testid=validar-<id> en padrón, pv-validar-<id> en por-validar) | button (acción) | no | FRONT: solo aparece si estado!='validada' (EmpresasPadron.jsx:142-144); al validar toast 'Empresa validada e inscrita al padrón (art. 43 RLOPSRM)' y recarga (EmpresasPadron.jsx:97-100). BACK: POST /empresas/:id/validar → estado='validada'; id inválido → 400; no existe → 404 (empresas.controller.js:170-178). Estado restringido a CHECK ('por_validar','validada') (schema.sql:1897). |
| Botón Fusionar (data-testid=fusionar-<id>) — pestaña Por validar, solo si hay posible_duplicado | button (acción) | condicional | FRONT: aparece solo cuando e.posible_duplicado existe; pasa canonica_id=posible_duplicado.id (EmpresasPadron.jsx:174). Posible duplicado se calcula por forma FUERTE contra empresas validadas (empresas.controller.js:142-153). BACK: POST /empresas/:id/fusionar body {canonica_id}: requiere ambos ints>0 → 400; id===canon → 400 'consigo misma'; canónica inexistente → 404; transaccional reapunta usuarios.empresa_id a la canónica y DELETE de la duplicada (empresas.controller.js:183-203). |
| Fila dependencia (data-testid=dep-<id>) — pestaña Dependencias, expandible | derivado/read-only (Dependencia, Contratos) | no | BACK: listarDependencias filtra tipo='dependencia', cuenta contratos por dependencia_id→empresa (empresas.controller.js:157-166). Expande a personas (mismo GET :id/personas). Copy: 'las dependencias no van en el padrón de contratistas; se administran aparte' (EmpresasPadron.jsx:186). |


---

## Alta / fianzas / roster (HU-01, HU-02, sustitución roster)

### AltaContrato — Paso 1: Datos generales (wizard tab 0)  ·  HU-01
- **Ruta:** `/contratos/alta (HeaderVista huId=HU-01); guardar contrato`
- **Roles que editan:** residente (E). contratista/supervision/dependencia = C (consulta); finanzas = sin acceso. Gate front: useVistaHU('HU-01').soloLectura desactiva todos los inputs (RegionEditable). Gate back crearContrato: solo residente crea (403 'Solo el residente puede crear contratos').
- **Notas:** Wizard 7 pasos (índices 0-6) + pestaña auxiliar 'Registrados' (7). Gating ESTRICTAMENTE SECUENCIAL (irAPaso jsx:1719): solo se avanza de a un paso validando el actual; nombres de pestaña no navegan hasta capturaCompleta. Solo residente edita (gate front useVistaHU + gate back 403). Toda la creación es transaccional (BEGIN/COMMIT, todo-o-nada controller:364-489). El alta SIEMBRA el contrato_roster (residente/superintendente/[supervision]) desde el alta (append, motivo 'Asignación inicial'). Tras guardar redirige a /bitacora/apertura?contrato=ID (FIX 22-jun: sin bitácora no se opera nada). Cabecera contratos NO tiene trigger de inmutabilidad pero el flujo no expone edición.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Folio del contrato (dg-folio) | text (maxLength 50) | sí | Front: no vacío (REQ_GENERALES, validarPaso(0) AltaContrato.jsx:1521); maxLength 50. Back: requerido (REQUIRED_FIELDS contratos.controller:16), LIMITES_TEXTO folio≤50 (controller:71), UNIQUE folio → 409 'El folio ya existe' (err 23505 controller:500). Front mapea 409 a campo folio + tab 0 (jsx:1844). |
| Tipo de contrato (select sin testid) | select (3 opciones: Obra pública base de precios unitarios / a precio alzado / Servicios relacionados con obra pública) | sí | Front: no vacío (REQ_GENERALES); default 'Obra pública sobre la base de precios unitarios'. Back: requerido, LIMITES_TEXTO tipo≤80 (controller:71). |
| Objeto del contrato (dg-objeto) | text | sí | Front: no vacío (REQ_GENERALES validarPaso(0)). Back: requerido (REQUIRED_FIELDS). |
| Ubicación de la obra (dg-ubicacion) | text | no | Front: sin validación (FASE 2 profe 16-jun; se redacta en nota de apertura). Back: emptyToNull → NULL si vacío (controller:360). |
| Dependencia (cuenta contratante) (dg-dependencia) | select (cuentas rol dependencia aprobadas vía api.listarAsignables('dependencia')) | sí | Front: validarPaso(0) exige dependenciaId (jsx:1529). Back: dependenciaId no null (controller:335), cuenta debe existir con rol='dependencia' y estado='activo' (controller:346-349) si no 400. El texto de columna 'dependencia' se DERIVA del nombre de la cuenta (controller:362). Corrección profe 04-jun: ya no es texto libre. |
| Monto del contrato derivado del catálogo (monto-derivado) | derivado (read-only, no se captura) | no | DERIVADO server-side = Σ ROUND(cantidad×pu,2) NUMERIC(18,2) (controller:132-143, art. 45 fr. IX RLOPSRM). Cuadre EXACTO al centavo, sin tolerancia. Front solo lo muestra; NO se envía en payload (jsx:1795). Tope MAX_MONTO 1e16 (jsx:1552 / err 22003 controller:518). |
| Plazo (días naturales) (dg-plazo) | number (min 1, step 1) | sí | Front: entero >0 (validarPaso(0) jsx:1526). Back: Number.isInteger && >0 si no 400 'plazoDias debe ser un entero mayor a 0' (controller:60-63). |
| Fecha de inicio (dg-fecha) | date | sí | Front: no vacío (REQ_GENERALES). Back: requerido; alimenta derivarTermino y generarPeriodos. |
| Fecha de término calculada (termino-derivado) | derivado (read-only, no se captura) | no | DERIVADO = inicio + (plazo − 1) (OFFSET_TERMINO_DIAS=1; LOPSRM 31-V / RLOPSRM 100). Front jsx:61; Back derivarTermino controller:24-29 (no se confía en el cliente). |
| % de pena por atraso (dg-pena) | number (min 0, max 1, step 0.0001; fracción 0–1) | no | Front: input min/max; vacío→null en payload (jsx:1784). Back: numOrNull; si no null debe estar 0–1 si no 400 (controller:86-89). Penas convencionales art. 46 Bis LOPSRM + arts. 86-88 RLOPSRM (tope art. 90). TASA exacta a confirmar con profe. |
| Residente tú (equipo-residente) | derivado (read-only, no editable) | no | = usuario del JWT (usuario.nombre). Back: residente_id = req.user.id (controller:399), nunca del body. Entra al contrato_roster como rol residente (controller:412). |
| Contratista · superintendente de obra (select-superintendente) | select (cuentas rol contratista aprobadas vía api.listarAsignables('contratista')) | sí | Front: validarPaso(0) exige superintendenteId (jsx:1527). Back: superintendenteId no null (controller:332), cuenta rol='contratista' estado='activo' si no 400 (controller:343). Texto 'contratista' DERIVADO del nombre (controller:361). Entra al roster como rol superintendente. Muestra empresa derivada (empresa-contratista). |
| Supervisión (select-supervision) | select (cuentas rol supervision aprobadas) | no | Front: opcional ('— Sin supervisión —'). Back: si supervisionId no null, cuenta rol='supervision' estado='activo' si no 400 (controller:350-354). Si presente entra al roster (controller:413). Muestra empresa (empresa-supervision). |
| Aviso misma empresa (aviso-misma-empresa) | derivado (aviso, no campo de captura) | no | Solo AVISO (no bloquea, O3): si superintendente.empresa_id === supervision.empresa_id (jsx:324-333). Criterio del equipo: la supervisión debe ser tercero independiente (no es regla dura). |

### AltaContrato — Paso 2: Catálogo de conceptos (tab 1)  ·  HU-01
- **Ruta:** `/contratos/alta (tab Catálogo de conceptos)`
- **Roles que editan:** residente (E). Resto C. soloLectura deshabilita inputs (prop soloLectura en TabCatalogo).
- **Notas:** Al menos un concepto obligatorio (el monto se deriva del catálogo): validarPaso(1) jsx:1534 y back si no hay catálogo exige monto manual (precio alzado, controller:144-148). MAX_FILAS 500 por bloque (controller:101). Monto derivado SIEMPRE server-side con el MISMO motor de redondeo NUMERIC que el subtotal de estimaciones (cuadre exacto al centavo). conceptos→contrato_conceptos.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Clave del concepto (concepto-clave-N) | text (maxLength 40, mono) | sí | Front validarPaso(1): no vacía, ≤40 chars, ÚNICA entre conceptos (jsx:1540-1543). Back: clave requerida (controller:114), ≤40 (controller:117), UNIQUE(contrato_id,clave) → 400 'clave repetida' (err 23505 controller:504). Clave CAPTURADA por el usuario, art. 45 fr. IX RLOPSRM. |
| Concepto (concepto-concepto-N) | text | sí | Front validarPaso(1): no vacío (jsx:1544). Back: c.concepto requerido (controller:108). |
| Unidad (concepto-unidad-N + input 'Otro') | select (m, m², m³, ml, cm, kg, ton, pza, lote, jornal, %, Otro…) + text si Otro (maxLength 20) | sí | Front validarPaso(1): no vacía (jsx:1544). Back: c.unidad requerida (controller:108), ≤20 chars (controller:120). |
| Cantidad (concepto-cantidad-N) | number (min 0, step 0.001) — NUMERIC(14,3) | sí | Front validarPaso(1): >0 y < MAX_CANTIDAD 1e11 (jsx:1544,1548). Back: numOrNull >0 si no 400 (controller:125). Recalcula importe en vivo (onConceptoCantidad jsx:1374). |
| P.U. (concepto-pu-N) | number (min 0, step 0.0001) — NUMERIC(16,4) | condicional | Obligatorio el P.U. O el Importe (uno deriva al otro). Front validarPaso(1): pu>0 y < MAX_PU 1e12 (jsx:1544,1549). Back: numOrNull >0 si no 400 (controller:126). pu es el dato canónico; importe=ROUND(cant×pu,2). |
| Importe (concepto-importe-N) | number (min 0, step 0.01) — derivado/editable bidireccional | condicional | Alternativa al P.U.: teclear importe back-solea pu = round4(importe/cantidad) en blur (jsx:1377-1383). No se envía al backend (solo cantidad+pu en payload jsx:1796). El monto del contrato = Σ importes, derivado server-side. |

### AltaContrato — Paso 3: Programa de obra / matriz concepto×periodo (tab 2)  ·  HU-01
- **Ruta:** `/contratos/alta (tab Programa de obra)`
- **Roles que editan:** residente (E). Resto C. soloLectura deshabilita celdas/select.
- **Notas:** Matriz concepto×periodo (art. 45 fr. X RLOPSRM). Solo celdas con cantidad>0 se envían (jsx:1800-1805); back ignora celda 0 (controller:220). REGLA DEL 100% por concepto (cuadre exacto al centavo, sin parciales). Periodos los genera SIEMPRE el backend (no se confía en el cliente). guardarMatriz hace lock advisory + freeze + DELETE+INSERT + valida cuadre en SQL (C2/C5/C7). programa→contrato_periodos + matriz.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Ciclo de estimación (select-ciclo) | select (Mensual ~30d / Quincenal 15d) | condicional | Obligatorio si hay programa. Front: default 'mensual'; genera columnas (periodos) en vivo (generarPeriodosC jsx:430, espejo de lib/programa.js). Back: ciclo ∈ {mensual,quincenal} si no null; con programa requiere ciclo si no 400 (controller:201,206). art. 54 LOPSRM. |
| Celda cantidad planeada concepto×periodo (celda-i-N) | number (min 0, step 0.001) | condicional | REGLA DEL 100% por concepto. Front validarPaso(2): sin negativos (jsx:1561); Σ por concepto = contratado (\|restante\|≤TOL_PROGRAMA 0.0005) si no descuadre bloquea (jsx:1570-1578). Back: generarPeriodos autoritativo; guardarMatriz revalida en SQL → PROGRAMA_DESCUADRE/PROGRAMA_EXCEDE/PROGRAMA_AJENO 400, PROGRAMA_CONGELADO 409 (controller:494-499). clave debe estar en catálogo, periodo en rango 1..N (controller:217-218). Fundamento RLOPSRM art. 45-A-X + LOPSRM art. 52 (programa convenido del 100% de conceptos). |

### AltaContrato — Paso 4: Datos jurídicos (tab 3)  ·  HU-01
- **Ruta:** `/contratos/alta (tab Datos jurídicos)`
- **Roles que editan:** residente (E). Resto C. soloLectura via RegionEditable.
- **Notas:** Todo el bloque se persiste como un único JSONB datos_juridicos (sin validación server-side de campos). REQ_JURIDICOS (firmante, cargo, representante, cédula) son obligatorios SOLO en front (validarPaso(3)). Antes era todo opcional (alta-v5 lo hizo obligatorio).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Firmante autorizado de la dependencia (jur-firmante) | text | sí | Front validarPaso(3): no vacío (REQ_JURIDICOS jsx:186,1586). Fundamento art. 46 fr. I LOPSRM. Prefill (P7): se rellena con nombre de la cuenta dependencia si está vacío, editable (jsx:1466). Back: no valida juridicos individualmente; se guarda como JSONB libre datos_juridicos (controller:397). |
| Cargo del firmante (jur-cargo) | text | sí | Front validarPaso(3): no vacío (REQ_JURIDICOS). art. 47/48 LOPSRM. Back: JSONB libre (sin validación back). |
| Representante legal del contratista (jur-representante) | text | sí | Front validarPaso(3): no vacío. art. 46 fr. IV LOPSRM + RLOPSRM art. 61 fr. VI-b/VII. Prefill con nombre del superintendente si vacío, editable (jsx:1465). Back: JSONB libre. |
| Cédula profesional del responsable técnico (jur-cedula) | text | sí | Front validarPaso(3): no vacío (REQ_JURIDICOS). NO exigida por LOPSRM/RLOPSRM federal al alta: criterio del equipo (default conservador, decisión Fundación). Back: JSONB libre. |
| No. de poder notarial (jur-poder) | text | no | Front: opcional (no en REQ_JURIDICOS). Back: JSONB libre. |
| Notaría (jur-notaria) | text | no | Front: opcional. Back: JSONB libre. |

### AltaContrato — Paso 5: Garantías, penalizaciones y amortización (tab 4)  ·  HU-01
- **Ruta:** `/contratos/alta (tab Garantías)`
- **Roles que editan:** residente (E). Resto C. soloLectura deshabilita.
- **Notas:** Fianzas OBLIGATORIAS por ley (gate front validarPaso(4)): CUMPLIMIENTO siempre (art. 47 + 48 fr. II), ANTICIPO si %anticipo>0 (art. 48 fr. I). Indicador garantias-ok/garantias-faltan. Una fila TOTALMENTE vacía se ignora (garantiaVacia jsx:78 / controller:43). Solo pólizas completas se persisten (jsx:1811 filter polizaCompleta). Back NO exige cumplimiento/anticipo (eso es gate front); back solo valida coherencia (monto>0, ≤contrato, vigencia). garantias→contrato_garantias. NOTA: estas garantías del alta NO pasan por gateCierre; las de HU-02 sí.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| % de anticipo otorgado (anticipo-input) | number (min 0, max 100, step 0.01) | no | Vacío=0=sin anticipo. Front validarPaso(4): si capturado, 0–100 (jsx:1596). Back: numOrNull, 0–100 si no 400 (controller:78-81). Base de amortización (art. 50 LOPSRM). >30% (ANTICIPO_UMBRAL_PDF) exige PDF autorización (art. 50 fr. IV). >50% aviso SFP art. 139 RLOPSRM; =100% solo plurianual último trimestre art. 50 fr. V. |
| PDF autorización del anticipo (anticipo-pdf-input / anticipo-pdf-pendiente-file) | file (application/pdf, máx 10 MB) | condicional | OBLIGATORIO si %anticipo > 30% (validarPaso(4) jsx:1602: bloquea avance Y guardado). Front: type=application/pdf, ≤10MB (jsx:582). Back subirDocumento tipo='anticipo_autorizacion': magic bytes %PDF (controller:637), append-only POR TIPO → 409 inmutable si ya existe (controller:653-657), solo residente asignado (403 controller:649). art. 50 fr. IV LOPSRM. |
| Tipo de póliza (garantia-tipo-N) | select (Cumplimiento, Anticipo, Vicios ocultos, Otra) | condicional | Obligatorio por fila no-vacía. Front validarPaso(4): si fila no vacía, tipo requerido + sin tipos duplicados (UNIQUE contrato_id,tipo) jsx:1613-1615. Back: g.tipo requerido si fila no vacía (controller:173); se canoniza a minúsculas+guion bajo antes de enviar (jsx:1811) para que UNIQUE muerda. Cumplimiento art. 48 fr. II, Anticipo art. 48 fr. I LOPSRM. |
| Afianzadora (garantia-afianzadora-N) | text | condicional | Obligatoria por fila no-vacía. Front validarPaso(4): requerida (jsx:1616). Back: no valida afianzadora explícitamente (solo tipo/monto/vigencia). |
| No. de póliza (garantia-poliza-N) | text (mono) | condicional | Obligatoria por fila no-vacía. Front validarPaso(4): requerida (jsx:1617). Back: no la valida (se persiste emptyToNull). |
| Monto (garantia-monto-N) | number (min 0, step 0.01); READ-ONLY si tipo=Anticipo (derivado) | condicional | Obligatorio por fila no-vacía. Front validarPaso(4): >0 (jsx:1618) y ≤ monto del contrato (jsx:1627). Si tipo=Anticipo monto DERIVADO = round2(%anticipo × monto) read-only (jsx:665,679). Back: numOrNull >0 (controller:175), gm ≤ monto contrato si no 400 (controller:181). AVISO no bloqueante (modal-exceso-garantia) si monto > % esperado (Cumplimiento 10%, Anticipo %anticipo); jsx:1418-1431. |
| Vigencia (garantia-vigencia-N) | date | condicional | Obligatoria por fila no-vacía. Front validarPaso(4): requerida + ≥ hoy local (no vencida, jsx:1622, O1-P5b). Back: si presente, vigencia ≥ ayerUTC (desfase zona horaria) si no 400 'ya está vencida' (controller:188-193, art. 48 LOPSRM). |

### AltaContrato — Paso 6: Plan de amortización del anticipo (tab 5, oculto sin anticipo)  ·  HU-01
- **Ruta:** `/contratos/alta (tab Plan de amortización; PASO_PLAN_AMORT=5)`
- **Roles que editan:** residente (E). Resto C. soloLectura deshabilita inputs.
- **Notas:** Paso OMITIDO (oculta, pestaña salta) si %anticipo=0 o montoAnticipo≤0 (planOmitido jsx:1411). Plan EDITABLE pedido por el profe; botón 'Restablecer proporcional al programa' (plan-restablecer). montoAnticipo = ROUND(monto derivado × %anticipo,2). Σ debe ser exacta al centavo. Si API no manda plan, back DERIVA el default proporcional (retrocompat). [Fase B pendiente validar profe: que la carátula G2 obedezca este plan; hoy la carátula amortiza proporcional al avance]. planAmortizacion→plan_amortizacion.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Monto a amortizar por periodo (plan-monto-N) | number (min 0, step 0.01) | condicional | Obligatorio si hay anticipo (paso omitido sin anticipo). Front validarPaso(5): cada periodo monto ≥0 (jsx:1658); Σ plan = montoAnticipo EXACTO al centavo (jsx:1662, art. 138 párr. 3 RLOPSRM); R3 no exceder lo programado del periodo (jsx:1677); R2 todo periodo con obra amortiza algo >0 (jsx:1680) — art. 143 fr. I RLOPSRM. Back revalida idéntico: Σ=anticipo (controller:275), R3 (controller:310), R2 (controller:313). Precarga DEFAULT PROPORCIONAL AL PROGRAMA (planProporcionalPrograma jsx:135 / espejo back controller:283-289). |
| Programado (cobro) por periodo (plan-programado-N) | derivado (read-only) | no | DERIVADO = Σ ROUND(cantidad×pu,2) de celdas del programa del periodo (importeProgramado useMemo jsx:1395; back programadoPorPeriodo controller:251). Es 'lo que se estima cobrar'; tope de R3. |

### AltaContrato — Paso 7: PDF firmado + Registrados (tab 6 y tab 7)  ·  HU-01
- **Ruta:** `/contratos/alta (tab PDF firmado = ULTIMO_PASO 6; tab Registrados = 7 auxiliar)`
- **Roles que editan:** residente (E) sube/guarda. Todos los actores autorizados consultan/descargan PDFs (criterio HU-01). soloLectura oculta 'Subir PDF'.
- **Notas:** Tras guardar con éxito: resetFormulario + redirect a /bitacora/apertura?contrato=ID (FIX 22-jun). Pestaña 'Registrados' (IDX 7) SIEMPRE navegable (lista solo lectura). Modal 'Ver info del contrato' (ModalDetalleContrato, ver-info-ID): SOLO LECTURA, reúne cabecera+conceptos+garantías+jurídicos+programa+documentos+indicador de atraso (gateado por HU-07 y participación 403→oculto). Documentos consultables Ver/Descargar por todos los con participación (esParteOSupervision). PDFs (BYTEA) son inmutables/append-only por tipo. listarContratos acota por participación (operativos solo donde son parte; dependencia/finanzas ven todo, post-filtro por empresa).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| PDF firmado del contrato (pdf-firmado-input-precaptura / pdf-firmado-pendiente-file) | file (application/pdf, máx 10 MB) | sí | OBLIGATORIO para guardar (validarPaso(6) jsx:1694; botón Guardar deshabilitado sin él jsx:2030). Front: type=application/pdf, ≤10MB (jsx:840). Back subirDocumento tipo='contrato' (default): magic bytes %PDF (controller:637), solo residente asignado 403 (controller:649), APPEND-ONLY → 409 inmutable si ya existe (controller:653). Criterio del equipo: el contrato se formaliza/existe una vez firmado. |
| Subir PDF desde Registrados (input file oculto) | file (application/pdf, máx 10 MB) | no | Post-alta, para contratos sin PDF aún. Mismas reglas back (append-only, solo residente). Solo visible si !soloLectura (jsx:1259). |

### RegistroFianzas (gestión post-alta de pólizas)  ·  HU-02
- **Ruta:** `RegistroFianzas.jsx (HeaderVista huId=HU-02); banco de endpoints /api/garantias`
- **Roles que editan:** dependencia (E). residente=C en permisos.js PERO el backend (puedeGestionar) permite EDITAR a dependencia O residente/creador del contrato. finanzas=C; contratista/supervision = sin acceso (permisos.js HU-02). useVistaHU('HU-02').soloLectura oculta botones de mutación.
- **Notas:** Crear: POST /api/garantias/contrato/:id (puedeGestionar = dependencia O residente/creador, 403 si no). Editar: PUT /api/garantias/:gid (contrato_garantias ES editable, sin trigger append-only). FIX #3: contrato cerrado (finiquito) = SOLO-LECTURA 409 (art. 64 LOPSRM, gateCierre) en crear/endoso. Lectura por participación (esParteOSupervision). Alertas de vencimiento a 30/15/5 días (badgePorDias). Las garantías ya persistían desde el alta; esta pantalla las gestiona/endosa con PDF real.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato (BannerContratoActivo) | derivado (heredado del contrato activo global) | sí | Se hereda del contrato activo global (no se re-selecciona). Back: acceso por participación (esParteOSupervision) → 403 si no es parte (controller:59). |
| Tipo (mp-tipo) [Modal Póliza] | select (Cumplimiento / Anticipo / Vicios ocultos); deshabilitado en modo editar; solo tipos disponibles en agregar | sí | Front datosOk: !!form.tipo (jsx:162). Solo tipos NO presentes (tiposDisponibles jsx:152). Back validarGarantia: tipo requerido (controller:27); UNIQUE(contrato_id,tipo) → 409 'ya tiene garantía de ese tipo' (controller:106); tipo canonizado minúsculas+guion bajo (controller:96). art. 48 LOPSRM (una por tipo). |
| Afianzadora (mp-afianzadora) | text | sí | Front datosOk: afianzadora.trim().length>0 (jsx:162). Back: no la valida explícitamente (se trim/null controller:101). |
| Número de póliza (mp-folio) | text | no | Front: sin validación (no en datosOk). Back: trim o null (controller:101). |
| Monto afianzado MXN (mp-monto) | number (min 0, step any) | sí | Front datosOk: Number(monto)>0 (jsx:162). Back validarGarantia: monto>0 (controller:29) y ≤ monto del contrato si no 400 (controller:30). art. 48 LOPSRM. |
| Fecha de vencimiento (mp-vencimiento) | date | sí | Front datosOk: !!vigencia (jsx:162). Back validarGarantia: si presente no vencida (vigenciaVencida = < ayerUTC) si no 400 (controller:31). art. 48 LOPSRM. |
| Archivo PDF de la póliza (mp-archivo) | file (application/pdf) | no | Front: opcional (reemplaza el actual en editar). Back subirPdfGarantia: magic bytes %PDF (controller:178), solo gestor (dependencia/residente/creador) 403 (controller:183). REEMPLAZABLE (UPDATE, NO append-only, a diferencia de los PDF del contrato). |

### RegistroFianzas — Modal Endoso (ajuste de garantía)  ·  HU-02
- **Ruta:** `RegistroFianzas.jsx ModalEndoso; POST /api/garantias/:garantiaId/endoso`
- **Roles que editan:** dependencia (E) / residente/creador (back). soloLectura oculta '+ endoso'.
- **Notas:** APPEND-ONLY: garantia_endosos tiene trigger BEFORE UPDATE de inmutabilidad — corregir = endoso nuevo, no editar (controller:138). registrado_por sale del JWT (controller:166), nunca del body. Contrato cerrado → 409 (gateCierre art. 64). El endoso NO altera la garantía original (criterio HU-02). art. 91 RLOPSRM.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Motivo (endoso-motivo) | select (Ampliación de monto / Prórroga de vigencia / Mixto / Otro) | sí | Front: select con default 'prorroga_vigencia'. Back: motivo ∈ MOTIVOS si no 'otro' (controller:150). Si motivo exige monto (ampliacion_monto/mixto) requiere nuevo_monto>0 (controller:159); si exige vigencia (prorroga/mixto) requiere nueva_vigencia (controller:160). art. 91 RLOPSRM. |
| Nuevo monto MXN (endoso-monto) | number (min 0, step any) | condicional | Obligatorio >0 si motivo=ampliacion_monto/mixto (controller:159). Front: sin gate (botón solo se bloquea por guardando). Back: numOrNull; si no null debe ser ≥0 (controller:152). Endoso totalmente vacío (sin monto ni vigencia) → 400 (controller:161). |
| Nueva vigencia (endoso-vigencia) | date | condicional | Obligatoria si motivo=prorroga_vigencia/mixto (controller:160). Back: si presente no vencida (controller:153). art. 91 RLOPSRM (remite art. 98 fr. II). |
| Observaciones (endoso-obs) | textarea | no | Front/back: opcional (trim o null controller:166). |

### RosterContrato — Sustitución de personas del roster  ·  Roster del contrato (sustitución; art. 125 fr. I g RLOPSRM). Sin HU numerada propia — pertenece a Fundación; relacionado a HU-22 (roster). No tiene huId en HeaderVista (usa Breadcrumb).
- **Ruta:** `RosterContrato.jsx; GET /api/roster/contrato/:id, POST /api/roster/contrato/:id/sustituir`
- **Roles que editan:** Sin useVistaHU (no gateado por matriz). Back puedeGestionar: dependencia (cualquier contrato) O residente asignado / creador del contrato (solo su contrato). Otros roles: 403. Lectura por participación.
- **Notas:** SUSTITUIR, NO BORRAR (art. 125 fr. I g RLOPSRM): cierra la fila activa (vigencia_hasta=CURRENT_DATE) + crea nueva ligada por sustituye_a + asienta nota de bitácora (res_sustitucion, atómica) + sincroniza caché escalar de contratos (UN solo punto, transaccional). Todo en una transacción con pg_advisory_xact_lock(3,contratoId) + FOR UPDATE. GATES BACK adicionales: (1) bitácora abierta obligatoria (P23) → 409 si no (controller:131-132, art. 122/125); (2) contrato cerrado (finiquito) → 409 SOLO-LECTURA (art. 64, gateCierre controller:127); (3) la persona saliente NO puede tener notas de bitácora pendientes de su firma dentro de plazo → 409 (FIX 22-jun controller:171-190); (4) el sustituto debe pertenecer a la MISMA empresa que el saliente → 409 (REGLA 4, criterio equipo, fail-open si saliente sin empresa, controller:198-205). registrado_por sale del JWT. Las firmas pasadas conservan su firmante original (sustitución solo afecta firmas FUTURAS). Histórico 1:N en contrato_roster (append-only de facto vía cierre+nueva fila). Evento derivado en front: 'Alta del contrato' vs 'Sustitución' (jsx:102).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato (BannerContratoActivo) | derivado (contrato activo global) | sí | Heredado del contrato activo (setContratoId). Back leerRoster: acceso por participación → 403 (controller:48). |
| Rol a sustituir (sust-rol) | select (Residente de obra / Superintendente (contratista) / Supervisión externa) | sí | Front: requerido (botón disabled sin él jsx:222). Back: rol ∈ ROLES_ROSTER {residente,superintendente,supervision} si no 400 (controller:99). La DEPENDENCIA NO es sustituible (FIX 3.4): no está en el whitelist ni en COL_CACHE — art. 125 fr. I g solo lista esos 3 (aviso roster-dependencia-aviso). |
| Nueva persona (sust-nuevo) | select (cuentas reales del rol-de-cuenta correcto vía api.listarAsignables(ROL_CUENTA[rol])) | sí | Front: requerido (jsx:222). Se ELIMINÓ teclear ID a mano (B-1 trazabilidad). Si no hay elegibles, avisa (no captura). Back: nuevoUsuarioId entero >0 (controller:100); usuario debe existir (controller:136), estar 'activo' (controller:138), tener rol-de-cuenta esperado ROL_CUENTA[rol] (controller:139). No puede ya ocupar el rol (controller:165). |
| Motivo (sust-motivo) | textarea | sí | Front: motivo.trim() requerido (jsx:222). Back: motivo obligatorio si no 400 'El motivo es obligatorio (art. 125 fr. I g RLOPSRM)' (controller:101). |


---

## Bitácora

### Apertura formal de la bitácora (AperturaBitacora.jsx)  ·  HU-08
- **Ruta:** `/bitacora/apertura  (acepta ?contrato=ID para preseleccionar)`
- **Roles que editan:** residente E (solo el residente ASIGNADO al contrato puede aperturar). contratista/supervision/dependencia = C (consulta). finanzas = sin acceso. Gating front: useVistaHU('HU-08').soloLectura = nivel!=='E'; además front exige soyResidenteDelContrato = contratoSel.residente_id===usuario.id (frontend/src/pages/AperturaBitacora.jsx:140,175). Gating back: router requireRole('residente') (bitacora.routes.js:22) + controller exige contrato.residente_id===req.user.id 403 (bitacora.controller.js:97-100).
- **Notas:** Acción APPEND-ONLY e inmutable: crea bitacora_aperturas (acta snapshot JSON, art.123 fr.VI RLOPSRM), una fila bitacora_firmantes (firmado=false) por miembro del equipo, y la NOTA #1 tipo 'apertura' (no se emite a mano; bitacora.controller.js:183-187, art.123 fr.III). Nadie firma aquí: cada parte firma en 'Por firmar'. La fecha/hora de apertura es inalterable (art.46 LOPSRM + 122-123 RLOPSRM). Server-side hace ASIENTOS DIFERIDOS: si antes de abrir hubo sustituciones de roster / avances / convenios sin nota, genera ahora sus notas automáticas numeradas tras la #1 (bitacora.controller.js:196-273). UNIQUE bitácora por contrato. Gating de rol server-side: requireRole('residente') + residente asignado.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo, no es <select> propio; hereda el contrato activo global) | derivado (banner) | sí | Front: si no hay contratoId no se muestra el formulario (AperturaBitacora.jsx:227). Back: api.bitacoraDeContrato(id) 404 si no hay bitácora; 403 si no eres parte (esParteOSupervision, bitacora.controller.js:359). Acceso por participación: lib/acceso.js:18. |
| Equipo que firmará (Residente / Superintendente / Supervisión) — solo lectura, derivado del contrato (EquipoFila) | derivado (display) | no | Display de contratoSel.residente_nombre / superintendente_nombre / supervision_nombre. Supervisión opcional ('Sin supervisión (opcional)' AperturaBitacora.jsx:254). Back: el roster de firmantes se arma de residente_id+superintendente_id(+supervision_id) (bitacora.controller.js:108-114). |
| Folio (input readOnly, derivado del contrato) | text (readOnly) | no | Solo lectura; valor = contratoSel.folio (AperturaBitacora.jsx:260). |
| Monto contractual (input readOnly, derivado) | text (readOnly, formato MXN) | no | Solo lectura; mxn(contratoSel.monto) (AperturaBitacora.jsx:261). |
| Plazo (input readOnly, derivado) | text (readOnly) | no | Solo lectura; contratoSel.plazo_dias + ' días' (AperturaBitacora.jsx:262). |
| Entrega del sitio · testid=input-fecha-apertura | date | sí | Front: required; puedeAperturar exige !!fechaEntregaSitio (AperturaBitacora.jsx:175,270). Prefill = soloFecha(contrato.fecha_inicio) (linea 153). Back: 400 'Falta la fecha de entrega del sitio' si vacía (bitacora.controller.js:57). NOTA: fecha_apertura de la BD = contrato.fecha_inicio (la bitácora abre el MISMO día que arranca el contrato, hallazgo profe audio 2026-06-01, bitacora.controller.js:133-136,144); la entrega del sitio se guarda aparte en el acta (cronograma.entrega_sitio). |
| Plazo de firma de notas (días naturales) · testid=input-plazo-firma | number (min 1, max 60) | no | Front: min=1 max=60; default 2 (AperturaBitacora.jsx:131,274). Back: default 2 si null; debe ser entero 1..60 → 400 'plazoFirmaDias debe ser un entero entre 1 y 60' (bitacora.controller.js:61-65). Schema CHECK chk_bitacora_aperturas_plazo 1..60 (schema.sql:404-407). Fundamento: art. 123 fr. III RLOPSRM (vencido el plazo, notas se tienen por aceptadas). |
| Domicilio de la dependencia · testid=md-domicilio-dependencia | text | sí | Front: MIN_KEYS exige trim()!=='' (minDataCompleta gobierna puedeAperturar; aviso md-incompleto) (AperturaBitacora.jsx:134-137,282,289). Back: se normaliza con trim()\|\|null (bitacora.controller.js:68-69); se persiste en bitacora_aperturas.domicilio_dependencia y en el acta-snapshot (art. 123 fr. III RLOPSRM, bitacora.controller.js:16-21,144). NOTA: el back NO rechaza si vienen vacíos (solo el front bloquea). |
| Teléfono de la dependencia · testid=md-telefono-dependencia | text | sí | Igual que el anterior. Front bloquea (minDataCompleta). Back normaliza trim()\|\|null (bitacora.controller.js:70). art. 123 fr. III RLOPSRM. |
| Domicilio del contratista · testid=md-domicilio-contratista | text | sí | Front bloquea (minDataCompleta). Back normaliza trim()\|\|null (bitacora.controller.js:71). art. 123 fr. III RLOPSRM. |
| Teléfono del contratista · testid=md-telefono-contratista | text | sí | Front bloquea (minDataCompleta). Back normaliza trim()\|\|null (bitacora.controller.js:72). art. 123 fr. III RLOPSRM. |
| Alcance/descripción de los trabajos · testid=md-descripcion-trabajos | textarea (rows 2) | sí | Front bloquea (minDataCompleta). Back normaliza trim()\|\|null (bitacora.controller.js:73). art. 123 fr. III RLOPSRM. |
| Características del sitio · testid=md-caracteristicas-sitio | textarea (rows 2) | sí | Front bloquea (minDataCompleta). Back normaliza trim()\|\|null (bitacora.controller.js:74). art. 123 fr. III RLOPSRM. |
| Botón Iniciar apertura · testid=btn-aperturar | submit (acción) | no | Front: disabled hasta puedeAperturar = !soloLectura && soyResidenteDelContrato && tieneSuperintendente && !bitacora && !cargando && fechaEntregaSitio && minDataCompleta && !aperturando (AperturaBitacora.jsx:175). Back POST /api/bitacora/apertura: 400 contratoId inválido / falta fecha / plazo inválido; 404 contrato no encontrado; 403 no es residente asignado; 400 'no tiene superintendente asignado' (linea 102-105); 409 (codigo 23505) 'Ya existe una bitácora para este contrato' (UNIQUE por contrato). Es TRANSACCIONAL. |

### Emisión y respuesta de notas tipificadas con firma (EmisionNotas.jsx)  ·  HU-09
- **Ruta:** `/bitacora/notas (acepta ?contrato=ID)`
- **Roles que editan:** residente E, contratista E, supervision E (las tres PARTES emiten). dependencia/finanzas = sin acceso (permisos.js:20). Gating front: useVistaHU('HU-09'); además esParte = !!data.mi_rol (rol en ESTE contrato; dependencia/finanzas no son parte → solo observan). Gating back: el controller deriva rolEnContrato del JWT vs residente_id/superintendente_id/supervision_id (bitacora.controller.js:408-412); 403 si no es parte.
- **Notas:** Las PARTES son residente/superintendente/supervisión (esParte = data.mi_rol no null); dependencia/finanzas no tienen acceso a HU-09 en permisos.js, pero el controller también las trataría como observadores (mi_rol null). UN emisor por nota (art.125), folio del servidor. Estado de ACEPTACIÓN derivado server-side (bitacora.controller.js:660-671): anulada / firmada (cuando todo el roster firmó, antes del plazo) / apertura→conjunta / respondida / aceptada_tacita (plazo vencido sin firmas) / en_plazo. Append-only e inmutable en triggers. Candado de emisión server-side (apertura firmada por TODOS). Contrato cerrado (finiquito) = solo-lectura (art.64).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo) | derivado (banner) | sí | Front: sin contratoId no hay formulario (EmisionNotas.jsx:282). Si no hay apertura → aviso sin-apertura, no se puede emitir (linea 284-288). Back bitacoraDeContrato/listarNotas acota por participación (esParteOSupervision). |
| Firmar apertura (nota #1) · testid=btn-firmar-apertura | acción (botón) | condicional | Solo visible si !soloLectura && miFirmaAperturaPendiente (soy firmante y no he firmado) (EmisionNotas.jsx:69,311). Back POST /:aperturaId/firmar: 403 'No eres firmante'; 409 'Ya firmaste'; transición pendiente→firmado, deriva 'completa' (bitacora.controller.js:293-325). APPEND-ONLY (firma conjunta de TODOS). |
| Tipo de nota · testid=select-tipo | select | sí | Front: opciones = tiposDeMiRol = catálogo filtrado por activo!==false && (rol_emisor===miRol \|\| null) && clave!=='apertura' (EmisionNotas.jsx:72-75). Back emitirNota: 400 si falta tipo; 400 'no se emite a mano' si tipo==='apertura' (bitacora.controller.js:548); validarTipoParaRol: 400 'Tipo no reconocido' / 403 'Tu rol no puede emitir notas de tipo X' (bitacora.controller.js:416-424). Matriz rol→tipo: art. 125 RLOPSRM (catálogo bitacora_nota_tipos, schema.sql:385-396,918-958: residente autoriza/aprueba, superintendente solicita/avisa, supervisión avance/calidad/seguridad/junta, 'otro' rol_emisor NULL para todos). |
| Tag de búsqueda · testid=input-tag | text (maxLength 60) | no | Front: maxLength=60 (EmisionNotas.jsx:359). Back: trim(); 400 'El tag no puede exceder 60 caracteres' (bitacora.controller.js:552). Sirve para búsqueda en HU-10. |
| Asunto · testid=input-asunto | text | no | Front: opcional. Back: trim(); 400 'El asunto no puede exceder 200 caracteres' (bitacora.controller.js:551). VARCHAR(200) en schema (bitacora_notas.asunto, schema.sql:420). |
| Contenido · testid=input-contenido | textarea (rows 5) | sí | Front: enviar() exige contenido.trim() (toast 'Tipo y contenido son obligatorios'); btn deshabilitado sin contenido (EmisionNotas.jsx:138,376). Back: 400 'Falta el contenido'; 400 'El contenido no puede exceder 5000 caracteres' (bitacora.controller.js:549-550). |
| Botón Emitir nota / Emitir respuesta · testid=btn-emitir | submit (acción) | no | Front: disabled = enviando \|\| !contenido.trim() \|\| !aperturaCompleta (EmisionNotas.jsx:376). CANDADO de emisión: no se emite hasta que la apertura esté firmada por TODOS (gate-emision UI linea 330-335). Back: candado SERVER-SIDE 409 'No se pueden emitir notas hasta que la apertura esté firmada por TODOS (faltan N firma(s))' (bitacora.controller.js:569-577); 403 si no eres parte; 409 contrato cerrado/finiquito (gateCierre, art.64 LOPSRM, bitacora.controller.js:563). Folio correlativo ATÓMICO server-side (advisory lock + MAX+1, UNIQUE(bitacora_id,numero); bitacora.controller.js:428-438). El emisor sale del JWT, nunca del body. Al emitir, FIRMA la nota (firmado_en=NOW) e INMUTABLE. |
| Firmar (aceptar) nota · testid=btn-firmar-nota-{numero} | acción (botón por nota) | condicional | Front visible si puedeFirmar = !soloLectura && esParte && !soyEmisor && !yaFirme && estado==='emitida' (EmisionNotas.jsx:200,237). Back POST /notas/:notaId/firmar: 404 nota; 409 si tipo='apertura' (va por Por firmar); 409 si anulada; 403 si no es parte; 409 si es el emisor; ON CONFLICT (nota_id,usuario_id) → 409 'Ya firmaste' (bitacora.controller.js:882-918). APPEND-ONLY (bitacora_nota_firmas). art. 123 fr. III RLOPSRM (firma por la contraparte dentro del plazo). |
| Responder / vincular · testid=btn-responder-{numero} | acción (abre form de respuesta) | condicional | Front visible si puedeResponder = !soloLectura && esParte && estado==='emitida' (EmisionNotas.jsx:199). Reusa el form (tipo/asunto/tag/contenido) → api.vincularNota. Back POST /notas/:notaId/vincular: crea nota NUEVA con vinculada_a (la original queda intacta, art.123 fr.VIII/XII); 404 nota; 403 no parte; valida que la nota referenciada pertenezca a la MISMA bitácora 400 (art.123 fr.VI, bitacora.controller.js:839-842); valida tipo→rol (art.125). |
| Anular (form 'dice / debe decir') · testid=btn-anular-{numero}, textarea correccion, testid=btn-confirmar-anular-{numero} | acción + textarea | condicional | Front visible si puedeAnular = !soloLectura && esParte && soyEmisor && estado==='emitida' && aceptacion!=='respondida' (EmisionNotas.jsx:198). confirmarAnular exige correccion.trim() (linea 153). Back POST /notas/:notaId/anular: 400 'Falta texto de corrección'; 400 max 5000 chars; 403 'apertura no puede anularse'; 403 'Solo el emisor puede anular'; 409 'ya anulada'; 409 'ya fue respondida por otra parte' (bitacora.controller.js:755-806). NO edita: pone estado='anulada' y crea nota correctiva vinculada (append-only, art.123 fr.VII). |
| Ver como documento · testid=btn-doc-nota-{numero} / btn-doc-apertura | acción (abre DocumentoNota imprimible) | no | Solo lectura, siempre disponible (EmisionNotas.jsx:230,322). La apertura muestra sus firmas conjuntas reales. |
| Ver bitácora (toggle del libro) · testid=btn-ver-bitacora | acción (toggle) | no | Muestra/oculta lista-notas (notasResto, sin la apertura para no duplicar) (EmisionNotas.jsx:386-400). |

### Consulta y búsqueda de notas de bitácora (ConsultaNotas.jsx)  ·  HU-10
- **Ruta:** `/bitacora/consulta (acepta ?contrato=ID)`
- **Roles que editan:** residente E; contratista/supervision = C (consulta). dependencia/finanzas sin acceso (permisos.js:21). NOTA: esta pantalla es de SOLO CONSULTA/búsqueda/export para todos los roles con acceso; soloLectura NO bloquea nada aquí (ConsultaNotas.jsx:25-27). Gating real lo da el acceso por participación del back.
- **Notas:** Pantalla de CONSULTA pura: no captura, no muta. La búsqueda/filtros/selección/export son client-side sobre el payload de GET /api/bitacora/contrato/:id/notas (mismo construirPayloadNotas que HU-09). Acceso por participación server-side (esParteOSupervision). Criterios HU-10: búsqueda AND de todos los filtros; export multi-nota a Excel. PestanasCiclo ciclo='bitacora' activo='consulta'.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo) | derivado (banner) | sí | Front: sin contratoId no hay búsqueda (mensaje 'Activa un contrato', ConsultaNotas.jsx:178). Si el contrato no tiene bitácora → 404 → aviso-sin-bitacora con link a HU-08 (linea 184). Back api.notasDeContrato acota por participación (esParteOSupervision); 403 'No tienes acceso a las notas de este contrato'. |
| Filtros de búsqueda (componente BuscadorNotas / useFiltrosNotas): tipo, fecha desde/hasta, firmante, vínculo, palabra clave | select / date / text (filtros AND, client-side) | no | Front: filtros combinan con AND; resultados = notas que cumplen todos (criterio HU-10). Opciones de tipo vienen de api.notaTipos() (catálogo art.125). firmantesUnicos derivados de las notas. Sin validación de servidor: la búsqueda es client-side sobre las notas ya cargadas (ConsultaNotas.jsx:85,225-237). |
| Selección múltiple de notas (checkbox por fila + 'todas') · onToggle/onToggleTodas | checkbox | no | Front: Set de ids seleccionadas; toggleTodas marca/desmarca el resultado filtrado (ConsultaNotas.jsx:87-108). Habilita el botón Exportar. |
| Exportar a Excel · testid=btn-exportar | acción (export client-side ExcelJS) | no | Front: solo si seleccionadas.size>0 (ConsultaNotas.jsx:113,214). Genera .xlsx con columnas Folio, Fecha, Tipo, Emisor, Vínculo, Asunto, Contenido, Estado (linea 118-137). 100% client-side, sin backend. |
| Ver como documento (DocumentoNota) · onVerDocumento | acción (modal imprimible) | no | Solo lectura; usa aperturaFirmantes reales para la nota #1 (ConsultaNotas.jsx:236,247). |

### Minutas y agenda de visitas (MinutasVisitas.jsx)  ·  HU-11
- **Ruta:** `/bitacora/minutas (3 pestañas: Minutas, Agenda de visitas, Acuerdos)`
- **Roles que editan:** residente E (registra/agenda/vincula); contratista/supervision = C (consulta). dependencia/finanzas sin acceso (permisos.js:22). Gating front: useVistaHU('HU-11').soloLectura oculta formularios y botones de acción. Gating back: puedeRegistrar = residente_id===user.id \|\| created_by===user.id → 403 'Solo el residente asignado…' (minutas.controller.js:18,57,159). Lectura por esParteOSupervision.
- **Notas:** Estados de visita: agendada (Programada) / realizada (Realizada) / cancelada (Cancelada). El PDF se guarda como BYTEA (pdf_contenido) con metadatos pdf_nombre/mime/tamano. Vincular minuta/visita a nota NO altera la nota (art.123 fr.VI/X). Contrato cerrado (finiquito) bloquea registrar minuta/visita (art.64). El residente del contrato (o created_by) es la única autoridad de registro server-side. Rutas montadas en /api/minutas (las visitas viven bajo el mismo router).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo) | derivado (banner) | sí | Front: botones deshabilitados si !hayContrato (title 'Selecciona un contrato', MinutasVisitas.jsx:76,126). Back acota por participación; 403 'No tienes acceso'. |
| [Minuta] Fecha · testid=min-fecha | date | sí | Front: datosOk exige form.fecha (MinutasVisitas.jsx:60). Back crearMinuta: 400 'La fecha de la minuta es obligatoria' (minutas.controller.js:62). |
| [Minuta] Lugar · testid=min-lugar | text | sí | Front: datosOk exige lugar.trim() (MinutasVisitas.jsx:60). Back: 400 'El lugar de la minuta es obligatorio' (minutas.controller.js:64; añadido 22-jun para igualar la historia HU-11). |
| [Minuta] Participantes · testid=min-participantes | text | sí | Front: datosOk exige participantes.trim(). Back: 400 'Los participantes de la minuta son obligatorios' (minutas.controller.js:65). |
| [Minuta] Asunto (mapea a titulo) · testid=min-asunto | text | sí | Front: datosOk exige asunto.trim(); se envía como titulo (MinutasVisitas.jsx:222). Back: 400 'El asunto/título de la minuta es obligatorio' (minutas.controller.js:61). |
| [Minuta] Acuerdos · testid=min-acuerdos | textarea (rows 2) | no | Opcional; trim()\|\|null (minutas.controller.js:72). Alimenta la pestaña Acuerdos (solo minutas con acuerdos no vacíos). |
| [Minuta] Adjuntar minuta (PDF) · testid=min-archivo | file (accept application/pdf) | no | Front: accept=application/pdf; se sube tras crear la minuta (api.subirPdfMinuta) (MinutasVisitas.jsx:223). Back POST /:minutaId/pdf (multer): campo 'documento', solo PDF (fileFilter), límite 10MB → 400 'supera el límite de 10 MB' (minutas.routes.js:14-27); valida magic bytes %PDF → 400 'no es un PDF válido' (minutas.controller.js:106); 403 puedeRegistrar. Reemplazable. |
| [Minuta] Botón Registrar minuta · testid=btn-registrar-minuta | submit (acción) | no | Front: disabled = !datosOk \|\| !hayContrato \|\| guardando (MinutasVisitas.jsx:76). Back: 404 contrato; 403 puedeRegistrar; 409 contrato cerrado/finiquito (gateCierre, art.64); valida campos; si viene nota_id, debe pertenecer a la bitácora del contrato → 400 (minutas.controller.js:67). registrada_por = JWT. Fundamento art.123 fr.X RLOPSRM. |
| [Minuta] Adjuntar a nota (modal) · testid=btn-adjuntar-{id}, select testid=adjuntar-nota-select, testid=btn-vincular-nota | select (modal ModalAdjuntar) | no | Front: lista notas de la bitácora del contrato; opción '— Sin vincular —' permite desligar (MinutasVisitas.jsx:43-50). Back PATCH /:minutaId/nota: 404 minuta; 403 puedeRegistrar; si nota_id no nulo, debe pertenecer a la bitácora del contrato → 400 (minutas.controller.js:85-96). NO modifica la nota firmada (relación, no edición; art.123 fr.VI/X RLOPSRM). |
| [Minuta] Ver PDF · testid=btn-ver-pdf-min-{id} | acción (abre PDF en pestaña) | no | Solo si m.tiene_pdf. Back GET /:minutaId/pdf: 403 esParteOSupervision; 404 si sin PDF; entrega inline (minutas.controller.js:116-129). |
| [Visita] Fecha · testid=vis-fecha | date | sí | Front: datosOk exige form.fecha (MinutasVisitas.jsx:112); se envía como fecha_programada. Back crearVisita: 400 'La fecha programada de la visita es obligatoria' (minutas.controller.js:163). |
| [Visita] Lugar · testid=vis-lugar | text | sí | Front: datosOk exige lugar.trim(). Back: 400 'El lugar de la visita es obligatorio' (minutas.controller.js:165). |
| [Visita] Responsable · testid=vis-responsable | text | sí | Front: datosOk exige responsable.trim(). Back: 400 'El responsable de la visita es obligatorio' (minutas.controller.js:166). NOTA histórica (memoria): se propuso que el responsable sea un select del roster (art.114-p2/53), pero hoy es texto libre. |
| [Visita] Propósito · testid=vis-proposito | textarea (rows 3) | sí | Front: datosOk exige proposito.trim(). Back: 400 'El propósito de la visita es obligatorio' (minutas.controller.js:167). |
| [Visita] Botón Agendar visita · testid=btn-agendar-visita | submit (acción) | no | Front: disabled = !datosOk \|\| !hayContrato \|\| guardando (MinutasVisitas.jsx:126). Back: 404 contrato; 403 puedeRegistrar; 409 contrato cerrado/finiquito (art.64); valida campos. estado inicial 'agendada'; tipo default 'visita' (slice 20); registrada_por = JWT (minutas.controller.js:153-174). |
| [Visita] Adjuntar a nota · testid=btn-adjuntar-vis-{id} (mismo modal) | select (modal) | no | Back PATCH /visita/:visitaId/nota: 404 visita; 403 puedeRegistrar; nota debe pertenecer a la bitácora del contrato → 400 (minutas.controller.js:178-192). Relación, no edición. |
| [Acuerdos] (pestaña de solo lectura, derivada de minutas con acuerdos) | derivado (tabla) | no | Sin captura. Filtra minutas con acuerdos no vacíos y muestra minuta/fecha/acuerdo/nota vinculada (MinutasVisitas.jsx:160-187). Criterio HU-11. |

### Por firmar — aperturas de bitácora (PorFirmar.jsx)  ·  HU-09 (transversal; no es una HU propia en permisos.js — la ruta no se filtra por nivel, cualquier autenticado entra y firma su parte)
- **Ruta:** `/bitacora/por-firmar (acepta ?contrato=ID para resaltar filas desde la campana/centro)`
- **Roles que editan:** Cualquier rol AUTENTICADO que sea firmante de una apertura (residente/superintendente/supervisión del contrato). No usa useVistaHU; no hay nivel E/C: cada usuario firma SU propia parte. Back: router POST /:aperturaId/firmar sin requireRole; el controller valida que seas firmante (403 si no).
- **Notas:** Pantalla transversal de la firma conjunta de la APERTURA (no de notas ordinarias — esas se firman en HU-09 con btn-firmar-nota). No hay gating E/C: cualquier autenticado entra, pero solo ve/firma las aperturas donde es firmante (acotado al propio usuario en el back). Firma propia, inmutable, append-only. Mientras la apertura no esté firmada por TODOS, el candado server-side de HU-09 impide emitir notas. NOTA: comparte el endpoint POST /:aperturaId/firmar con el botón 'Firmar apertura' de HU-09.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Tabla 'Por firmar' (fila testid=fila-por-firmar; data-folio, data-contrato): Folio, Objeto, Tu parte (rol_en_firma), Aperturada (fecha/hora) | derivado (display) | no | Solo lectura. Datos de GET /api/bitacora/pendientes: aperturas donde eres firmante y firmado=false, ordenadas por apertura_en DESC (bitacora.controller.js:328-344). Acotado al propio usuario (req.user.id), no expone pendientes ajenos. Fila resaltada si data-contrato === ?contrato (deep-link, PorFirmar.jsx:88). |
| Botón Firmar · testid=btn-firmar (por fila) | acción (botón) | no | Front: disabled mientras firmando===apertura_id (PorFirmar.jsx:94). Back POST /api/bitacora/:aperturaId/firmar: 400 aperturaId inválido; 403 'No eres firmante de esta apertura'; 409 'Ya firmaste esta apertura'; éxito → transición firmado=false→true con firmado_en=NOW, deriva completa (pendientes===0) (bitacora.controller.js:293-325). APPEND-ONLY: firma tu propia parte, nadie firma por otro. La firma conjunta de la apertura se deriva de todas las firmas (art.123 fr.III RLOPSRM). |
| Botón Recargar (↻) | acción | no | Recarga la bandeja (PorFirmar.jsx:62). |


---

## Avance / curva / atrasos

### TrabajosTerminados.jsx (Registro de trabajos terminados / avance ejecutado)  ·  HU-06
- **Ruta:** `/seguimiento/trabajos-terminados (acepta ?contrato=ID para preselección)`
- **Roles que editan:** contratista/superintendente = E (único editor). residente y supervisión = C (solo lectura). dependencia y finanzas = sin acceso (no aparece). Gate front: useVistaHU('HU-06') -> soloLectura cuando nivel != E (TrabajosTerminados.jsx:128). Gate back: router GET requireRole('contratista','residente','supervision'); POST y POST /:id/corregir requireRole('contratista') (trabajos.routes.js:16,20,22) + esParteOSupervision por participación.
- **Notas:** Gating por rol: solo contratista edita (E); residente/supervisión consultan (C); dependencia/finanzas fuera. Cálculos SERVER-SIDE como fuente de verdad: art.118 (Σ ejecutado vigente <= contratado) es el ÚNICO bloqueo; el front replica el cálculo solo como guía. APPEND-ONLY: el avance NO se edita ni borra (se eliminaron PATCH/DELETE); corregir = anular original + registro nuevo vinculado (reemplaza_a) + nota 'dice/debe decir' (art.123 fr.VI/VII). NOTA automática de bitácora tipo 'avance' al registrar (art.125 fr.II); si no hay bitácora abierta -> O JO: el POST EXIGE bitácora abierta (409 art.122 RLOPSRM, trabajos.controller.js:256-259), pese a comentarios viejos sobre 'nota diferida'. Contrato cerrado (finiquito) = solo lectura, 409 art.64 LOPSRM (:249-253). registrado_por SIEMPRE del JWT, nunca del body. Aviso_programa y aviso_periodo viajan en la respuesta 201 (no bloquean). Carrera art.118 cerrada con SELECT...FOR UPDATE del concepto.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (etiqueta 'Contrato'; heredado del contrato activo global vía BannerContratoActivo, NO es <select> propio; testid del banner, no de esta pantalla) | derivado (se hereda del contrato activo global; el banner ofrece 'Cambiar') | sí | Front: sin contrato no se muestra el formulario de captura (TrabajosTerminados.jsx:362-367). Back: GET /api/trabajos/contrato/:contratoId valida contratoId entero>0 (400), contrato existe (404), esParteOSupervision (403) (trabajos.controller.js:132-144). |
| Concepto del catálogo (label 'Concepto del catálogo' / testid=cap-concepto) | select (opciones = conceptos del contrato; value = contrato_concepto_id) | sí | Front: puedeGuardar exige conceptoSel (TrabajosTerminados.jsx:268). Back: contrato_concepto_id entero>0 requerido (400, trabajos.controller.js:216-219); concepto debe existir (404, :244); el lock FOR UPDATE de contrato_conceptos cierra carrera art.118 (:241). |
| Periodo del programa (label 'Periodo del programa' / testid=cap-periodo) | select (opciones = periodos del contrato; value = numero; marca '— en curso' el periodo que contiene hoy) | sí | Front: disabled si no hay periodos; puedeGuardar exige periodoSel (TrabajosTerminados.jsx:268,458). Preselecciona el periodo en curso o último iniciado (:183-188). Back: periodo_numero entero>0 requerido (400, :226-229); el periodo debe existir en el programa del contrato (400 'El periodo N no existe', :262-266); periodo FUTURO (inicio>CURRENT_DATE) -> 409 'aún no inicia; no se puede reportar avance de trabajo no ejecutado' (:271-279, FIX 22-jun profe = validar periodo actual); periodo YA CERRADO (fin<CURRENT_DATE) se permite pero devuelve aviso_periodo 'registro tardío' (:280-282). |
| Cantidad ejecutada (label 'Cantidad ejecutada' / testid=cap-cantidad) | number (min=0, step=any; texto a la derecha; bordes color: rojo si excede118, ámbar si noProgramado/excedePeriodo) | sí | Front: puedeGuardar exige cantNueva>0 y !validacion.excede118 (TrabajosTerminados.jsx:268-269). Validación en vivo: excede118 cuando acumulado_ejecutado+nueva > contratada+EPS (:256-257). Back: cantidad número finito >=0 (400, :220-223); se cuantiza q3 a 3 decimales = NUMERIC(14,3) (:32,224). BLOQUEO art. 118 RLOPSRM: Σ ejecutado vigente total por concepto + nueva <= cantidad_contratada+EPS (1e-6), si no 409 'Excede lo contratado (art.118)' (:293-298). AVISO no bloqueante (aviso_programa) art.45-A-X RLOPSRM + art.52 LOPSRM: si el ejecutado acum del periodo excede lo programado acum, o el concepto no estaba programado -> se registra igual (adelantar a precios pactados no requiere convenio) (:284-291, validarProgramaPeriodo :92-118). |
| Toggle 'Ejecuté todo lo programado del periodo' (testid=toggle-todo-periodo) | checkbox (autollena cantidad = refPrograma.disponible) | no | Front-only / atajo UX: solo aparece si hay concepto+periodo+refPrograma; disabled si disponible<=0 (TrabajosTerminados.jsx:481-491). No es campo que se envíe; autollena 'Cantidad ejecutada' con programadoAcum-ejecutadoAcum (:272-275). |
| Observaciones (label 'Observaciones (opcional)' / testid=cap-observaciones) | text | no | Front: libre. Back: si string se hace trim() o null (trabajos.controller.js:230); se persiste en concepto_avance.observaciones. |
| Foto de evidencia (label 'Foto de evidencia *' / testid=cap-foto-evidencia; estados testid=foto-evidencia-ok / foto-evidencia-falta) | file (accept image/jpeg,image/png) | sí | A1 — CRITERIO DEL EQUIPO, NO es ley. Front: puedeGuardar exige !!fotoEvidencia; sin foto no se registra (TrabajosTerminados.jsx:269,525-541). Tras el POST del avance, se redimensiona (utils/imagen.js) y se sube vía api.subirFotoAvance al id del avance creado (:281-293). Back de la foto (avance-fotos.controller.js): valida magic bytes JPEG/PNG (:37-42,68), tope 5 MB multer (avance-fotos.routes.js:14,23), esParteOSupervision (403), subido_por del JWT (:76). NOTA: el backend de trabajos NO exige foto (la regla A1 vive solo en el front); por API se puede crear el avance sin foto. |
| [Edición inline] Cantidad — al 'Corregir' una entrada (testid=edit-cantidad-{id}) | number (min=0, step=any) | sí | Append-only (art.123 fr.VI/VII RLOPSRM): NO edita la fila; POST /api/trabajos/:id/corregir ANULA la original (estado vigente->anulada, trigger sigecop_avance_inmutable) y registra una NUEVA vinculada por reemplaza_a (trabajos.controller.js:360-462). Back: cantidad >=0 (400, :365); revalida art.118 sobre las vigentes (409, :405-410); exige bitácora abierta (409, :393); contrato no cerrado (409 art.64, :391); genera nota 'dice/debe decir' (:419-433). |
| [Edición inline] Observaciones — al 'Corregir' (testid=edit-obs-{id}) | text | no | Back: trim()\|\|null (trabajos.controller.js:367). Va al INSERT de la entrada corregida. |
| [Galería por avance] Agregar foto (testid=foto-avance-subir-{avanceId}); Eliminar foto (testid=foto-avance-eliminar-{fotoId}) | file (accept image/jpeg,image/png) / acción eliminar | no | Componente FotosDeAvance (TrabajosTerminados.jsx:39-124). soloLectura cuando soloLectura global o el avance está 'anulada' (:659). Back avance-fotos.controller.js: subir valida imagen JPEG/PNG (400, :68), participación (403, :71); eliminar por participación (403, :105). Etiqueta legal en UI: art.132 fr.IV RLOPSRM (:90). |

### CurvaAvance.jsx (Programa y curva de avance / curva S)  ·  HU-05
- **Ruta:** `/seguimiento/curva-avance (acepta ?contrato=ID)`
- **Roles que editan:** residente = E; contratista, supervisión, dependencia = C; finanzas = sin acceso (permisos.js:16). PANTALLA DE SOLO LECTURA en la práctica: NO captura datos, no usa useVistaHU. Solo filtros de presentación. Back: GET /api/contratos/:id/programa esParteOSupervision (403); api.listarPagos / api.convenios / api.versionPrograma todos acotados por participación.
- **Notas:** Pantalla de CONSULTA pura (no captura). Componente CurvaSVG con tooltip interactivo por punto (testid=curva-pt-{serie}-{i}, curva-tooltip). 3 series % acumulado sobre eje X = periodos: programado (azul oscuro, llega a 100%), ejecutado (azul, se detiene en hoy), financiero (verde, se detiene en hoy). Cálculos CLIENT-SIDE pero leen endpoints canónicos: PROGRAMADO de api.leerProgramaObra (celdas), EJECUTADO de api.trabajosDeContrato (avances+conceptos.acumulado_ejecutado), FINANCIERO de api.listarPagos (Σ importe con fecha_pago<=corte ÷ monto vigente ×100, misma def. que financiero_pct canónico). G1 (23-jun, profe '26% hoy, 13% mañana'): el % financiero histórico NO se re-escala al subir el monto por convenio — cada punto se divide por montoEnFecha (monto de la versión vigente entonces) (CurvaAvance.jsx:323-352). Etapas por versión (Propuesta B) solo si >=2 versiones (convenio): plan original congelado + etapa vigente, vía derivarEtapas/api.versionPrograma (:412-416,587-649). Badge 'Adicional' en conceptos de convenio (art.101 RLOPSRM, :557,713). Matriz Gantt concepto×periodo con colores: ejecutado(verde)/atraso(rojo, programado vencido sin ejecutar)/pendiente(ámbar)/vacío. KPIs a hoy: programado, ejecutado, financiero, desviación (ejecutado-programado). % avance global = Σ ejecutado ÷ Σ contratado.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (heredado vía BannerContratoActivo; no es select propio) | derivado (contrato activo global) | sí | Front: sin contrato no se grafica (CurvaAvance.jsx:478-480). Back leerPrograma: id entero>0 (404), contrato existe (404), esParteOSupervision (403) -> el front muestra 'No tienes acceso al programa de este contrato' (programa.controller.js:13-24; CurvaAvance.jsx:253). |
| Filtro Concepto (label 'Concepto' / testid=filtro-concepto) | select ('Todos' \| contrato_concepto_id) | no | Front-only (presentación). Recalcula matriz, curvas y KPIs (denominador = Σ contratado del filtro). El financiero es siempre a nivel CONTRATO, no por concepto (CurvaAvance.jsx:362-372, aviso :669). |
| Filtro Periodo / rango (label 'Periodo' / testid=filtro-periodo) | select ('todo' \| 'ultimos3' \| 'ultimo') | no | Front-only. Recorta columnas/curvas (periodosVisibles); los acumulados se siguen calculando desde el inicio del contrato (CurvaAvance.jsx:356-360). |

### AlertasAtraso.jsx (Atraso por concepto)  ·  HU-07
- **Ruta:** `/seguimiento/alertas (acepta ?contrato=ID y ?concepto=ID para resaltar fila)`
- **Roles que editan:** residente = E (único que puede 'Asentar en bitácora'); supervisión = C; contratista/dependencia/finanzas = sin acceso (permisos.js:18). Gate front: useVistaHU('HU-07') -> soloLectura cuando nivel != E oculta columna Acciones y botón asentar (AlertasAtraso.jsx:25,182,208). Gate back: GET por participación (cualquier rol con acceso); POST asentar requireRole('residente') (alertas.routes.js:25).
- **Notas:** Pantalla AUTOMÁTICA, sin campos de captura (rediseño profe P15: se quitaron umbrales/canales/porcentajes). Lista TODOS los conceptos con DÉFICIT = programado_acumulado(programa vigente, al periodo en curso) − ejecutado_acumulado(TOTAL), en unidades del concepto, solo filas con déficit>EPS(1e-6). Cálculo SERVER-SIDE (deficitsDeContrato, cuantizado q3 a 3 decimales), se recalcula al consultar (sin cron). 'periodo actual' = el de mayor número cuyo inicio<=CURRENT_DATE (null si ninguno arrancó -> no hay déficit). Programa VIGENTE: los convenios lo reescriben en vivo. Tomar ejecutado TOTAL (no acotado al periodo) evita falsos atrasos por ir adelantado. Append-only: el asiento de atraso genera nota inmutable + fila en atraso_asentado. Distinto del 'Avance de obra' ponderado (curva HU-05) — link en el banner (AlertasAtraso.jsx:128). Resaltado de fila por ?concepto= (deep-link de la campana, :198). Otros endpoints del controller (no campos de esta pantalla): GET /api/alertas/resumen (badge de login, conteos por participación) y GET /api/alertas/detalle?contrato= (filas para el centro de notificaciones).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (heredado vía BannerContratoActivo; no es select propio) | derivado (contrato activo global) | sí | Front: sin contrato no carga atrasos (AlertasAtraso.jsx:135-137). Back alertasDeContrato: contratoId entero>0 (400), contrato existe (404), esParteOSupervision (403 'No tienes acceso al atraso de este contrato') (alertas.controller.js:84-95). |
| Botón 'Asentar en bitácora' por fila de atraso (testid=btn-asentar-{contrato_concepto_id}) | acción (POST /api/alertas/contrato/:contratoId/asentar con body { contrato_concepto_id }) | no | Front: solo si !soloLectura (residente); disabled mientras asienta esa fila (AlertasAtraso.jsx:208-219). Back: requireRole('residente') (alertas.routes.js:25); contrato_concepto_id entero>0 (400); concepto debe pertenecer al contrato (404); déficit ACTUAL del concepto debe ser >0 (409 'no tiene atraso; nada que asentar'); EXIGE bitácora abierta (409 art.123 RLOPSRM, no se difiere — es snapshot en vivo); idempotente: UN asiento por (concepto, periodo) vía UNIQUE uq_atraso_asentado (409 'ya fue asentado; no se duplica'). Emisor de la nota = residente_id del contrato (art.53 LOPSRM); tipo de nota propio 'atraso' (art.125 fr.I). (alertas.controller.js:168-270) |

### AmbienteAvance.jsx (Ambiente de avance físico y seguimiento — cascarón de navegación)  ·  HU-06 (envuelve HU-06 -> HU-05 -> HU-07 sin fundirlas)
- **Ruta:** `/seguimiento/ambiente (ruta NUEVA fuera del catálogo; acepta selección de contrato activo; propaga ?contrato= a las HU enlazadas)`
- **Roles que editan:** Cascarón sin captura: NO usa useVistaHU. Visible para contratista/residente/supervisión (ejecutores + supervisión). Dependencia queda fuera (HU-06 null para dependencia + router de trabajos exige requireRole('contratista') para escritura). Cada bloque enlaza a su pantalla real, donde aplica el gating propio de la HU.
- **Notas:** CASCARÓN de navegación (solo lectura + enlaces), NO captura nada. 5 bloques: (1) Contrato y programa; (2) Registrar avance HU-06 -> link a /seguimiento/trabajos-terminados (acción del flujo, LinkHU 'Lo registra el Contratista'); (3) Curva y desviación HU-05 -> KPIs (fisico_pct, planeado_pct, financiero_pct) de la MISMA fuente que la curva (prep.avance de estimacion-prep, NO se recalculan aquí), link a /seguimiento/curva-avance; (4) Atrasos HU-07 -> link a /seguimiento/alertas (SÍ preselecciona contrato vía ?contrato=); (5) Evidencia fotográfica -> placeholder que redirige al Expediente (foto por estimación, art.132 fr.IV RLOPSRM; nota desactualizada: la foto de avance por periodo SÍ existe ya en TrabajosTerminados/avance-fotos). Curva (HU-05) y atrasos (HU-07) van EN PARALELO (lectura, no se bloquean), solo el registro de avance es la acción encadenada. testids: bloque-avance-{n}, kpis-avance, link-trabajos, link-curva, link-alertas, evidencia-placeholder, programa-periodos, ambiente-avance-aviso.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato y programa — Contrato activo (heredado vía BannerContratoActivo; muestra '{N} periodo(s)' testid=programa-periodos) | derivado (contrato activo global) | sí | Front: al seleccionar carga api.leerProgramaObra (periodos) y api.preparacionEstimacion(id, fin del último periodo) para KPIs (AmbienteAvance.jsx:59-76). Back: 403 -> toast 'No tienes acceso a este contrato' (:74). No hay validación de captura: la pantalla no escribe. |


---

## Estimación (ciclo)

### Integración del periodo (wizard de 5 pasos: Periodo / Generadores / Carátula / Soportes y notas / Integrar) + historial  ·  HU-12
- **Ruta:** `/estimaciones/integracion (acepta ?contrato=ID; hereda contrato activo global vía BannerContratoActivo)`
- **Roles que editan:** contratista (E). Residente/supervisión = C (solo lectura). dependencia/finanzas sin acceso. NOTA: aunque el rol sea E, el backend exige además que el usuario sea el superintendente_id del contrato (esSuperintendente); si no lo es, banner amarillo y el server rechaza 403. useVistaHU('HU-12').soloLectura = nivel!=='E'.
- **Notas:** Gating por rol: solo contratista E, y además gate de PUESTO server-side (superintendente_id del contrato, no rol global; espejo de cómo la bitácora restringe al residente). Cálculo de carátula 100% server-side (subtotal=Σ ROUND(cant×pu,2), amortización art.143 fr.I RLOPSRM, retención 5 al millar 0.5% art.191 LFD, retención por atraso art.46 Bis LOPSRM+86-90 RLOPSRM si hay pena pactada y atraso, todo SIN IVA art.2 fr.XIX RLOPSRM); la carátula viva del front es solo preview (espejo r2). Append-only: estimaciones/estimacion_generadores/estimacion_notas inmutables por trigger. Wizard: atrás libre, adelante valida prefijo (pasoValido). Snapshots: anticipo_pct_snapshot, pu_snapshot, cantidad_anterior_acum, avance_fisico_pct, avance_financiero_pct.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo, no es <select> propio; hereda selección global) | derivado (selección global de contrato) | sí | Front: sin contrato no se renderiza el wizard. Back: GET listarContratos acota por participación; integrar valida contrato existe (404) y superintendente_id===req.user.id (403). estimaciones.controller.js:146 |
| Periodo del programa (periodo-selector) | select (periodos del programa de obra) | no | Atajo opcional que autocompleta inicio/fin. Front: solo periodos VENCIDOS habilitados (fin<hoy); en curso/futuro disabled con etiqueta 'en curso · aún no cierra' / 'futuro'. IntegracionEstimacion.jsx:967-982. Solo aparece si el contrato tiene programa.periodos. |
| Periodo — inicio (periodo-inicio) | date | sí | Front: gate paso 0 requiere inicio y fin (pasoValido). Back: fechaValida AAAA-MM-DD (400); periodo_fin no anterior a inicio (400); periodo_fin <= inicio+1mes (art. 54, 400). estimaciones.controller.js:59-66 |
| Periodo — fin (periodo-fin) | date | sí | Front: gate paso 0. Back: además debe ser periodo VENCIDO (periodo_fin < CURRENT_DATE) o 409 'periodoNoCerrado' (art. 54). estimaciones.controller.js:79-82. No-solape con otra estimación no rechazada del contrato (409). estimaciones.controller.js:181-192 |
| Cantidad ejecutada por concepto (gen-cantidad-{contrato_concepto_id}) — una fila por concepto del periodo | number (min 0, step any) | condicional | Requerido al menos un concepto con cantidad>0 para integrar. Front: semáforo rojo si acumulado>contratado (excede, art.118) o periodo>disponible_periodo (excedePlan, art.45-A-X/52); gate paso 1 = hayLineas && !hayExceso && !hayExcesoPlan (IntegracionEstimacion.jsx:695-704); prellena con avance_periodo del prep. Back: q4 a 4 decimales; filtra <=0; sin conceptos repetidos (400); conceptos deben pertenecer al contrato (400); art.118 acumulado+periodo<=contratado (409, :251-256); art.45-A-X plan: acumulado<=planeado_hasta_periodo si hay programa (409, :281-284). Importe = ROUND(cant×pu,2) server-side, cuadre al centavo. |
| Deductivas (caratula-deductivas) | number (min 0, step any) | no | Default 0. Front: gate paso 2 = caratula.neto>=0 (deductivas no dejan neto negativo). Back: debe ser número >=0 (400, art.46 Bis LOPSRM); r2; neto final no puede ser negativo tras deductivas+retención por atraso (400). estimaciones.controller.js:84-92, :367-370 |
| Notas de bitácora a vincular (modal ModalVincularNotas / btn-abrir-buscador-notas → mb-btn-confirmar) | checkbox múltiple (selección de notas firmadas) | no | Opcional (soporte art.132 fr.II). Front: solo notas con aceptacion==='firmada' && tipo!=='apertura' (IntegracionEstimacion.jsx:786-789). Back: cada nota debe colgar de la bitácora de ESTE contrato o 400 (estimaciones.controller.js:221-234). Append-only: estimacion_notas. Se quita en front antes de integrar; no editable tras integrar. |
| Checklist documentos art.132 (checklist-art132, art132-fr-I..VII) | derivado (solo lectura informativo) | no | Presentación pura; lista enunciativa art.132 RLOPSRM. fr.II marca pendiente/incluido según notas vinculadas; fr.VI 'No aplica' salvo contrato a precio alzado. IntegracionEstimacion.jsx:1057-1082 |
| Confirmación de cierre (check-cierre) | checkbox | sí | Requerido para habilitar Integrar. Front: btn-integrar disabled si !cierreConfirmado \|\| soloLectura \|\| integrando \|\| hayExceso \|\| hayExcesoPlan \|\| !hayLineas \|\| !periodo. IntegracionEstimacion.jsx:1109. No se envía al backend (candado de UI). |
| Botón Confirmar e integrar (btn-integrar) → POST integrarEstimacion | derivado (acción) | sí | Acción final del wizard. Back: bitácora del contrato abierta o 409 requiereBitacora (art.122, estimaciones.controller.js:71-74); contrato no 'cerrado'/finiquito o 409 (art.64, :142-145); PDF firmado del contrato ligado o 409 (HU-01, :164); si anticipo>30% exige doc anticipo_autorizacion o 409 (art.50 fr.IV, :166-168). Numeración atómica MAX+1 (advisory lock). Carátula calculada server-side e inmutable (trigger sigecop_estimacion_inmutable). Estado inicial 'integrada'. |

### Presentación de la estimación (lista de estimaciones con acción Presentar)  ·  HU-13
- **Ruta:** `/estimaciones/envio (acepta ?contrato=ID; hereda contrato activo global)`
- **Roles que editan:** contratista (E). Residente/supervisión = C. dependencia/finanzas sin acceso. Gate de puesto server-side: superintendente_id del contrato. useVistaHU('HU-13').
- **Notas:** Sin campos de captura libres: el único input es la ACCIÓN de presentar (sella fecha/hora del acto, art.54 LOPSRM). Append-only: el folio/carátula no cambian; una estimación se presenta una sola vez (estados autorizada/pagada → no-presentable; rechazada → reintegrar en HU-12, no hay reingreso aquí). Plazos art.54 son referencia VISUAL, no bloquean. Estado: integrada→enviada.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo) | derivado (selección global) | sí | Front: sin contrato no carga lista. Back: GET historialEstimaciones acota por participación (403 si no es parte). |
| Botón Presentar estimación (btn-presentar-{id}) → POST enviarEstimacion | derivado (acción, una fila por estimación 'integrada') | no | Solo aparece si estado==='integrada' && !soloLectura (presentables = estado 'integrada'). EnvioEstimacion.jsx:174,261-282. Back: solo superintendente_id (403, estimaciones-ciclo.controller.js:175); solo desde 'integrada', no re-presentar (409, :180-185); contrato no cerrado (409 art.64, :172); bitácora abierta o 409 requiereBitacora (art.122, :190-196). Sella enviada_en=NOW() y enviada_por=JWT atómicamente (WHERE estado='integrada' bloquea doble-presentación). Genera nota automática sup_estimaciones (art.125 fr.II-b). |
| Semáforo de plazo (semaforo-plazo-{id} / sello-presentacion-{id}) | derivado (solo lectura) | no | Front puro: 'integrada' muestra plazo presentación 6 días desde corte (informativo, distingue antesDelCorte→'faltan X días'); 'enviada' muestra fecha de presentación + semáforo de 15 días de revisión derivado de enviada_en (art.54). EnvioEstimacion.jsx:71-104,246-298 |

### Recepción, revisión técnica y autorización/rechazo (tabs por sección + observaciones + panel de resolución)  ·  HU-15
- **Ruta:** `/estimaciones/revision (acepta ?contrato=ID)`
- **Roles que editan:** residente (E) y supervisión (E). dependencia = C. contratista/finanzas sin acceso. Gate de puesto server-side: supervisión=supervision_id (registra obs/turna/rechaza directo); residencia=residente_id (autoriza/rechaza tras turnado). useVistaHU('HU-15').
- **Notas:** Tabs de revisión: solo caratula/generadores/notas (fotos/soportes ocultas por falta de datos reales; SECCIONES front=3, pero el back acepta 5: caratula/generadores/fotos/soportes/notas). Flujo secuencial obligatorio: supervisión revisa→turna→residencia resuelve; residencia no puede resolver antes del turnado (IndicadorFlujo 3 pasos). Severidad eliminada. Semáforo 15 días (art.54) derivado de enviada_en. NO se calcula dinero aquí (solo lee detalle de HU-12). Estado: enviada→autorizada\|rechazada. Observaciones append-only (eliminar solo el propio autor, solo antes de turnar). Rechazo registra observación tipo='rechazo' turnado_a='contratista'.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo) | derivado (selección global) | sí | Back: historialEstimaciones acota por participación. Filtra estados revisables: enviada/autorizada/rechazada. RevisionEstimacion.jsx:409 |
| Estimación (select-estimacion) | select | sí | Front: solo estimaciones enviada/autorizada/rechazada. Back: revisionEstimacion acota por participación (403). Vacío → mensaje 'sin-estimaciones'. |
| Descripción de observación (obs-{seccion}-nueva-texto) — por sección caratula/generadores/notas | textarea | condicional | Requerido para agregar una observación. Front: btn-agregar disabled si !texto.trim(). Back: descripcion obligatoria trim (400, estimaciones-ciclo.controller.js:352-353); solo supervision_id (403); solo estado 'enviada' y NO turnada (409); contrato no cerrado (409). Append: estimacion_observaciones. Solo visible si puedeRegistrar (supervisión, enviada, no turnada). |
| Tipo de observación (obs-{seccion}-nueva-tipo) | select (aclaracion/correccion/rechazo) | sí | Default 'aclaracion'. Front: TIPOS fijos. Back: tipo ∈ {aclaracion,correccion,rechazo} (400). SEVERIDAD ELIMINADA (FIX 22-jun profe): toda observación cuenta igual. estimaciones-ciclo.controller.js:259,349-350 |
| Marcar sin observaciones (chk-sin-observaciones) | checkbox | no | Front: disabled si totalObs>0; habilita Turnar cuando no hay observaciones (puedeTurnar = totalObs>0 \|\| sinObservaciones). RevisionEstimacion.jsx:532,665-673. Back: turnar requiere n>0 o sin_observaciones=true (409, estimaciones-ciclo.controller.js:459-462). |
| Botón Turnar a residencia (btn-turnar) → POST turnarEstimacion | derivado (acción) | no | Front: disabled si !puedeTurnar. Back: solo supervision_id (403); estado 'enviada' (409); no re-turnar (FOR UPDATE serializa, 409); contrato no cerrado. Marca turnado_a='residencia' o inserta marcador. estimaciones-ciclo.controller.js:422-488 |
| Motivo del rechazo — supervisión (motivo-rechazo-supervision) | textarea | condicional | Requerido para rechazar (supervisión). Front: btn-rechazar-supervision disabled si !motivoRechazo.trim(). Solo visible para supervisión no-turnada. FIX 22-jun: supervisión puede RECHAZAR DIRECTO sin turnar. Back: motivo obligatorio trim (400); supervisión rechaza sin exigir turnado. estimaciones-ciclo.controller.js:570-585 |
| Motivo del rechazo — residencia (motivo-rechazo) | textarea | condicional | Requerido para rechazar (residencia). Front: btn-rechazar disabled si !puedeResolver \|\| !motivoRechazo.trim(). Visible para residencia tras turnado. Back: motivo obligatorio (400); residencia exige turnado previo (409, EXISTS turnado_a='residencia' dentro del UPDATE cierra TOCTOU). estimaciones-ciclo.controller.js:557-616 |
| Botón Autorizar (btn-autorizar) → POST autorizarEstimacion | derivado (acción) | no | Front: disabled si !puedeResolver (= !soloLectura && esResidencia && estado='enviada' && turnada). Back: solo residente_id (403); estado 'enviada' (409); turnado previo obligatorio (409); contrato no cerrado. UPDATE atómico enviada→autorizada. Genera nota automática res_estimaciones (art.125 fr.I-b). estimaciones-ciclo.controller.js:492-552 |

### Reingreso de estimación tras rechazo (observaciones de la rechazada + crear nueva versión)  ·  HU-16
- **Ruta:** `/estimaciones/reingreso (acepta ?contrato=ID). NOTA: pantalla DESCONECTADA del flujo por indicación del profe — la HU-13 dirige a reintegrar en HU-12, no a reingresar aquí.`
- **Roles que editan:** contratista (E). residente = C. supervisión/dependencia/finanzas sin acceso. Gate de puesto server-side: superintendente_id. useVistaHU('HU-16').
- **Notas:** CA-1: nueva versión = bloque completo independiente; la rechazada queda como histórico vinculado (reemplaza_a, self-FK). CA-3: el plazo de presentación (art.54) NO se reinicia (referencia enviada_en de la rechazada). Sin DDL (reemplaza_a+UNIQUE ya en schema). No toca zona congelada (HU-12). La nota de atención NO se persiste. Trazabilidad derivada del historial. OJO: pantalla actualmente desconectada del flujo del profe; la severidad mostrada aquí (menor/mayor/critica) está desalineada con HU-15 que eliminó severidad.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo) | derivado (selección global) | sí | Back: historialEstimaciones acota por participación. |
| Estimación rechazada (select-estimacion) | select | sí | Front: solo reingresables = estado 'rechazada' que NO tengan ya un reingreso (ninguna otra con reemplaza_a===e.id). ReingresoEstimacion.jsx:152-155. Back: solo estado 'rechazada' (409); unicidad 1 rechazada→1 reingreso por UNIQUE(reemplaza_a)+pre-chequeo (409). estimaciones-ciclo.controller.js:671-685 |
| Nota de atención a observaciones (textarea-nota) | textarea | condicional | Requerida (front) para habilitar el reingreso: puedeReingresar exige nota.trim().length>0 (ReingresoEstimacion.jsx:213). NO se persiste (control de UI; no hay columna; explícito 'no se almacena de forma permanente'). estimaciones-ciclo.controller.js:643-647 |
| Confirmo que atendí las observaciones (chk-confirmado) | checkbox | sí | Front: puedeReingresar exige confirmado. Back: body.confirmacion===true obligatorio o 400. estimaciones-ciclo.controller.js:656-658 |
| Botón Reingresar estimación (btn-reingresar) → POST reingresarEstimacion | derivado (acción) | no | Front: disabled si !puedeReingresar (= !soloLectura && rechazadaSel && nota && confirmado && !reingreso && !accion). Back: solo superintendente_id (403); contrato no cerrado (409); crea fila nueva (MAX+1, estado 'integrada', reemplaza_a=rechazada) COPIANDO carátula+generadores (no recalcula); enviada_en NULL — plazo art.54 NO se reinicia. estimaciones-ciclo.controller.js:649-737 |
| Descargar observaciones PDF/Excel (btn-descargar-obs-pdf / btn-descargar-obs-excel) | derivado (export client-side) | no | Front: disabled si observaciones.length===0. jsPDF/exceljs. Las observaciones (con severidad menor/mayor/critica en este front — desfasado con HU-15 que la eliminó) se leen del detalle real de revisión (CA-2). |

### Historial de estimaciones del contrato (filtros + tabla + drawer de expediente)  ·  HU-14
- **Ruta:** `/estimaciones/historial (acepta ?contrato=ID)`
- **Roles que editan:** residente (E). contratista/supervisión/dependencia = C. finanzas sin acceso. NOTA: la pantalla es 100% consulta (no usa useVistaHU/soloLectura; no hay escritura). useSesion() solo para token.
- **Notas:** Pantalla de SOLO consulta (incluye rechazadas, para fiscalización). No tiene captura editable. 'Versión' siempre '—' (el modelo no versiona; una corrección es registro nuevo). Datos del backend historialEstimaciones (read-only, deriva línea de tiempo de columnas de estimaciones — Opción A, sin tabla estimacion_transiciones).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato activo (BannerContratoActivo) | derivado (selección global) | sí | Back: historialEstimaciones acota por participación (403). |
| Filtro Periodo (he-periodo) | select | no | Default 'Todos'. Front puro: opciones derivadas de los periodos presentes; filtro Y con estado. HistorialEstimaciones.jsx:211-226 |
| Filtro Estado (he-estado) | select | no | Default 'Todos'. Front puro: opciones derivadas de estados presentes; lógica Y con periodo. |
| Exportar historial (btn-exportar-historial) | derivado (export Excel client-side) | no | Front: exceljs sobre las filas filtradas. |
| Fila de estimación (fila-historial-{id}) → drawer PanelDetalle | derivado (acción de apertura, solo lectura) | no | Front: abre expediente compacto. Fecha de presentación = enviada_en real ('—' si no presentada). Fechas revisión/pago derivadas de transiciones (back deriva autorizada_en/rechazada_en/pagada_en sin columnas-sello). Observaciones vacías en este drawer (no se fabrican). |

### Tablero de estimaciones (KPIs agregados + contadores por estado + grid de tarjetas + mis pendientes)  ·  HU-17
- **Ruta:** `/estimaciones/tablero (sin selección de contrato; agrega toda la cartera visible)`
- **Roles que editan:** residente (E) — pero la vista es de SOLO LECTURA agregada (no edita nada). contratista/supervisión/dependencia = C. finanzas sin acceso (oculta en UI Y bloqueada server-side 403). No usa useVistaHU; useSesion() solo lee rol para 'mis pendientes'.
- **Notas:** Vista 100% lectura/agregada — NO hay campos de captura. Agregación SERVER-SIDE con montos EXACTOS congelados por HU-12 (cuadre al centavo): KPIs (contratos, monto_estimado=Σneto no rechazadas, monto_pagado=Σneto pagadas, monto_pendiente), antigüedad promedio por estado, contadores y montos por estado, grid (excluye rechazadas, CA-1), 'mis pendientes' filtrado por req.user.rol del JWT (next-actor por estado). Finanzas: 403 server-side (CA-5) además de permisos.js finanzas:null. Acotamiento por participación (esParteOSupervision); dependencia/finanzas verían todo pero finanzas está bloqueada. Pipeline: integrada→enviada(presentada)→autorizada→pagada; rechazada solo en métrica.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Filtro Estado (filtro-estado) | select | no | Default 'Todos'. Front puro: opciones = estados con en_grid (excluye rechazada). TableroEstimaciones.jsx:191-194 |
| Filtro Periodo (filtro-periodo) | select | no | Default 'Todos'. Front puro: periodos derivados del backend (AAAA-MM). |
| Filtro Responsable (filtro-responsable) | select | no | Default 'Todos'. Front puro: responsables (next-actor) derivados de las tarjetas. |
| Reintentar (botón en banner de error) | derivado (acción de recarga) | no | Front: recarga GET tablero/estimaciones. |


---

## Pago / convenios

### RegistroPago (registro de pago efectuado)  ·  HU-21
- **Ruta:** `/pagos/registro (acepta ?contrato=ID y ?estimacion=ID)`
- **Roles que editan:** finanzas (E). Resto consulta: residente(C), contratista(null), supervision(null), dependencia(C). En permisos.js HU-21 = residente:C, contratista:null, supervision:null, dependencia:C, finanzas:E. El form (RegistroPagoForm) sólo se renderiza si !soloLectura; además el botón se gatea a rol==='finanzas' (RegistroPagoForm.jsx:79).
- **Notas:** POST /api/pagos requireRole('finanzas') en la ruta (pagos.routes.js:10) — doble barrera con el gate de UI. registrado_por SIEMPRE del JWT, nunca del body (pagos.controller.js:115, CA-2). TRANSACCIONAL/append-only: al pagar la estimación pasa a 'pagada' y la instruccion_pago emitida→cumplida en la misma tx (:118,:123), saliendo de la cola de finanzas. Candado server-side extra (FIX 22-jun profe): NO se paga sin AVANCE físico vigente del contrato HU-06 (409 :88-91). GET /api/pagos/contrato/:id acotada por participación esParteOSupervision (403 :150); plazo 20 días (art. 54) DERIVADO en lectura: dias_transcurridos = fecha_pago − GREATEST(fecha_autorizacion, fecha_factura), plazo_cumplido ≤20, base_provisional si sin autorización (:160-162).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Selector de contrato — heredado del contrato activo global vía BannerContratoActivo (no es <select> propio; se preselecciona por ?contrato=) | derivado (banner / query param) | sí | Front: sin contrato no se muestra el form ni la tabla (RegistroPago.jsx:85,90). Back: contrato_id requerido entero>0 (pagos.controller.js:37) y debe existir (404 :62). |
| Estimación a pagar (testid=pago-estimacion) — '(autorizada, no pagada)' | select | sí | Front: lista SOLO estimaciones estado='autorizada' (PAGABLES Set, RegistroPagoForm.jsx:19,47); requerido (requeridosOk :77). Back: estimacion_id entero>0 (pagos.controller.js:38); debe existir (404 :67); pertenecer al contrato (400 :69); NO 'pagada' (409 :70); NO 'rechazada' (409 :71); SOLO 'autorizada' art. 54 LOPSRM (409 :76); sin pago previo UNIQUE+FOR UPDATE no-doble-pago (409 :78,:128). |
| Importe a pagar (neto de la estimación) (testid=pago-importe-neto) | derivado (read-only, server-side) | no | Front: muestra neto de la estimación, NO editable (RegistroPagoForm.jsx:141-143). Back: importe = est.neto, derivado server-side, el body NO lo envía; pago exacto del neto, no parcial (pagos.controller.js:107). Art. 54 LOPSRM / cuadre al centavo. |
| Fecha de pago (testid=pago-fecha) | date | sí | Front: default hoy (RegistroPagoForm.jsx:24); requerido (:77). Back: requerida, formato AAAA-MM-DD válido (400, pagos.controller.js:39); NO anterior a integrada_en de la estimación (400 :100). Art. 54 LOPSRM. |
| Referencia bancaria / clave de rastreo SPEI (testid=pago-referencia) | text (inputMode numeric, pattern \d*) | sí | Front: filtra no-dígitos replace(/\D/g,'') (RegistroPagoForm.jsx:153); requerido (:77). Back: requerida (400 :40); ≤100 chars (400 :41); NUMÉRICA ^\d{6,100}$ (400 :43, FIX 22-jun profe: clave SPEI numérica). |
| Folio fiscal (CFDI) / UUID (testid=pago-cfdi; aviso heredado testid=pago-cfdi-heredado) | text | sí | Front: requerido (:77); G4 — al elegir estimación HEREDA el folio de instruccion_pago.factura_cfdi vía api.transitoEstimacion, editable (RegistroPagoForm.jsx:63-75). Back: requerido (400 :44); ≤60 chars (400 :45). |
| Fecha de la factura (testid=pago-fecha-factura) | date (max=hoy) | sí | Front: max=hoy (RegistroPagoForm.jsx:165); requerida (:77). Back: requerida, formato válido (400 :46); NO futura/post-fechada (400 :48, FIX 22-jun profe). |
| Fecha de autorización de la estimación (testid=pago-fecha-autorizacion) | date | no | Front: opcional, 'provisional — pasará a HU-20' (RegistroPagoForm.jsx:170). Back: si se envía formato AAAA-MM-DD válido (400 :51); usada para derivar plazo art. 54 en lectura (GREATEST con fecha_factura). |
| Observaciones (testid=pago-observaciones) | textarea | no | Front: opcional (RegistroPagoForm.jsx:175). Back: ≤2000 chars (400, pagos.controller.js:54). |

### TransitoPago (tránsito a pago: suficiencia + soportes + instrucción) — wizard 4 pasos  ·  HU-20 (paso 4 embebe HU-21)
- **Ruta:** `/pagos/transito (acepta ?contrato=ID)`
- **Roles que editan:** contratista (E) y finanzas (E). Resto consulta: residente(C), dependencia(C), supervision(null). permisos.js HU-20 = residente:C, contratista:E, supervision:null, dependencia:C, finanzas:E.
- **Notas:** GATING WIZARD (pasoValidoPago, TransitoPago.jsx:229-235): paso0 OK = es_autorizada && suficiencia sin_presupuesto=false && !excede (art. 24); paso1 OK = soportes obligatorios_ok; paso2 OK = instrucción generada (art. 54); paso3 libre. Atrás libre; siguiente bloqueado con motivo (testid=wsiguiente-pago-motivo). SERVER-SIDE: suficiencia = techo(presupuesto_anual por dependencia_id+ejercicio) − comprometido(Σ neto estimaciones autorizada+pagada de esa dependencia/ejercicio, excluyendo la actual), art. 24 párr. 2; join por FK dependencia_id (ITEM 3.1). Semáforo plazo (art. 54): ancla = MÁS TARDÍA de [nota autorización en bitácora tipo res_estimaciones y fecha presentación factura]; sin factura el plazo no corre; sin nota semáforo deshabilitado (testid=semaforo-pago-deshabilitado); umbrales 0/1-10/>10 días-vencidos = criterio del equipo. Append-only: instruccion_pago emitida→cumplida en el pago. Ruta NO exige rol global salvo POST /presupuesto requireRole('finanzas') (instruccion-pago.routes.js:41); el resto acota por participación. OJO: instruccion-pago.routes NO montada en server.js congelado (pendiente Maiki, routes:54-77).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato — heredado del contrato activo global (BannerContratoActivo), preselección por ?contrato= | derivado (banner / query) | sí | Front: deshabilita el select de estimación si no hay contrato (TransitoPago.jsx:270). Carga estimaciones (api.estimacionesDeContrato). |
| Estimación autorizada (testid=select-estimacion) | select | sí | Front: SOLO estimaciones estado='autorizada' (filtro TransitoPago.jsx:132, art. 54). Back: GET /api/instruccion-pago/estimacion/:id valida acceso por participación (403, instruccion-pago.controller.js:192); es_autorizada derivado (:210). |
| PASO 1 · Partida específica art. 24 LOPSRM (testid=input-partida) — sólo si NO hay techo (suf.sin_presupuesto) y !soloLectura | text (maxLength 60) | condicional | Front: requerida; botón Cargar techo deshabilitado sin partida.trim() (TransitoPago.jsx:336,342); valida en guardarTecho (:190). Back: crearPresupuesto exige partida no vacía (400, instruccion-pago.controller.js:399, art. 24 párr. 2 LOPSRM). |
| PASO 1 · Techo de la partida (testid=input-techo) | number (min 0, step any) | condicional | Front: Number finito ≥0 (TransitoPago.jsx:187-188). Back: ejercicio 2000-2100 (400 :398), dependenciaId requerido (400 :400), techo finito ≥0 (400 :401), dependenciaId debe existir y ser rol 'dependencia' (400 :404). Requiere dependencia_id != null (legacy: 409). UNIQUE (ejercicio, dependencia_id, partida) UPSERT (:411). |
| PASO 2 · Factura del periodo — folio/descr. (testid=input-factura, botón btn-cargar-factura) | text | sí | Front: sólo si !soloLectura y no cargado (TransitoPago.jsx:363-366). Back POST /soportes: nombre obligatorio (400 :236); SOLO rol 'contratista' promueve (403 :243); factura.cargado = existe metadato 'Factura' (:127); contrato cerrado bloquea (409 :252). |
| PASO 2 · CFDI (folio fiscal) (testid=input-cfdi, botón btn-cargar-cfdi) | text | sí | Front: sólo si !soloLectura y no cargado (TransitoPago.jsx:371-374). Back: cfdiOk = existe metadato 'CFDI' con descripcion (folio) no vacía (:128). Mismas barreras (rol contratista, contrato no cerrado). |
| PASO 2 · Fianza de cumplimiento (read-only, leída de garantías HU-01) | derivado (read-only) | condicional | Back: se LEE de contrato_garantias tipo ILIKE '%cumplimiento%' (instruccion-pago.controller.js:118); exigible si existe garantía (art. 48 fr. II LOPSRM); fianzaOk = !exigible \|\| vigente (:129). No es captura (TransitoPago.jsx:378-384). |
| PASO 2 · Soportes digitales para cobro — Tipo (testid=select-tipo-archivo-cobro) | select (cfdi/oficio/otro) | no | Front: visible sólo si !soloLectura y rol==='contratista' (TransitoPago.jsx:414-422). Back: tipo se normaliza a 'otro' si no ∈ {cfdi,oficio,otro} (instruccion-pago.controller.js:505). |
| PASO 2 · Subir PDF de soporte de cobro (testid=btn-subir-archivo-cobro; input file accept=application/pdf) | file (PDF) | no | Front: accept=application/pdf, sólo contratista (TransitoPago.jsx:424-428). Back POST /estimacion/:id/archivo (multer memoria 10MB sólo PDF, instruccion-pago.routes.js:13-26): archivo requerido (400 :502); magic-bytes %PDF (400 :503); SOLO rol 'contratista' (403 :511); contrato cerrado bloquea (409 :514); subido_por del JWT (:521). FOLLOW-ON b 22-jun. |
| PASO 3 · Botón Generar instrucción de pago (testid=btn-generar-instruccion) | acción (gate duro) | no | Front: puedeGenerar = !soloLectura && transito && !instr && bloqueos.length===0 (TransitoPago.jsx:225); bloqueos = no autorizada / sin techo / excede techo / faltan soportes (:216-224). Back POST /estimacion/:id: SOLO rol 'contratista' (403, instruccion-pago.controller.js:278); contrato no cerrado (409 :286, art. 64); estado='autorizada' (409 :290, art. 54); soportes OK (409 :296); dependencia_id != null (409 :307); suficiencia art. 24 FOR UPDATE techo, neto ≤ disponible (409 :334); UNIQUE estimacion_id evita doble (409 :355). |
| PASO 4 · Registrar pago — RegistroPagoForm embebido (todos los campos de HU-21) | formulario embebido | condicional | Front: soloLectura = rol !== 'finanzas' (TransitoPago.jsx:495). Mismas validaciones front+back que RegistroPago / pagos.controller.js. |

### ConveniosModificatorios (trámite de convenios modificatorios)  ·  HU-03
- **Ruta:** `/contratos/modificatorios (acepta ?contrato=ID)`
- **Roles que editan:** dependencia (E). Resto consulta: residente(C), contratista(C), supervision(C), finanzas(null). permisos.js HU-03 = residente:C, contratista:C, supervision:C, dependencia:E, finanzas:null. Backend 2.ª barrera: crearConvenio permite dependencia O residente_id O created_by del contrato (convenios.controller.js:163).
- **Notas:** Cálculos SERVER-SIDE (fuente de verdad): monto = Σ ROUND(cantidad×pu,2) al centavo (art. 45 fr. IX RLOPSRM); deltas monto/plazo y clasificación SFP(>25%, art.102 RLOPSRM)/ajuste(>50%, art.59 Bis) los DERIVA el backend, NO se teclean (convenios.controller.js:254-268). APPEND-ONLY/INMUTABLE: convenio registrado no se edita ni anula, corregir=convenio nuevo (art. 59/99); cada convenio que toca el programa crea programa_version NUEVA (snapshot) y supersede la anterior (:334-335); conceptos originales se CONGELAN, los cambios entran como conceptos ADICIONALES es_adicional=true (:247) o ampliaciones con P.U. heredado. Ciclo de estado del convenio: 'registrado' (nace autorizado_por=NULL) → 'autorizado' por servidor facultado (ITEM 3.2, separa sustento del residente del acto formal de la dependencia). Nota automática de bitácora del convenio (art. 123 fr. III / 125 RLOPSRM): se asienta en vivo si hay bitácora (emisor=residente del contrato, nota de consecuencia art. 53) o se difiere (nota_id NULL) hasta abrir bitácora. Tabla de convenios y de versiones del programa = lectura (testids tabla-convenios, tabla-versiones, btn-ver-version-ID). Todas las rutas exigen sesión; autoridad/participación en el controller (convenios.routes.js:12).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato — heredado del contrato activo global (BannerContratoActivo), preselección por ?contrato= | derivado (banner / query) | sí | Front: sin contrato no se muestra el form (ConveniosModificatorios.jsx:490). Back: contrato debe existir (404 :159); acceso por participación en listarConvenios (403, convenios.controller.js:76). |
| Tipo de convenio (testid=cm-tipo) | select (plazo/monto/programa/mixto) | sí | Front: los 4 creables (TIPOS_CREABLES, ConveniosModificatorios.jsx:57). Back: tipo ∈ {monto,plazo,programa,mixto} (400, convenios.controller.js:127). tocaPrograma=monto/programa/mixto; tocaPlazo=plazo/mixto. |
| Plazo vigente (testid=cm-plazo-vigente) | derivado (read-only) | no | Front: read-only/disabled, derivado del contrato (ConveniosModificatorios.jsx:534-540). Sólo visible si tocaPlazo. |
| Nuevo plazo (días) (testid=cm-plazo-nuevo) — sólo si tocaPlazo (plazo/mixto) | number (min 1, step 1) | condicional | Front: plazoCambiaOk = >0 && != plazo vigente (ConveniosModificatorios.jsx:329); preview delta% (cm-delta-plazo). Back: plazo_nuevo_dias entero >0 obligatorio para tocaPlazo (400 :138); tope PLAZO_MAX_DIAS=36500 días ≈100 años, evita RangeError Date (400 :142, P4 22-jun). |
| Editor de catálogo — Clave de concepto (testid=cm-concepto-clave-N) — sólo si tocaPrograma | text (maxLength 40) | condicional | Front: cada concepto requiere clave.trim() (cmCamposOk, ConveniosModificatorios.jsx:308); sin claves duplicadas (cmSinDup :309, aviso cm-claves-dup). CONGELADO/disabled en existentes y ampliaciones (EditorProgramaConvenio.jsx:73,86). Back: clave obligatoria por concepto (400, convenios.controller.js:144); sin repetidas en el body (400 :148); catálogo nuevo debe incluir TODOS los existentes (400 :196); concepto original CONGELADO — cantidad/PU no cambian, sólo re-enviar igual (409 :233, FIX 22-jun profe). |
| Editor — Concepto/descripción (testid=cm-concepto-nombre-N) | text | no | Front: disabled si existente/ampliación (EditorProgramaConvenio.jsx:91). Back: se persiste en INSERT de conceptos adicionales; el original no se altera. |
| Editor — Unidad (testid=cm-concepto-unidad-N) | text (maxLength 20) | no | Front: disabled si bloqueado (EditorProgramaConvenio.jsx:100). |
| Editor — Cantidad (testid=cm-concepto-cantidad-N) | number (min 0, step 0.001) | condicional | Front: cmCamposOk exige cantidad >0 (no >=0: impide poner en cero un existente, ConveniosModificatorios.jsx:308); disabled en originales/ampliaciones. Back: cantidad >=0 (400 :144); NO reducir por debajo de lo ya estimado en estimaciones no rechazadas (400 :190, criterio de diseño; art. 118 trata excedentes no reducciones); original congelado (409 :231). |
| Editor — P.U. (testid=cm-concepto-pu-N) | number (min 0, step 0.0001) | condicional | Front: cmCamposOk exige PU >0 (ConveniosModificatorios.jsx:308); disabled en originales y ampliaciones (P.U. heredado). Back: pu >=0 (400 :144); en ampliaciones (amplia_a) el P.U. DEBE heredar el del original, validado server-side, no se teclea (400 :243, art. 59 LOPSRM). |
| Editor — Matriz concepto × periodo / celdas (testid=cm-celda-N-P) | number (min 0, step 0.001) | condicional | Front: cmCuadra = cada concepto Σ celdas = contratado con TOL 0.0005 (ConveniosModificatorios.jsx:305); disabled en originales/ampliaciones. Periodos VIGENTES (no se regeneran Etapa 1). Back: guardarMatriz revalida cuadre 100% en SQL (PROGRAMA_DESCUADRE→400 :358); celda de clave/periodo inexistente (400 :324-325); concepto ADICIONAL no se programa en periodo ya cerrado fin<hoy (409 :329, FIX 22-jun (c) no se adiciona al pasado). |
| Panel «Ampliar» (B4) — Cantidad adicional (testid=cm-ampliar-extra) | number (min 0, step 0.001) | condicional | Front: puedeAgregar = extra>0 && ampliarPeriodo (ConveniosModificatorios.jsx:909). Genera fila ADICIONAL con clave derivada CONC-A/-B que HEREDA el P.U. del original (art. 59). Back: validación de ampliación (amplia_a, P.U. heredado, convenios.controller.js:240-243). |
| Panel «Ampliar» — Precio unitario (heredado, candado 🔒) | derivado (read-only) | no | Front: bloqueado, P.U. del original (ConveniosModificatorios.jsx:929-933, art. 59 LOPSRM). Back: server valida que coincida con el original (400 :243). |
| Panel «Ampliar» — Periodo del programa donde se ejecuta (testid=cm-ampliar-periodo) | select (periodos vigentes) | condicional | Front: requerido para confirmar (ConveniosModificatorios.jsx:909,937). Back: el concepto adicional no puede caer en periodo cerrado (409 :329). |
| Motivo / dictamen técnico (testid=cm-motivo) — art. 99 RLOPSRM | textarea | sí | Front: motivo.trim().length>0 (datosOk, ConveniosModificatorios.jsx:331). Back: motivo obligatorio, razones fundadas/dictamen técnico (400, convenios.controller.js:128, art. 99 RLOPSRM). |
| Oficio de soporte (solicitud/autorización) (testid=cm-oficio) — art. 99 RLOPSRM | text (referencia/folio) | sí | Front: oficioRef.trim().length>0 (datosOk, ConveniosModificatorios.jsx:331). Back: oficio obligatorio — soporte documental PREVIO; sin él NO procede (409 requiereOficio, convenios.controller.js:133, FIX 22-jun (a)); se antepone al motivo '[Oficio de soporte: ...]' (:134). El PDF se adjunta DESPUÉS (subirOficioConvenio). |
| Folio del convenio (testid=cm-folio) | text | no | Front: opcional (ConveniosModificatorios.jsx:629-637). Back: si vacío se genera CM-NNN (folioFinal, convenios.controller.js:273). |
| Botón Promover convenio modificatorio (testid=btn-registrar-convenio) | acción | no | Front: puedeRegistrar = !soloLectura && !registrando && !cargandoEditor && datosOk (ConveniosModificatorios.jsx:336). Avisos en vivo: >25% revisión SFP art. 102 RLOPSRM (testid=aviso-sfp), >50% ajuste de costos art. 59 Bis (testid=aviso-ajuste) — informativos. Back POST /api/convenios/contrato/:id: autoridad dependencia/residente/creador (403 :164); contrato no cerrado (409 :166, art. 64); EXIGE bitácora abierta antes de registrar (409 :170, P23, art. 122 RLOPSRM); guardrail variación parametrizable CONVENIO_LIMITE_VARIACION_PCT (default 25, NO tope legal) AVISA no bloquea (aviso_variacion :349). |
| Subir oficio de aprobación PDF por convenio registrado (testid=conv-oficio-subir-ID; input file accept=application/pdf) | file (PDF) | condicional | Front: sólo PDF, !soloLectura (ConveniosModificatorios.jsx:393-396,795-803). Back POST /:convenioId/oficio (multer 10MB PDF): archivo requerido (400 :438); magic-bytes %PDF (400 :441); autoridad dependencia/residente/creador (403 :451); APPEND-ONLY — un oficio por convenio, inmutable (409 :454, índice uq_contrato_doc_oficio_convenio). Obligatorio para autorizar si variación >25%. |
| Botón Autorizar convenio (testid=conv-autorizar-ID) — sobre convenio estado='registrado' | acción (acto de autorización) | no | Front: !soloLectura, deshabilitado mientras autorizandoId!=null (ConveniosModificatorios.jsx:754-763). Back POST /:convenioId/autorizar: SOLO rol 'dependencia' servidor facultado (403, convenios.controller.js:384, art. 59 párr.3 + art. 99 p5); acceso por participación (403 :397); convenio debe estar 'registrado' (409 acto único :398); contrato no cerrado (409 :400, art. 64); GUARDRAIL art. 102: variación monto o plazo >25% exige oficio YA cargado (409 :405-409); APPEND-ONLY sella estado='autorizado'+autorizado_por(JWT)+autorizado_en una sola vez. |

### AmbientePago (ambiente de pago / ciclo de cobro — cascarón de navegación)  ·  HU-21 (encadena HU-14/15/20/21/24)
- **Ruta:** `/pagos/ambiente (acepta ?contrato=ID; ruta fuera del catálogo, no toca permisos.js)`
- **Roles que editan:** Cascarón de navegación, no captura. No usa useVistaHU (no aplica soloLectura). Encadena pantallas reales con sus propios gates. puedeFiniquito = rol ∈ {dependencia, residente} (AmbientePago.jsx:45).
- **Notas:** Cascarón que ENVUELVE el ciclo de cobro sin fundir HU: bloque1 estimación autorizada (HU-15 art.54) → bloque2 tránsito a pago (HU-20) → bloque3 instrucción → bloque4 registro del pago (HU-21, LinkHU gateado a finanzas) → bloque5 cierre/finiquito (HU-24, sólo dependencia/residente; testid=finiquito-informativo si no aplica). No recalcula nada; neto/suficiencia los hace el backend. autorizadas/pagadas se derivan de estimaciones.estado. Los LinkHU muestran 'actor' cuando el rol no puede ejecutar.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato — BannerContratoActivo (contrato activo global) | derivado (banner) | no | Front: al seleccionar carga estimaciones y pagos del contrato (AmbientePago.jsx:68-81). Sin captura propia. |
| Cola global de solicitudes de cobro (Finanzas) (testid=cola-cobro) — descarga soportes (testid=cola-archivo-ID), enlace Registrar pago (testid=cola-pagar-ID) | tabla read-only / acciones de navegación | no | Back GET /api/instruccion-pago/cola: instrucciones estado='emitida' de TODOS los contratos, acotada por participación — finanzas (transversal) ve todas; roles operativos sólo las de sus contratos (instruccion-pago.controller.js:428-457, FIX 22-jun profe). Enlace Registrar pago lleva a /pagos/registro?contrato=&estimacion= (heredando preselección). Descarga PDF: descargarArchivoCobro acotada por participación. |

### AmbienteConvenio (ambiente de convenio modificatorio — cascarón de navegación, ciclo episódico)  ·  HU-03 (encadena HU-03/10/04)
- **Ruta:** `/contratos/convenio-ambiente (acepta ?contrato=ID; ruta fuera del catálogo, no toca permisos.js)`
- **Roles que editan:** Cascarón de navegación, no captura. NO usa HeaderVista ni useVistaHU (evita el aviso 'solo lectura' de HU-03 para roles C). Las acciones reales (promover, autorizar, oficio) viven en HU-03 con su gating (dependencia E).
- **Notas:** Cascarón EPISÓDICO (sólo cuando cambia monto/plazo/alcance) que envuelve HU-03 sin fundir: bloque1 contrato+convenios vigentes → bloque2 promover convenio (HU-03, lee ?contrato=) → bloque3 variaciones y avisos read-only (lo marca el backend: requiere_revision_sfp, delta_monto_pct, delta_plazo_pct; testid=convenio-variaciones, aviso-sfp) → bloque4 oficio de aprobación → bloque5 asiento automático en bitácora (informativo; consulta HU-10 sin preselección) → bloque6 ver en expediente (HU-04 sin preselección). No recalcula nada (el 25% lo marca el backend; umbral parametrizable, criterio del equipo). LinkHU con 'actor' por rol.

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Contrato — BannerContratoActivo (contrato activo global) | derivado (banner) | no | Front: al seleccionar carga los convenios del contrato (api.convenios, AmbienteConvenio.jsx:56-65); 403 si no hay acceso. Sin captura propia. |
| Ver oficio de aprobación (testid=ver-oficio-ID) | acción de descarga (read-only) | no | Back GET /api/convenios/:convenioId/oficio: acotado por participación (403, convenios.controller.js:473); 404 si el convenio no tiene oficio (:476). Sirve el PDF inline. |


---

## Expediente / reportes / finiquito / portafolio

### ConsultaExpediente (Expediente consolidado)  ·  HU-04
- **Ruta:** `/contratos/expediente  (acepta ?contrato=ID para preselección)`
- **Roles que editan:** residente=E. contratista/supervision/dependencia=C. finanzas=sin acceso (permisos.js:15). NOTA: HU-04 es de LECTURA aun para el editor; el único 'edit' real es subir fotos del expediente, gateado por useVistaHU('HU-04').soloLectura (ConsultaExpediente.jsx:536).
- **Notas:** VISOR read-only de UN contrato: 9 bloques (configuracion, catalogo, programa, fianzas, amortizacion, juridicos, roster, convenios, estimaciones — memo :601-715). Datos DERIVADOS server-side; no hay captura salvo búsqueda, periodo y fotos. CÁLCULOS derivados: Importe por concepto = ROUND(cant×pu,2) (:136, art.45 fr.IX RLOPSRM clave capturada); Total neto estimaciones EXCLUYE rechazadas (:484, P3 — append-only: rechazada se sustituye por reingreso HU-16 vía reemplaza_a). Plan amortización art.143 fr.I + art.138 párr.3 RLOPSRM (:279-280). Roster append-only/sustitución art.125 RLOPSRM (:314,:329). Convenios inmutables art.59 LOPSRM/99 RLOPSRM, badge 'Adicional' art.101 RLOPSRM (:130,:407). Botón 'Ver oficio' convenio = descarga blob con token (api.descargarOficioConvenio :390-400). Superintendente VIGENTE sale del roster (art.125), snapshot del alta = fallback (testid config-super-vigente :81-87).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Selector de contrato — componente BannerContratoActivo (contrato activo · Cambiar). label='Contrato activo'. Sin testid select-contrato propio (hereda el contrato activo global). ConsultaExpediente.jsx:747 | select (contrato activo global) | sí | FRONT: sin contrato no se renderiza el expediente (ConsultaExpediente.jsx:763). Al seleccionar carga api.detalleContrato + programa/roster/planAmort/convenios/estimaciones (:559-589). BACK: GET /api/contratos/:id acotado por participación (lib/acceso.js esParteOSupervision); 403 'No tienes acceso a este contrato', 404 'Contrato no encontrado' (:580-585). |
| Buscar en el expediente — testid='input-busqueda'. label='Buscar en el expediente'. placeholder='Tipo de documento (catálogo, programa, fianzas…) o periodo…'. ConsultaExpediente.jsx:778 | text (filtro client-side de bloques) | no | FRONT-only, no toca backend. Tokeniza por espacios y filtra bloques con lógica Y: terminos.every(t => b.haceMatch(t, campo)) (:718-721). Solo afecta visualización en pantalla; al imprimir todos los bloques se muestran (oculto = hidden print:block, :62). Sin validación de formato. |
| Buscar por — <select> sin testid. label='Buscar por'. Opciones: 'documento' (Tipo de documento) y 'periodo' (Periodo). ConsultaExpediente.jsx:788, CAMPOS_BUSQUEDA :27-30 | select | no | FRONT-only. Solo 2 criterios (el profe quitó folio/contratista/empresa/objeto: dentro de un solo contrato son únicos — :23-26). default 'documento' (useState :550). Ramas viejas en haceMatch (folio/contratista/objeto/empresa) quedan inertes. |
| Exportar expediente (PDF) — testid='btn-exportar-pdf'. Botón window.print(). ConsultaExpediente.jsx:802 | botón (acción: impresión consolidada del navegador) | no | FRONT-only. No genera jsPDF; usa CSS print (print:block fuerza todos los bloques abiertos → PDF = expediente consolidado completo, :55-56,:820-822). Sin descargas sueltas por bloque (O9, :18-21). |
| Subir/ver fotos por estimación — componente FotosEstimacion (porGenerador), uno por estimación dentro del bloque 'Resumen de estimaciones'. ConsultaExpediente.jsx:526 | file (galería de evidencia fotográfica, BYTEA) | no | FRONT: soloLectura={soloLecturaExp}=useVistaHU('HU-04').soloLectura → solo residente (nivel E) sube; el resto solo ve (:536,:712,:526). Fundamento art.132 fr.IV RLOPSRM (:521). BACK: endpoints propios de fotos por estimación (BYTEA), acotados por participación. |

### ExportacionReportes (7 reportes del contrato)  ·  HU-19
- **Ruta:** `/reportes  (acepta ?contrato=ID)`
- **Roles que editan:** residente=E. contratista/supervision/dependencia/finanzas=C (permisos.js:33). NOTA: 'E' = botones de exportar habilitados; soloLectura los deshabilita (ExportacionReportes.jsx:36,:123). finanzas SÍ aparece (único grupo con acceso de finanzas).
- **Notas:** 7 reportes: 1 Avance físico (PDF modal+Excel), 2 Avance financiero (Excel), 3 Listado estimaciones (Excel), 4 Observaciones (Excel), 5 Bitácora (PDF modal, requiereBitacora), 6 Modificatorios (Excel), 7 Penalizaciones (Excel). TODOS disponible:true (reportesContrato.js:456-464). NO toca backend/server.js (client-side jsPDF/exceljs :2-5). Citas/cálculos DERIVADOS: curva S corta período en curso en hoy (:90-149); pena por atraso DERIVADA por identidad de carátula = subtotal−amort−retención−deductivas−neto (penaAtrasoDerivada :48-49; art.46 Bis LOPSRM + 86-88 RLOPSRM, tope art.90); 5 al millar = art.191 LFD (fiscal, no pena); R3/R4 incluyen rechazadas por trazabilidad (append-only HU-16 :318,:289); R6 Rev.SFP 'Sí' si Δ>25% art.102 RLOPSRM (:406); conceptos originales congelados/adicionales separados art.101 RLOPSRM (:405). Total neto financiero excluye rechazadas (:250). Botones envueltos en RegionEditable disabled={soloLectura} (:196).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Selector de contrato — componente BannerContratoActivo (hereda contrato activo global). ExportacionReportes.jsx:147 | select (contrato activo global) | sí | FRONT: sin contrato no se exporta (mensaje :150); hayContrato = !!contrato && !!datos && !cargando && !error (:117). Carga 8 fuentes en paralelo, cada una con .catch degradado a null/[] (programa, trabajos, historial, prep, pagos, notas, convenios, observaciones :69-81). BACK: cada GET acotado por participación; 403 'No tienes acceso a este contrato' (:83). |
| Periodo del reporte — testid='select-periodo-reporte'. label='Periodo'. Opciones PERIODOS_REPORTE=['Mensual','Trimestral','Acumulado'] (reportesContrato.js:466). ExportacionReportes.jsx:176 | select | sí | default 'Mensual' (PERIODOS_REPORTE[0], :50). CA-2: el periodo SOLO acota el rango de fechas (avance físico/financiero/bitácora); NUNCA cambia columnas/contenido (:186-190, reportesContrato.js:60-62). Ventana anclada al dato MÁS RECIENTE (ventanaPeriodo :63-70): Mensual=último mes, Trimestral=últimos 3 meses, Acumulado=sin recorte. FRONT-only (generación en cliente). |
| Botones de exportar por reporte — testid='btn-exportar-{id}-{pdf\|excel}'. 7 reportes (CATALOGO_REPORTES) × formatos. ExportacionReportes.jsx:238, BotonFormato :20 | botón (genera PDF jsPDF / Excel exceljs en cliente, o modal imprimible R1/R5) | no | FRONT: botonDeshabilitado(r) = soloLectura \|\| !hayContrato \|\| !r.disponible \|\| (r.requiereBitacora && sinBitacora) (:122-123). R5 Bitácora requiereBitacora=true → deshabilitado si !datos.notas (badge 'Sin bitácora aperturada' :229). R1(PDF) y R5(PDF) abren documento imprimible window.print (HANDLERS[1].PDF='modal' :102,:471/475). Resto = funciones generadoras client-side. Sin validación back en export (100% cliente). |

### Finiquito y cierre del contrato  ·  HU-24
- **Ruta:** `/contratos/finiquito  (acepta ?contrato=ID)`
- **Roles que editan:** SOLO dependencia y residente cierran (puedeCerrar=['dependencia','residente'].includes(rol), Finiquito.jsx:86). El resto consulta el desglose pero no ve el formulario de cierre. NOTA: HU-24 NO está en permisos.js (ruta fuera del catálogo clásico; gate por rol directo en página + server-side).
- **Notas:** SALDO 100% server-side, fuente única (NO recalcula carátula): saldo = Σneto(estimaciones autorizada\|pagada art.54) − Σpagos − anticipo_no_amortizado − ajustes_finales (calcularFiniquito :29-60). anticipo_no_amortizado = max(0, anticipo − amortización aplicada) art.143. a_favor_de: saldo>0.005 → contratista (se paga art.171); <-0.005 → dependencia (reintegra art.171); else 'ninguno' (:55). importe_real_ejecutado (Σ ejecutado×pu) = informativo art.170 fr.IV, NO entra al saldo base (:46-51). Finiquito APPEND-ONLY: 1 por contrato, inalterable, extingue derechos/obligaciones art.172 RLOPSRM. cerrado = estado==='cerrado' \|\| !!finiquito (:127). El finiquito ES una nota de bitácora (definición del profe). El cierre además bloquea el resto del ciclo (gate art.64 en otros endpoints, helper lib/gateCierre.js).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Selector de contrato — componente BannerContratoActivo. Finiquito.jsx:165 | select (contrato activo global) | sí | FRONT: sin contrato no carga desglose (cargar() retorna si !id, :104). BACK: GET /api/finiquito/contrato/:id (finiquito.routes.js:11) → prepararFiniquito acota por esParteOSupervision; 400 'contrato inválido' (id no entero/≤0), 404 'Contrato no encontrado', 403 'No tienes acceso a este contrato' (finiquito.controller.js:76-79). |
| (−) Ajustes finales — testid='finiquito-ajustes'. type=number step=0.01. placeholder='0.00'. Solo visible si !cerrado && puedeCerrar. Finiquito.jsx:189 | number (parametrizable; deductivas finales/sobrecosto/5-al-millar pendiente) | no | default 0 (ajustes==='' → 0 al cerrar, :134). FRONT: onChange re-carga preview server-side cargar(contratoId, e.target.value) → ?ajustes= simula sin persistir (:190, controller :81-82). Sin min/max ni validación de signo en front. BACK: ajustes_finales = r2(Number(req.body.ajustes_finales)) si Number.isFinite, sino 0 (:101,:123; calcularFiniquito :53). Se RESTA del saldo. Criterio del equipo, default conservador 0, no hardcodeado (:19-20; el profe confirma qué conceptos entran). |
| Observaciones (opcional) — testid='finiquito-observaciones'. <textarea> rows=2. label='Observaciones (opcional)'. Finiquito.jsx:211 | textarea | no | FRONT: sin validación de longitud/formato. BACK: observaciones = String(req.body.observaciones).trim() o null (:102); se persiste en finiquitos.observaciones y se concatena al texto de la nota de bitácora (:136) y al documento imprimible (:68). Append-only (parte de la nota inmutable). |
| Cerrar contrato (elaborar finiquito) — testid='btn-abrir-cierre'. Finiquito.jsx:213 | botón (abre confirmación) | no | FRONT: disabled={!data.tiene_bitacora} → no se abre el cierre sin bitácora aperturada (banner 'finiquito-sin-bitacora' :203). Solo se muestra si puedeCerrar (:208). |
| Confirmar cierre — testid='btn-confirmar-cierre' (en bloque 'finiquito-confirmar'). Finiquito.jsx:221 | botón (acción destructiva: cierra el contrato, append-only) | no | FRONT: confirmación de 2 pasos con texto 'inalterable... no se puede deshacer' (:218-219). BACK: POST /api/finiquito/contrato/:id (cerrarFiniquito, transaccional FOR UPDATE :109). VALIDA: 400 id inválido; 404 contrato no encontrado; 403 'Solo la dependencia o el residente asignado puede elaborar el finiquito' (rol dependencia \|\| residente_id==user \|\| created_by==user :113-114); 409 'El contrato ya está cerrado' (estado='cerrado' :116); 409 'Este contrato ya tiene finiquito' (1 por contrato :117-118); 409 'Abre la bitácora... antes' (sin bitacora_aperturas :120-121). Asienta nota tipo 'finiquito' (emisor=residente_id art.53 :139-143) + INSERT finiquitos + UPDATE contratos estado='cerrado' (:154). Art.64 LOPSRM / 168-172 RLOPSRM. |
| Ver documento de finiquito (art. 170) — testid='btn-ver-documento-finiquito' / imprimir 'btn-imprimir-finiquito'. Finiquito.jsx:236,:48 | botón (abre DocumentoFiniquito imprimible window.print) | no | FRONT-only. Solo si cerrado. Documento con contenido mínimo art.170 RLOPSRM (importes, saldo, extinción art.172, garantía vicios ocultos art.66 LOPSRM :71). |

### Portafolio ejecutivo (semáforos multi-contrato)  ·  HU-18
- **Ruta:** `/portafolio (sin ?contrato=; opera sobre MÚLTIPLES contratos)`
- **Roles que editan:** dependencia=E. residente/supervision=C. contratista/finanzas=sin acceso en sidebar (permisos.js:32). NOTA: pantalla 100% READ-ONLY — 'E' no edita; el control real es por PARTICIPACIÓN server-side (portafolio.routes.js:8-10): dependencia/finanzas ven todos, operativos solo donde son parte.
- **Notas:** ÚNICA vista multi-contrato del backlog. CERO captura: todo presentación de datos calculados SERVER-SIDE (GET /api/portafolio, portafolio.controller.js). Semáforo = 3 factores: (1) Avance vs programado (desviación pp, umbral DESVIACION_PP), (2) Atrasos en plazos legales fechables (días ejecución post-término + días art.54 revisión 15d LOPSRM :223-225), (3) Pendientes sin atender (obs abiertas + rechazadas sin reingreso :230-232). Color: total≤1 verde, 2-3 amarillo, ≥4 rojo (:55-59). Avance por VALOR (Σcant×pu) fuente única reusa estimacion-prep (:98-138). Umbrales = criterio del equipo (lib/umbrales-semaforo.js), salvo 15 días = art.54 LOPSRM. RIESGO G6 'pago_sin_avance' (testid 'riesgo-pago-sin-avance'): financiero > físico + 1pp → bandera art.54/143 fr.I RLOPSRM (:234-241; UI :102-107). Comparación Δ mes = SOLO avance físico (CA-3 :215-219). Semáforo PARCIAL '*' cuando no hay programa (:253). Atrasos en autorización/pago OMITIDOS honestamente (faltan sellos autorizada_en/pagada_en :135-137,:284). penalizaciones REAL = Σ retencion_atraso+deductivas de NO rechazadas (:256). IMPORTANTE: portafolio.routes.js aún SIN montar en server.js congelado (snippet para Maiki :17-27).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Agrupar por — testid='select-agrupar-por'. label='Agrupar por'. Opciones AGRUPADORES: 'Ninguno', 'Contratista', 'Ejercicio fiscal'. PortafolioEjecutivo.jsx:303, AGRUPADORES :215-219 | select | no | default 'Ninguno' (:230). FRONT-only (agrupación client-side :252-264). Se QUITÓ 'Tipo de contratación' (el procedimiento de adjudicación no existe en el esquema :213-214). 'Ejercicio fiscal' DERIVADO de EXTRACT(YEAR FROM fecha_inicio) server-side (:74,:263; criterio del equipo: año de inicio=ejercicio presupuestal). Sin validación de formato. |
| Semáforo / fila de contrato — testid='semaforo-dot-{folio}', 'semaforo-badge-{folio}', 'fila-portafolio-{folio}'. PortafolioEjecutivo.jsx:160,:175,:154 | botón/acción de navegación (no captura): clic en dot/badge → /contratos/expediente?contrato=ID; doble clic fila → abre PanelDetalle | no | FRONT-only navegación (irAlExpediente :144). NO hay captura. El COLOR del semáforo y todos los KPIs se calculan SERVER-SIDE. |
| Cerrar detalle — testid='btn-cerrar-detalle-contrato'. PortafolioEjecutivo.jsx:80 | botón (cierra PanelDetalle) | no | FRONT-only (setFolioSeleccionado(null)). |

### AmbienteExpediente (cascarón de cierre documental)  ·  HU-04 (encadena HU-04 + HU-19)
- **Ruta:** `/contratos/expediente-ambiente (ruta NUEVA fuera del catálogo de HU; NO toca permisos.js)`
- **Roles que editan:** residente/contratista/supervision/dependencia (los que ven expediente Y reportes). finanzas FUERA (solo tiene reportes, usa /reportes directo — AmbienteExpediente.jsx:20). NOTA: cascarón read-only de navegación; no edita.
- **Notas:** CASCARÓN de navegación: encadena HU-04 (expediente consolidado) + HU-19 (exportación) SIN fundirlas; cada bloque enlaza a la RUTA REAL de su HU. NO reescribe lógica ni captura datos. Resumen read-only (folio/monto/estado/num estimaciones :100-106) calculado por el sistema. 3 bloques: 1 Contrato+resumen, 2 Expediente HU-04, 3 Reportes HU-19. Estado 'listo' cuando detalle cargado. Números generadores = único reporte pendiente del Equipo 3 (:132).

| Campo | Tipo | Oblig. | Validaciones |
|---|---|---|---|
| Selector de contrato — componente BannerContratoActivo (Bloque 1). AmbienteExpediente.jsx:99 | select (contrato activo global) | sí | FRONT: al seleccionar carga api.detalleContrato + api.historialEstimaciones (informativo). BACK: detalleContrato acotado por participación; 403 'No tienes acceso a este contrato' vía toast (:58). historialEstimaciones es informativo: si falla se omite (:65). |
| Abrir el expediente (HU-04) — testid='link-expediente'. AmbienteExpediente.jsx:118 | link (navega a /contratos/expediente?contrato=ID) | no | FRONT-only. Deshabilitado (pointer-events-none opacity-50) hasta que haya detalle cargado (:118). |
| Exportar reportes (HU-19) — testid='link-reportes'. AmbienteExpediente.jsx:129 | link (navega a /reportes?contrato=ID) | no | FRONT-only. Deshabilitado hasta que haya detalle (:129). |
